# Continue the ERP Goal

Continue ERP-System toward [GOAL](GOAL.md), using [PROGRESS](PROGRESS.md) for current gaps. Follow AGENTS.md and CLAUDE.md. Reply in Mandarin; write code and technical artifacts in English.

1. Inspect Git status, HEAD and worktrees; preserve unrelated changes. Resolve/search `erp-system-project-logic` via `kb_list`/`kb_search`. Verify context against current source/tests, STATUS, SPEC and PROJECT_LOGIC.
2. Run GOAL's count validator. Read the selected registry task, dependencies, packet and evidence. Choose one actionable acceptance gap; announce its expected result and verification. Consult [execution procedures](docs/AI_NATIVE_EXECUTION.md). Check existing candidate work before rebuilding; PROGRESS records TASK-236's separate branch.
3. Implement the smallest complete solution through canonical commands. Preserve tenant isolation, approvals and Demo/PostgreSQL parity. Continue authorized work autonomously. Respect existing authorization; ask only for genuinely missing intent, authority or resources. Never expose secrets.
4. Verify applicable CLAUDE/packet gates and persisted outcomes. Keep local, fixture, Demo, real-provider, CI and production evidence distinct. Record revision, dirty state, environment, actor, expected/actual results and evidence links. Documentation-only: count validator, docs:check, root Markdown/link review and git diff --check.
5. Update dated evidence, packet steps, registry and GOAL only as proven. Done requires four criteria, five steps and common DoD. Refresh PROGRESS counts, fingerprint, blockers and next exit; update affected docs/KB and read back. Never count repeated tests or prose as capability progress.
6. If blocked, record the exact missing resource, owner role and unblock action; continue independent eligible work. Avoid unchanged retries or invented success. Report outcome, acceptance delta, remaining gap and next measurable action; end with four A/B/C/D options, A recommended.
