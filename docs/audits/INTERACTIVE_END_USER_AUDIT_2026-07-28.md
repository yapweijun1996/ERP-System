# Interactive end-user ERP audit — 2026-07-28

## Status

Complete on branch `codex/interactive-uat-round-1` from baseline `33f6cc9`.
This audit did not treat route rendering alone as completion: the sidebar, module
tabs, menus, list drill-downs, declared detail pages and role-specific write/denial
journeys were exercised as signed-in end users. Every confirmed issue below was fixed
and retested before closure. Physical-phone acceptance remains separately blocked by
TASK-017 and is not represented as passed.

## Verified baseline

| Check | Result |
| --- | --- |
| Browser | In-app Chromium, Chinese UI, Acme Singapore, FY2026 P06 |
| Responsive route audit | Pass after the latest remediation: 124 Canonical routes at desktop and 375px; no console/page/layout-contract failures |
| Static and regression gates | Pass after the final v13 tranche and 2026-07-29 PWA follow-up: lint, root/Web typecheck, Demo/API builds, Demo/schema/drift proofs, desktop/375px smoke, the dedicated PWA lifecycle audit, 124-route responsive audit, five-language browser matrix and the full suite with 531 passed / 1 intentional skip / 0 failures (532 total tests). |
| Five-language browser matrix | Pass after the latest remediation: 1439 canonical keys and 124 routes × five languages × desktop/375px; no hard-coded UI findings |
| Enterprise Demo pack | Fresh-database proof passes for v13: SHA-256 manifest, 10,422 linked records, exact role-template permission/scope parity, company-managed Employee base roles, deterministic persona-to-employee/reporting-line identity, complete SG/MY posting-control accounts, real sales approvals with warehouse-specific fulfilment stock, balanced AP cases and payroll/leave/statutory invariants. Existing IndexedDB upgrades additively and legacy shared Employee grants are removed only after the company role exists. |
| Physical phone | Blocked under TASK-017; 375px automation is not physical-device acceptance |

## Findings and remediation ledger

| ID | Severity | Finding | State | Evidence |
| --- | --- | --- | --- | --- |
| IUA-001 | P1 UX | Staff onboarding rendered a broken step indicator, used a 47-item native manager selector and gave only a transient toast for missing required fields. | Fixed and browser verified | Shared wizard rail, searchable manager combobox, five inline errors and focus on the first invalid field. |
| IUA-002 | P1 business logic | New Payroll Run defaulted from the wall clock instead of the selected fiscal period. | Fixed and browser verified | FY2026 P06 now yields `2026-06-01` through `2026-06-30`, pay date `2026-06-30` and a 2026 run number. |
| IUA-003 | P2 i18n/a11y | Modal close names, fiscal controls, account actions, Demo switcher and PWA update prompts exposed English in Chinese; Japanese “Close” was mistranslated as “near”. | Fixed and browser verified for Chinese; five-language matrix pending | Chinese fiscal/account/Demo/PWA surfaces now use translated copy; Japanese close is `閉じる`. |
| IUA-004 | P1 Demo completeness | Enterprise Demo contained no leave approvals, payroll runs or reporting lines although the product specification claimed controlled enterprise cases. | Fixed in Demo pack v6 and browser verified on a historical IndexedDB | Added 12 leave requests, 98 governed leave opening/reservation entries, 6 payroll runs, 282 payroll lines and SG/MY reporting lines without replacing existing IndexedDB data. |
| IUA-005 | P2 UX | Empty master-detail panes compressed their icon, title and guidance into an unreadable row. | Fixed; responsive browser retest pending | Shared centered, stacked and width-bounded empty-state styling. |
| IUA-006 | P1 data correctness | Payroll loaded only the first 100 lines across all runs, so later batches displayed six employees and incorrect totals for a 47-person company. | Fixed and browser verified | Shared bounded pagination reads all 47 company employees per run; API/Demo accept a tenant-scoped `runId` line filter. |
| IUA-007 | P2 business state | A cancelled payroll run was labelled “Draft” and could not be filtered separately. | Fixed and browser verified | Added five-language Cancelled status, neutral tone and filter. |
| IUA-008 | P2 i18n/data boundary | Chinese leave approval rendered the system leave type “Annual” as the unnatural adjective “年度的”, while seeded department data was partly translated and partly left in English. | Fixed and browser verified for Chinese; five-language matrix pending | Added explicit five-language leave-type labels and preserved all seeded department names as business data. |
| IUA-009 | P1 business logic | SG showcase payroll deducted a fabricated 5% monthly income tax and omitted SDL, diverging from the canonical SG statutory engine. | Fixed, integrity-tested and browser verified for SG | SG income-tax withholding is zero; CPF/SDL and MY EPF/SOCSO/EIS/PCB use the canonical engine's documented approximations. Existing deterministic lines are corrected on confirmed pack upgrade. |
| IUA-010 | P1 business state | A draft or cancelled payslip said net pay was already disbursed/credited, falsely implying payment. | Fixed and browser verified for draft | Posted runs show disbursed, drafts show projected and scheduled-not-paid, and cancelled runs state that no payment will be released. |
| IUA-011 | P1 UI/UX | The payslip was capped at 920px, left a large unused desktop canvas, squeezed four cards into a narrow rail and wrapped related records into unreadable fragments. | Fixed and desktop/375px verified | The document now uses up to 1240px, balances a fluid detail column with a 330–360px summary, moves related records to a full-width panel and provides a compact sticky payment action bar. The 122-route mobile contract passed after the change. |
| IUA-012 | P2 UX | The employee directory had 47 SG employees (about 100 group-wide) but no search, forcing administrators to scan the full table. | Fixed and desktop/375px verified | Added accessible, keyboard-stable live search across name, employee number, role and department; search composes with department filters and has a localized no-results state in all five languages. |
| IUA-013 | P1 data/business integrity | Showcase employees had annual-leave entitlement in the employee master and pending leave requests, but their profile projected `0 / 0` because no immutable opening or reservation entries existed. Historical SG data and MY also lacked governed leave setup. | Fixed and browser verified in SG/MY on a historical IndexedDB | Added self-healing SG/MY calendars, calendar versions, Annual/Medical/Unpaid policies, 94 deterministic annual openings, four pending annual reservations and integrity assertions for every showcase employee. Both company profiles now show `13 / 14` with one pending day reserved. |
| IUA-014 | P2 identity/UX | The 12 Demo users were linked to generic “Showcase Employee” names, so the employee profile contradicted the signed-in persona (for example Company Admin Chen Wei appeared as Employee 001). | Fixed and browser verified in v5/v6 | Persona-linked SG/MY employee records now use the same human name, email, department and role as the real session identity; automated tests reject mismatches. |
| IUA-015 | P2 i18n | The employee profile still rendered the raw legacy leave type through generic word translation, showing Annual as “年度的” even though the approval page had been corrected to “年假”. | Fixed and Chinese browser verified; five-language matrix pending | Employee-profile leave rows now use the explicit Annual/Medical/Unpaid HR labels in all five languages. |
| IUA-016 | P1 cross-company context | After switching from Acme Singapore to Acme Malaysia, the company context changed but the fiscal-period button retained `SGD`, presenting the wrong legal-entity currency. | Fixed and bidirectional browser verified | Company switching now repaints the fiscal context from the refreshed active-company data while preserving the selected fiscal period. SG→MY displays MYR and MY→SG displays SGD; the route audit now enforces both directions. |
| IUA-017 | P2 responsive UX | The English payroll-run detail footer was 31px wider than its 419px detail rail, making the approval actions horizontally overflow. | Fixed and desktop/375px verified | Payroll actions now wrap within the detail rail, use container-safe padding and constrain each button independently of the viewport width. The full 122-route responsive audit passes after the change. |
| IUA-018 | P2 quality tooling | The five-language audit treated employee names, departments, job titles and a payroll document identifier as untranslated UI, producing 25 false blocking findings. | Fixed and full five-language matrix verified | Business-data nodes are now explicitly marked at the shared filter and HR table boundaries; the payslip breadcrumb identifier is covered by a narrowly scoped document-number pattern. The 122-route × 5-language × 2-viewport audit passes. |
| IUA-019 | P1 permission UX/logic | A Sales user correctly had Purchasing hidden, but opening `#purchasing-home` directly claimed that the company module was inactive and told the user to enable it. | Fixed and real Sales-session/full regression verified | Route guarding now distinguishes permission denial from company module state, renders a localized non-disclosing `403 · Permission required` panel, suppresses the denied module's subnavigation and confirms that no records were loaded. The automated desktop audit now repeats the Sales direct-route denial and restores Superadmin before continuing. |
| IUA-020 | P1 dashboard i18n/business truth | The Chinese Superadmin dashboard mixed several English Demo narratives into operational cards, rendered `now ago` / `today ago`, and still claimed Purchasing and HR would be connected in a later task although both modules are Canonical. | Fixed and Chinese browser verified; five-language matrix pending | Demo-specific dashboard narratives, approval kinds/actors and relative ages now use explicit five-language keys. The stale roadmap claims are replaced with current P06 purchasing and governed HR persona guidance. |
| IUA-021 | P1 dashboard navigation | The overdue receivables card promised to open aging but targeted `dashboard`, so a real click silently kept the user on the same page instead of opening the AR aging report. | Fixed and Chinese browser verified; responsive regression pending | A real click now opens `#ar-aging` with the localized `应收账龄` heading; the responsive route audit also exercises the card and rejects route drift. |
| IUA-022 | P1 approval UX/access | The dashboard claimed its three approval rows spanned four modules, while only two were present; “Open all” opened a purchase-order decision instead of the cross-module queue. | Fixed and superseded by the data-truth correction in IUA-034; role/matrix regression pending | The dashboard card, sidebar and “查看全部” now share the permission-filtered cross-module `审批收件箱`; row sources are rebuilt from real approval facts rather than synthetic draft rows. |
| IUA-023 | P1 quick-create accessibility/i18n | Five quick-create document composers had no level-one heading, while Chinese module tabs used misleading machine translations such as `委员会` for Sales Commission and `增强现实老化` for AR Aging. | Fixed and all seven Chinese quick-create titles browser verified; five-language matrix pending | Sales order, purchase order, work order, journal, payment voucher, stock adjustment and item composers expose an `h1`. Finance, Purchasing and Sales tabs use contextual business labels rather than isolated machine translations. |
| IUA-024 | P1 accounting date control | Manual journals, payment vouchers, stock adjustments and purchase orders defaulted from the workstation clock (`2026-07-28`) while the selected/open posting period was FY2026 P06, silently placing financial work outside the active period. | Fixed and journal/PO/adjustment browser plus domain verified; payment step blocked by IUA-025 | Transaction composers derive the default from the selected fiscal-period end (`2026-06-30` for P06). Manual-journal posting and reversal fail closed unless exactly one open accounting period covers the transaction date; 10 targeted domain/API tests pass. |
| IUA-025 | P1 Demo completeness | The payment-voucher quick create lists 49 SG suppliers, but every displayed open balance is zero; selecting a supplier reports no unpaid invoices, so the user cannot reach the payment-and-review step or demonstrate AP settlement. | Fixed and historical IndexedDB/browser payment post verified | Demo pack v7 adds one received PO, receipt/stock movement, unpaid supplier invoice and balanced three-leg AP journal per SG/MY legal entity. The existing browser database upgraded without replacement; SG supplier 001 exposed S$1,090, posted as `PV-2026-0001`, settled the invoice and produced equal AP/bank legs with S$0 difference. |
| IUA-026 | P2 quick-create selection UX | Purchase order and payment voucher used 50-option native supplier selectors, while stock adjustment exposed a 100-item native selector with no search. | Fixed and Chinese browser verified; five-language/responsive matrix pending | Supplier search reduces 50 options to the matching code/name and restores all options when cleared; item search does the same for 100 SKUs. Both expose a localized governed no-match state without leaving an unintended selectable item. |
| IUA-027 | P1 quick-create render regression | The first localization pass called the global translation helper from functions that already used a local `t` totals variable, causing stock adjustment and payment voucher to fail with `t is not a function`. | Fixed and Chinese browser verified | Totals variables were renamed, the PWA cache was advanced, and both composers now render their `h1`, localized labels and selectors without a fallback/sample-data substitution. |
| IUA-028 | P1 payment-voucher i18n | After selecting the new unpaid Demo invoice, payment review still mixed English headings, fields, balance explanations, posting logic and its primary action into the Chinese workflow. | Fixed and Chinese browser verified; five-language matrix pending | The complete two-step voucher surface now uses dedicated English/Malay/Chinese/Japanese/Vietnamese business copy, including the accounting explanation and P06 payment date. |
| IUA-029 | P1 payment-detail truth/i18n | A successfully posted voucher showed a zero supplier balance but still claimed unpaid invoices remained, while the accounting equality `Dr = Cr` was machine-translated as `博士=铬`. | Fixed and Chinese browser verified; five-language matrix pending | The posted detail reports a fully settled supplier at S$0 and uses valid accounting labels (`借方 = 贷方`, `Debit = Kredit`, `借方 = 貸方`, `Nợ = Có`) instead of literal word translation. |
| IUA-030 | P1 deterministic Demo date | New Work Order defaulted to the workstation date (`2026-07-28`) and due date (`2026-08-04`) although the enterprise Demo manifest fixes its operational business date at `2026-07-27`. | Fixed and Work Order/Sales Order browser verified; matrix pending | Demo operational composers read the signed `2026-07-27` business date, while API/production follows the workstation. Work Order now defaults to `2026-07-27`–`2026-08-03`; direct Sales Order uses `2026-07-27`. |
| IUA-031 | P1 sales navigation i18n | Chinese Sales subnavigation used generic machine translations (`查询`, `语录`) for Enquiries/Quotations and left `Invoices` in English, obscuring the lead-to-cash information architecture. | Fixed and all 15 Chinese Sales tabs browser verified; five-language matrix pending | Every Sales route now has an explicit contextual label in English/Malay/Chinese/Japanese/Vietnamese, including `客户询价`, `销售报价`, `销售发票` and customer-specific credit/debit notes. |
| IUA-032 | P1 global-search i18n/data truth | The Chinese command palette exposed almost every navigation and create command in English and offered fabricated static documents (`SO-26-0418`, `PO-26-0291`, `JE-26-0611`) unrelated to the active Demo database. | Fixed and Chinese browser verified; five-language matrix pending | Route commands reuse canonical localized titles, create commands use explicit five-language labels, and the document group contains only real loaded records. Searching `SO-2` produced one result and opened the exact canonical detail. |
| IUA-033 | P1 approval/posting integrity | Draft confirmation trusted only `sales_order.status`. An inconsistent import or legacy write could leave an approval row pending/rejected while changing only the order to `draft`, then issue stock, create an invoice and post GL without approval. | Fixed and domain regression verified; API/PostgreSQL suite pending | The row-locked posting boundary now also locks the approval row and requires `approved` whenever a request exists; pre-migration drafts without an approval row remain compatible. The corrupt `draft + pending` regression proves zero stock, invoice and GL writes. |
| IUA-034 | P1 dashboard business truth | Demo refresh labelled every non-confirmed sales draft as an approval even though the canonical sales approval register contained zero requests. This falsely presented the deliberately confirmable SO-2 and rollback SO-3 teaching drafts as maker-checker work. | Fixed and historical IndexedDB/Chinese browser verified; role/matrix regression pending | The cross-module inbox now projects only pending `sales_order_approval`, `purchase_order_approval` and `leave_request` facts. Manifest v9 adds one real pending sales approval per SG/MY company while preserving SO-2/SO-3 semantics. |
| IUA-035 | P1 sales-order UI/UX | The sales-order detail was capped at 1080px, leaving its line table around 560px wide beside a fixed rail; the crowded six-stage progress rail and dense header reduced scanability, while 375px had no bounded line-table or progress overflow behavior. | Fixed and desktop/375px browser verified; route matrix pending | The page now uses a semantic H1 and up to 1280px, yielding an 894px desktop main column plus a 320px summary. Metadata has stable proportions; line items and the six-stage rail have their own keyboard-safe responsive scrolling. Desktop and 375px show no document-level horizontal overflow. |
| IUA-036 | P1 PWA/i18n cache | A source update loaded newly added Chinese keys but retained older values for existing Chinese keys because lazy locale JSON requests had no release version, allowing the service-worker/runtime cache to serve a stale pack independently of `i18n.js`. | Fixed and Chinese browser verified; role/matrix regression pending | Lazy locale requests now inherit the release query version from the i18n runtime URL, so code and all non-English packs advance atomically instead of mixing releases. |
| IUA-037 | P1 dashboard business truth | The cross-module inbox contained two real pending leave requests for Superadmin, while the separate leave-approval work card still displayed zero and claimed this account had no work. | Fixed and Chinese Superadmin browser verified; role/matrix regression pending | The adapter now counts real pending leave facts, while the dashboard derives the displayed count from approval rows visible to the active role and selects truthful zero/pending copy without leaking hidden approvals. The live dashboard now shows 2 and the matching pending-work narrative beside the same two leave rows in the approval queue. |
| IUA-038 | P1 sales approval state truth | After a real sales approval was completed, the approval register correctly showed Approved but the sales-order list still labelled the same draft as Draft. | Fixed and Chinese Superadmin browser verified; role/matrix regression pending | Approval status now influences the list only while the base order remains a draft: pending, rejected and approved render as their respective effective states, while a subsequently confirmed/cancelled order continues to use its base lifecycle state. The same `DEMO-SO-APP-SG-0001` record now shows Approved in both the approval register and order list. |
| IUA-039 | P1 inventory/fulfilment truth | The approved Demo sales order showed 10 units available and an OK badge, but confirmation failed because its selected fulfilment warehouse had zero; the UI had summed stock across warehouses. | Fixed; fresh proof and historical Chinese browser path verified; role/matrix pending | Sales-order availability now uses the same selected warehouse as confirmation. Each controlled SG/MY approval case receives 25 idempotent opening units in that exact warehouse, with a proof asserting stock is at least the order quantity. Historical v8→v9 upgrade changed the live SG line from false 10 aggregate units to 25 WH-SALES units; confirmation then consumed 10, left 15, closed the order and produced `INV-DEMO-SO-APP-SG-0001` plus stock/GL postings. |
| IUA-040 | P1 sales analytics truth | While `DEMO-SO-APP-SG-0001` was approved but not yet confirmed, the order list showed Approved but the sales overview status chart still grouped it under Draft because analytics used only the base lifecycle column. | Fixed in canonical analytics; browser/matrix retest pending | The analytics query now joins the one-to-one approval record and groups draft-base orders by effective Pending Approval, Approved or Rejected status; confirmed/cancelled orders continue to use their base lifecycle state. |
| IUA-041 | P1 supplier navigation/UX | Supplier codes were styled as blue document links, but the register explicitly had no row action and no supplier detail route, so clicking `DEMO-SG-S001` did nothing. | Fixed; Chinese browser flow and full 124-route desktop/375px audit verified | Supplier rows and their action menus now open a company-scoped canonical detail showing only maintained master fields and real PO, invoice and outstanding-AP facts. Unmodelled contact and payment terms are explicitly labelled “Not maintained” instead of being presented as fabricated business data. The exact `DEMO-SG-S001` path, return action and related paid-invoice navigation were exercised against the historical Demo database. |
| IUA-042 | P2 global-shell alignment | The 30px account avatar button inherited native button padding, leaving a 16px content box for its 19px fallback SVG and shifting the visible icon 1.5px to the right of the circular button centre. | Fixed and Chinese browser verified; full matrix pending | Topbar and account-menu avatar containers now use zero-padding flex centring, border-box sizing, zero line-height and block-level media so fallback SVGs and real photos share the same optical centre. Browser geometry changed from a `+1.5px` horizontal offset to exactly `0px`; the open-menu 42px avatar also measures `0px` on both axes. |
| IUA-043 | P1 release-proof stability | The post-fix Demo smoke run passed at 375px but its desktop pass failed inside the existing manual-journal proof with `Exactly one accounting period must cover the posting date.` | Fixed; desktop and 375px pass in one rebuilt Demo run | The smoke fixture still hard-coded July journal and bank-statement dates after product posting rules correctly moved the selected working period to FY2026 P06. The proof now reads the active company's open accounting-period boundaries from canonical data and posts/reverses/reconciles on that period end without weakening the locked/missing-period guard. `npm run build:demo && npm run smoke` passes both viewports with zero console/page errors. |
| IUA-044 | P1 calendar discoverability/access UX | The canonical Team Calendar already provided month, week and list views, filters, conflict indicators and privacy-redacted detail, but its only visible entry was a capability-gated subtab inside My Work. Even Superadmin and managers could reasonably conclude that the ERP had no built-in calendar. A non-manager who retained the direct URL saw a generic load error rather than the governed permission panel. | Fixed; real-role browser proof and full 124-route desktop/375px audit passed | A persistent topbar calendar entry now opens the existing canonical workspace for users with `employee.team.read`; it is hidden for users without team capability, direct calendar/approval routes render the localized non-disclosing 403 panel, the icon uses the active locale and exposes an active-page state. |
| IUA-045 | P1 listing detail contract | Many transaction registers had no row action at all, while others silently lost their detail action when a dedicated fixture or status-specific document was unavailable. Stock Movement promised drill-down but all 100 visible rows were static; User Management and several pricing/control registers also presented records that could not be inspected. | Fixed; all 40 transaction-list routes are covered by desktop/375px row-action and first-record click audits; Chinese browser proof passed for Stock Movement, User Management and native Sales Order detail | The shared transaction-list SSOT now preserves every dedicated detail route and supplies a read-only, localized record preview when no dedicated action is available or enabled. Report rows remain intentionally static. The audit fails if any populated transaction list contains a static row or if clicking its first record opens neither a native detail nor the governed preview. |
| IUA-046 | P1 detail-page UI/UX contract | Detail screens had drifted into incompatible page shapes: 14 legacy document routes lacked a semantic H1, 11 line tables had no bounded narrow-screen scroll container, the credit-note empty state lost its page identity, and Sales Return presented an actionable inventory/credit decision as a sparse legacy document with a detached full-width footer. On 375px the first standardized Sales Return footer also covered the return line and appeared before the totals/reason context. | Fixed; all 29 declared detail routes pass their dedicated desktop/375px audits, and Chinese browser proof passed for Sales Return plus native Sales Order | Legacy document pages now share a 1280px canvas, semantic title contract and keyboard-safe line-table boundary. Sales Return is a real `case-detail-v1` view with lifecycle, source invoice/delivery/warehouse facts, return lines, totals and governed decision actions. Mobile case/posting actions are placed after review/accounting context instead of obscuring it. The audit separately exercises 15 document, 6 case, 4 master-editor, 1 ledger and 3 posting detail routes and rejects non-semantic titles, uncontrolled tables, legacy case chrome, content-covering mobile actions and action/document width drift. Missing singular Sales Order/Return route labels were added in all five locales, eliminating the observed Chinese fallback warnings. |
| IUA-047 | P2 i18n/data boundary | The post-detail five-language matrix treated employee names, an approval reason, warehouse names and PO/GRN/source references as translatable UI, while the Stock Movement explanation remained hard-coded English. Japanese and Vietnamese also labelled inventory movements as stock-market movement. | Fixed; 124-route × five-language × desktop/375px matrix passed | Dynamic names, master-data labels, audit reasons and document references now carry an explicit business-text boundary. The Stock Movement explanation uses a dedicated five-language key, and Japanese/Vietnamese inventory terminology now means warehouse stock movement rather than securities volatility. |
| IUA-048 | P3 i18n/console hygiene | A genuine missing status value rendered as the em dash placeholder, but the shared status badge attempted to translate it as the key `st.—`, producing a console warning even though the visible result was acceptable. | Fixed; stricter 124-route desktop/375px regression passed | Empty and em-dash statuses now render as a neutral placeholder without entering the translation lookup. Real status values continue through the localized status dictionary. The route audit now blocks this placeholder warning and the two singular Sales detail-route fallback warnings. |
| IUA-049 | P1 permission-aware shell/data exposure | Restricted personas inherited controls and dashboard facts from Superadmin: Viewer could see quick-create, Settings, unrelated companies and approvals; Sales could see cross-module KPI/cards; module navigation and direct create URLs were not consistently action-gated. | Fixed; all 12 real personas and governed direct-URL paths browser verified | Navigation, quick create, company switcher, Settings, account actions, dashboard KPI/cards and approval rows now derive from the same effective permission payload. Company lists are membership-bounded, Master Control remains Superadmin-only, and direct create routes show a non-disclosing 403 without rendering their form. The full 124-route desktop/375px audit includes an automated Sales shell/direct-denial regression. |
| IUA-050 | P1 role-template/Demo authorization drift | Demo role SQL had diverged from `ROLE_TEMPLATES`: Manager lacked project approval; Warehouse lacked inventory actions and `warehouse.read`; Production lacked warehouse/quality actions and scope; HR lacked payroll actions; Finance roles omitted governed export, tax-evidence and budget capabilities; Company Admin used a nonexistent permission key. | Fixed in Demo pack v10; fresh PGlite proof and 12-persona browser matrix passed | The deterministic-pack test now compares every non-Superadmin Demo role's exact permission and scope sets against the authoritative catalog. v10 adds the missing capabilities/scopes, uses `session.switch_company`, and removes the obsolete `company.switch` key during persisted upgrades. Warehouse now visibly receives Stock Adjustment/New Item, Production retains its necessary warehouse, and Buyer/Viewer no longer inherit warehouse access from `inventory.read`. |
| IUA-051 | P1 personal workspace/admin boundary | Personal Activity and Notifications were classified as Admin routes. Ordinary users could lose the pages or receive Admin navigation, while an HR reader could see Admin tabs it could not actually open. The first Account shell also exposed its untranslated key/English fallback in non-English locales. | Fixed; Viewer and HR browser verified; responsive and five-language audits passed | Personal Activity and Notifications now form a permission-neutral `account` module with their own shared two-tab shell. Admin subnavigation filters each route by fine-grained permission; HR sees only Users and Roles & Permissions, while Viewer opens both personal pages without gaining any Admin navigation. The shell reuses canonical `acct.activity` and `route.notifications` keys, and the final 124-route × five-language × desktop/375px matrix has zero blocking findings. |
| IUA-052 | P1 deterministic Demo upgrade integrity | Role/data content changed while the manifest still reported v9, so an existing IndexedDB treated the new hash as already installed and could silently retain stale permissions. | Fixed; fresh v10 browser environment and manifest/hash test passed | The data pack and browser adapter now declare v10, the service-worker/runtime asset versions advance together, and the persisted pack marker is updated only after the idempotent SQL transaction succeeds. Existing v9 databases receive the new role rows and targeted obsolete-key cleanup instead of being reset. |
| IUA-053 | P1 MY sales posting completeness | The real MY sales approval could be approved, but confirming the released order failed with `Account 1100 not configured`; the Demo pack had AP/inventory accounts for MY but omitted AR, revenue and output-tax posting controls. | Fixed in Demo pack v11; historical IndexedDB and live MY chain verified | v11 idempotently provides the complete SG/MY sales, purchasing, treasury and landed-cost posting account set. `DEMO-SO-APP-MY-0001` then confirmed in one transaction, changed stock from 35 to 25, closed the order and created posted `INV-DEMO-SO-APP-MY-0001` for RM125. |
| IUA-054 | P0 closed-period financial control | Payment Voucher accepted `2026-05-31` while P05 was locked, settled the MY supplier, and posted RM1,250 AP/cash legs. Bank Receipt used the same unguarded treasury-posting pattern. | Fixed in the shared domain boundary; focused tests and fresh live browser proof passed | A shared row-locking posting-period guard now requires exactly one open company accounting period for manual journals, payment vouchers and bank receipts. Locked-period unit proofs assert zero voucher/receipt/GL writes and an unchanged unpaid invoice. In a fresh SG browser database, P05 was rejected with `Accounting period May 2026 is locked`; the same form remained retryable and P06 then posted `PV-2026-0001` with S$1,090 equal debit/credit and zero supplier balance. |
| IUA-055 | P1 permission-aware finance action | Finance Checker could read a posted payment voucher but still saw `New voucher`; clicking it only then reached the correct non-disclosing 403 because the detail header action ignored `finance.create`. | Fixed and real Checker/Preparer browser verified | The posting detail exposes the creation action only when the effective session includes `finance.create`. Checker retains read access with no create button; Finance Preparer retains the visible working action and the direct route remains permission-gated. |
| IUA-056 | P1 inventory input truth | The stock-count field displayed `-1` but its input listener silently coerced the internal value to zero, so confirmation posted a full write-down to zero while the operator still saw `-1`. The domain correctly rejected a truly negative count, but the UI submitted a different value than it displayed. | Fixed and live Warehouse browser verified | Negative/non-numeric counts now remain visibly invalid, set `aria-invalid`, show governed guidance and disable posting; no hidden coercion occurs. The domain regression independently rejects negative counts with zero draft, movement and GL rows. |
| IUA-057 | P0 inventory period control | Stock adjustment writes both inventory movements and GL variance legs but had no accounting-period guard. Adding a guard only at posting could also leave an unreachable draft when the quick composer first created the document in a locked period. | Fixed at UI and domain boundaries; live Warehouse and focused rollback proofs passed | The composer blocks dates outside the selected P06 open-period range before creating a draft. The posting command independently row-locks the company period and leaves a draft retryable with zero stock/GL effects if a direct caller targets a locked period. Live P05 input was rejected before confirmation; P06 posted a one-box SG-GADGET write-down, changed the warehouse balance 97→96 and produced a S$13 variance impact. |
| IUA-058 | P2 regression-fixture completeness | The new payment-period guard correctly caused the full suite's supplier-debit-note net-payment scenario to fail because its legacy fixture created a payment dated `2026-01-12` without creating any accounting period. | Fixed and full suite verified | The scenario now owns an explicit open January 2026 period. Its focused three-test suite passes, followed by the complete 530-pass / 1-skip / 0-failure run; the production guard remains fail closed. |
| IUA-059 | P2 release-proof stability | Under concurrent full-suite and browser-matrix load, Account Ledger and Journal Entry recovered correctly but later than the audit's fixed 250 ms sleep, producing two false layout failures. | Fixed and focused/full route audits verified | Ledger, Journal Entry and Goods Receipt retry checks now poll the actual error-state boundary for up to two seconds instead of assuming a workstation speed. Focused ledger/posting audits and the repeated 124-route desktop/375px audit pass. |
| IUA-060 | P1 setup/identity | First-run Demo setup defaulted to `admin`/`admin@acme.co`, then attempted to create a duplicate identity already owned by the deterministic pack; a collision was discovered only at Finish. | Fixed; focused tests and fresh-browser setup verified | Exact seeded username/email reuses the identity inside the setup transaction and applies the entered administrator profile and credential. A conflicting email now returns to the Admin step with an inline, focused error instead of failing ambiguously at Finish. |
| IUA-061 | P1 privacy/boot UX | During cold PGlite startup the static prototype identity `Dana Reyes · Northwind · PRODUCTION` was briefly visible before the canonical session loaded. | Fixed; 50 ms cold-boot polling and screenshot verified | The shell starts auth-locked behind a neutral loading view, exposes no application identity or data, and unlocks only after the first awaited route render. Cold boot ends at Avery Tan / Acme Singapore / Demo with zero console errors. |
| IUA-062 | P1 purchasing workflow truth | Purchase Order review claimed orders below S$50,000 auto-issued, while the canonical command correctly created every PO as Pending Approval under maker-checker control. | Fixed; real Buyer write/readback verified | The UI no longer invents a threshold. Review and completion copy state that approval is required; a real Buyer created `PO-20260630-AD8D9B65`, received Pending Approval, could read it back and remained denied from Sales. |
| IUA-063 | P1 Demo account security | A manually created staff account could enter the normal dashboard with its temporary password without seeing the mandatory first-login password change. | Fixed; activation and permanent-password re-login browser verified | Demo login now verifies the stored PBKDF2 credential, limits passwordless one-click access to the fixed 12 personas, renders the five-language activation shell before any ERP route, completes the canonical activation transaction and signs the employee out. Permanent-password re-login succeeds with `password_change_required=false` and cleared expiry. |
| IUA-064 | P1 staff authorization | Atomic Staff activation looked only for a company-owned `Employee` role, while upgraded tenants could still have the compatibility role with `company_fn=null`; the new employee therefore received only the selected job role. | Fixed; rollback/lifecycle tests and live activation readback verified | Activation accepts the company role or compatibility fallback and always unions the Employee base role. The verified employee finishes with `Employee + Viewer`, one company membership and no credential stored in a draft. |
| IUA-065 | P1 Demo identity/approval routing | `viewer@acme.co` remained linked to the compact seed's Marcus Silva record rather than deterministic Jordan Lee, whose intended direct manager is Mei Lin. Governed leave therefore fell back to HR instead of appearing in the Manager persona's queue. | Fixed in Demo pack v13; fresh-browser submit/approve and SQL readback verified | The upgrade releases a same-company legacy link before assigning each persona to its canonical employee number. Jordan Lee (`DEMO-SG-E012`) now reports to `manager@acme.co`; a two-day leave produced a user-bound Direct manager approval, appeared privacy-redacted in Mei Lin's queue and completed with both leave and approval statuses `approved`. |
| IUA-066 | P1 Demo role upgrade integrity | Adding the company Employee base role left Viewer's older shared Employee assignment in place, exposing `Viewer, Employee, Employee` and two authority sources; retaining the same pack version would also skip the repair on an existing IndexedDB. | Fixed in Demo pack v13; manifest, identity and i18n proofs passed | v13 first creates/assigns the managed company role, then removes only the legacy shared Employee assignment for deterministic personas. Tests reject missing base roles, legacy persona assignments and viewer/reporting-line drift; the changed version/hash forces an additive upgrade. |
| IUA-067 | P2 regression tooling | The Sales shell audit treated the now-required My Work navigation and persisted role-assignment labels as permission leakage/hard-coded UI. | Fixed; final responsive and five-language matrices passed | Sales is expected to have `Home + Sales + CRM + My Work` through its job plus Employee roles, while unrelated modules remain absent. Role-assignment strings are classified as administrator-maintained business data; `Viewer, Employee` is shown once. |
| IUA-068 | P1 PWA update lifecycle | After loading a new source fingerprint, the independent source-hash detector and waiting service worker could each show an indistinguishable update prompt for the same release. Choosing Later did not identify the deferred worker version, and the fixed bottom toast could therefore look like an update that never completed. | Fixed in PWA v208; dedicated real-service-worker lifecycle audit and in-app browser proof passed | The service worker is now the single update authority and reports its exact cache version over `MessageChannel`. Registration bypasses the HTTP cache for `sw.js`; Later suppresses only that worker version for the tab session, a genuinely newer version still prompts, Update now disables duplicate actions and reloads exactly once after `controllerchange`, and the obsolete source fingerprint/query marker is removed. |

## Role and journey closure

| Journey | Result |
| --- | --- |
| Superadmin / Company Admin | Pass: setup, company/module/user/role shell, cross-company context and non-disclosing permission boundaries. |
| Viewer / Employee | Pass: company read role plus Employee self-service, real temporary-password activation, linked employee context and governed leave submission. |
| Manager | Pass: assigned, privacy-redacted leave queue and real approval; unrelated Finance route returns 403. |
| Sales / Buyer | Pass: Sales navigation/direct-denial boundary; Buyer created a real PO that correctly remained Pending Approval. |
| Warehouse / Production | Pass: negative count and locked-period rollback, valid adjustment stock/GL effect; Production created and released a BOM-backed work order and remained denied from Purchasing. |
| Finance preparer / checker | Pass: prepare-versus-release controls, maker-checker action visibility, locked-period treasury rollback and SG/MY AP settlement. |
| HR / new Staff | Pass: Staff onboarding, Employee base role, temporary credential, forced activation and permanent-password re-login. |
| Service | Pass: restricted navigation, direct Finance 403, ticket creation, assignment, resolution and canonical detail readback. |
| Listings and details | Pass: 40 populated transaction-list routes open a native detail or governed preview; all 29 declared detail routes pass desktop and 375px document/case/editor/ledger/posting contracts. |
| Languages and responsive UI | Pass: 124 routes × five languages × desktop/375px, 1439 canonical keys and zero blocking findings. |
| Physical phone | Blocked: TASK-017 still requires a human with a real device. |

## Final evidence

- `npm test -- --maxWorkers=2`: 531 passed, one intentional skip, zero failures.
- Lint, root/Web typecheck, 75-migration Demo schema, 232-table drift, Demo pack v13
  hash, API build, Demo build and desktop/375px smoke passed.
- `npm run audit:screens`: 124 Canonical / 0 Preview routes passed desktop and 375px.
- `npm run audit:i18n`: 124 routes × five languages × two viewports passed.
- `npm run audit:pwa-update`: baseline install is silent, deferred v208 audit-b prompts
  once, later v208 audit-c prompts normally, and Update now activates/reloads once.
- P0–P2 browser images are under `docs/audits/assets/2026-07-28/`, including setup,
  neutral boot, first-login activation, employee submission, manager approval and
  completed service-ticket evidence.
