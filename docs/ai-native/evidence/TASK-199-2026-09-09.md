# TASK-199 Evidence — 2026-09-09

## Read-only production availability recheck

- **Linked task:** [TASK-199](../INHERITED_TASKS.md#task-199--availability-and-exact-release-identity).
- **Severity:** Release-blocking availability and deployed-identity evidence gap; no repository source defect is inferred from this probe alone.
- **Scope:** Public Cloudflare origin `https://gmb01.xyz/erp`, expected release revision `ff6e0d9355c38ce06065267034c020ac55eea7e9`.
- **Safety boundary:** The existing verifier performs bounded read-only HTTP checks. No login, tenant write, migration, reset, reseed, deployment or credential use occurred.

### Reproduction

Command executed from the repository root:

```bash
npm run verify:release -- https://gmb01.xyz/erp --expected-revision ff6e0d9355c38ce06065267034c020ac55eea7e9
```

Observed result:

```json
{"status":"failed","code":"root_http_502","message":"root returned HTTP 502."}
```

The process exited with code `1` in 0.152 seconds and emitted no additional
stderr. The verifier stopped at the root response, so no health, setup-status,
release-manifest or asset identity is claimed from this run.

### Interpretation and next gate

The production origin remains unavailable at the public entry point, so current
API health, deployed revision, asset hashes, monitoring and rollback evidence
cannot be verified. This is the same release-blocking boundary recorded on
2026-09-08, freshly reproduced without changing remote state. Further retries
require an external origin/tunnel/host state change or authorized operator access;
local release-writer and verifier tests remain valid but cannot substitute for
deployed evidence.

## Local production origin diagnosis — 2026-09-10

The user identified the current Mac as the production host. Public /erp and
/erp/health return 502. The active system launch daemon starts cloudflared with
/etc/cloudflared/config.yml, whose gmb01.xyz rules include /idp and the fallback
localhost:6859 but no /erp rule. The home-directory config has /erp -> localhost:8791,
but it is not the active daemon config. Port 8791 is the launch-agent Node placeholder
from deploy/erp-server.mjs; its 200 health is not ERP application/database health.
Docker Desktop and the default context show no ERP application containers or named
ERP data volumes, only the separate test PostgreSQL and unrelated containers/volumes.
The repository .env and documented output backup are absent. No config, production
process, database or volume was changed. The operator is choosing fresh provisioning
versus recovery of old data before deployment work proceeds. Do not route to the
placeholder and claim production recovery.

## Authorized fresh production provisioning — 2026-09-10

The user chose a new production environment without importing old data and supplied
admin@gmb01.xyz for the first Platform Superadmin (principal key admin). A detached
release checkout under ~/Documents/ERP-Production/releases owns the deployment;
the original development index/worktree and unrelated Docker objects are preserved.
The new erp-system-production namespace contains a fresh pgdata volume. Migration
journal count is 112; 231 tables have both RLS and FORCE RLS; erp_api/erp_worker
are NOSUPERUSER, NOBYPASSRLS, NOCREATEDB and NOCREATEROLE. Bootstrap returned 201,
generated-password login returned 200 with Secure cookies, and repeat bootstrap is
retired by setup status. Credentials are only in mode-0600 deployment configuration.
No SMTP worker or outbound AI integration was enabled. API/web/database became
healthy; calendar worker runs without an outbound endpoint. Only web is published,
on loopback 127.0.0.1:18791. No public Tunnel cutover has occurred.

Actual nginx verification found release.json was mode 0600 and unreadable by its
worker (403); the web image now sets this public artifact to 0644. An actual /erp
request also exposed an absolute redirect to an internal HTTP origin; generated
mount redirects now use absolute_redirect off. The four existing subpath unit tests
pass; rebuilt-image HTTP/hash verification follows. The Tunnel candidate adds only
/erp -> http://127.0.0.1:18791 and passes cloudflared ingress validation. The guarded
activation script checks source/candidate hashes and initialized ERP health, backs
up the system config and rolls back on launchctl restart failure. System activation
still requires an operator's macOS administrator authentication (sudo -n unavailable).

Earlier production local acceptance — 2026-09-10: revision b9326f87732d904dc640d78d8b783418f5e9eb90
is running privately on 127.0.0.1:18791. Actual nginx /erp returns Location: /erp/;
the release verifier passes root, health, initialized setup, revision and all 126
asset hashes. The real browser displays the production Platform Superadmin login.
The original development index is preserved. This revision contains only the public
manifest permission and mount redirect fixes on top of 539f4f0. Four public-base-path
tests, lint, both typechecks and image builds pass. CI 34475283757 and Pages
34475283620 later completed successfully for this preceding revision. The current
coherent production revision is recorded below; public cutover still requires the
prepared system Tunnel activation and subsequent HTTPS health/revision verification.

## Initial backup restore and Pages verification — 2026-09-10

The fresh production archive is mode 0600 in the mode-0700 ERP-Production/backups
directory. An isolated PostgreSQL16 container restored the archive successfully;
112 migrations, 261 public tables, one Platform principal and zero Masters/Companies
match production. An internal whole-principal fingerprint also matches without
printing credential hashes. Both temporary restore containers were removed with
their anonymous volumes; production data was not reset. The first comparison query
used a nonexistent id column; the corrected row-content ordering comparison passed.
This is a baseline restore proof, not off-machine disaster recovery or load/SLO proof.
Pages run 34475283620 for b9326f8 succeeded; all 135 served assets were hash-verified
at 2026-09-10T12:15:21Z. Empty .nojekyll remains separately unserved. CI 34475283757
also completed successfully. Public production still returns 502 before Tunnel cutover.

Deployed unauthenticated boundary check — 2026-09-10: actual loopback production
HTTP requests to /erp/api/hr/employees, /erp/api/company-receipts and
/erp/api/platform/session return 401. A second empty-database Platform bootstrap
request returns 409, confirming the initialized instance cannot be claimed again.
No tenant/company records were created by these probes. Public cutover and live
Receipt-to-Pack outcome remain separate open gates.

## Current production checkpoint — 2026-09-10

The coherent local production API/Web revision is
`9ec8c0e5c1361dfe77c8a3e8cdca4730e0b56e05` in the detached release checkout.
Authenticated SG and MY employee sessions completed clean-evidence inspection,
manual metadata confirmation, Receipt creation, exact Pack preview, persisted Pack
readback and two-page PDF export. The verifier matched all 126 local production
asset hashes; database readback shows one Receipt and one Pack per Company. Details,
digests and PDF hashes are in the [TASK-234 production evidence](TASK-234-2026-09-09.md#production-receipt-to-pack-pilot--2026-09-10).

The current workspace full Vitest passed 204 files (900 tests), with 3 files/tests
skipped; schema drift, permission, i18n, Demo artifacts and production-RLS checks
also passed. Public `https://gmb01.xyz/erp/health` remains HTTP 502 because the
active system Tunnel configuration still lacks `/erp` and administrator
authentication has not completed. Production OCR is not configured, so the two
pilot extractions remain dead-lettered after clean ClamAV scans; governed manual
confirmation remains the permitted path. Human visual PDF review and a live
server-assistant provider run remain open.
