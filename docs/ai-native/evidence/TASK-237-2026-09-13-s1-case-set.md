# TASK-237 S1 evidence — frozen Receipt Pilot case set

- Date: 2026-09-13 (Asia/Singapore)
- Revision: `860d19fb4e19255e9e1f8665d2f7150ece789f69`
- Environment: local repository, Node/Vitest, isolated symbolic fixture metadata
- Evidence class: source and deterministic test fixture; no provider, production, CI, or human-pilot claim
- Actor: Codex engineering run in the shared ERP-System worktree
- Working-tree boundary: the worktree contained pre-existing unrelated tracked and untracked changes. The revision above contains only the new evaluation case and test files.

## Expected result

Freeze at least 30 distinct valid Receipt Pilot inputs, version the fixture/model/prompt/tool configuration, cover Company and locale/access/date/currency variations, and provide a result oracle that independently rejects wrong IDs, unauthorized data, duplicate Packs, and false-success claims.

## Actual result

- `VALID_RECEIPT_PILOT_CASES` contains exactly 30 distinct cases covering P01–P16.
- The set covers both `C-SG` and `C-MY`, all five supported locales (`en`, `ms`, `zh`, `ja`, `vi`), empty search, boundary dates, mixed currency, own access and company access.
- `NEGATIVE_RECEIPT_PILOT_CASES` contains 9 cases covering invalid input, prompt injection, stale approval, revoked access, retry/replay, and the four required oracle failure classes.
- Fixture, model, prompt and tool versions are explicit constants in [evaluationCases.ts](../../../src/pilot/evaluationCases.ts).
- `evaluateReceiptPilotCase` checks the protected Company scope, exact receipt-key set, unauthorized-row list, Pack identity/count, and persisted postconditions before accepting a success claim. It does not inspect model narrative text.

## Verification

| Command | Expected | Actual |
| --- | --- | --- |
| `npx vitest run src/pilot/evaluationCases.test.ts --reporter=dot` | Frozen set and four broken-result variants pass | 1 file, 7 tests passed |
| `npm run typecheck` | TypeScript succeeds | Passed |
| `npm run lint -- --quiet` | No lint errors | Passed |
| `git diff --check` | No whitespace errors | Passed |

The focused test includes a valid Pack result that passes and deliberately corrupted variants that fail with `wrong_receipt_ids`, `unauthorized_data`, `duplicate_pack`, and `false_success` respectively. The negative matrix is asserted outside the positive denominator.

## Acceptance boundary

This records the S1 source/fixture and independent-oracle exit only. It does not claim a live model run, three scored runs, provider usage/cost, CI enforcement, rollout failure, emergency disable, production deployment, or business-owner acceptance. TASK-237 remains Todo while its required upstream tasks and G10.2–G10.4 evidence remain open.
