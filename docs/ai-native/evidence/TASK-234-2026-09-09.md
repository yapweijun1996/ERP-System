# TASK-234 Evidence — Server AI runtime and contextual ERP workspace

## Verified release gate — 2026-09-10

GitHub Actions run [34475283757](https://github.com/yapweijun1996/ERP-System/actions/runs/34475283757)
completed successfully for revision `b9326f87732d904dc640d78d8b783418f5e9eb90`.
All four Vitest shards and the aggregate job passed, including lint, documentation,
generated artifacts, schema drift, permission contracts, root/web types,
PostgreSQL 16 non-superuser security lifecycle, cross-engine transaction/concurrency,
Demo build, five-language desktop/mobile matrix, browser smoke, all-screen audit,
transaction-list/workspace layouts and the production public-subpath browser contract.
The Chromium installation step was skipped because the browser cache was available;
OS dependencies and browser checks ran successfully.

This supersedes earlier pending-CI observations for this revision. It does not
prove public Tunnel cutover, production OCR readiness or a real-provider assistant
run. The local production SG/MY Receipt-to-Pack records, persisted Pack/PDF
verification and artifact readback are documented below; business-owner visual
review, public HTTPS and the live server assistant remain open. TASK-234 remains
in progress.

## Identity and scope

- Task / goal: TASK-234 / G07.
- Current checkpoint: S5 complete for local/repository scope; real-provider evidence remains an explicit gate.
- Date and source revision: 2026-09-09; current working tree after TASK-234/S5.
- Environment: local TypeScript/Vitest runtime, loopback HTTP API over isolated PGlite, built Demo/API browser fixtures and Playwright Chromium 149; no provider account, network model call, deployed host or production claim.
- Scope: define the server-owned provider boundary, explicit contextual-run states and bounded budget policy, add Company-owned secret-safe provider configuration, implement the governed Receipt conversation loop, and verify its contextual vanilla-JS workspace. The deterministic provider remains a local zero-spend test double; it is not real-model evidence.
- Existing user changes preserved: no reset, checkout or broad overwrite was used.

## S1 — Define runtime states and limits

`src/modules/agent/aiRuntime.ts` defines runtime contract version 1:

- Provider requests carry an explicit provider/model, run ID, typed messages and tool-call definitions. Raw provider payloads are not part of the application result contract.
- Run states are `draft`, `waiting`, `running`, `succeeded`, `failed` and `cancelled`. A successful result is returned only after the provider returns a validated response; errors never contain a simulated success response.
- `AiRuntimeLimits` makes physical provider calls, retries, whole-run duration, input characters, output characters and integer micro-USD cost explicit. Retries count against `maxProviderCalls`.
- Cost policy is `reserve_before_call_release_on_preflight_failure`; the runtime checks the per-call reservation before dispatch and enforces the returned cost before success.
- A linked cancellation signal and one whole-run deadline abort the provider call. Explicit cancellation produces `cancelled`; a deadline produces `run_timeout`.
- `createDeterministicZeroSpendProvider()` is the approved local provider/model pair (`deterministic.zero_spend` / `erp-test-zero-spend-v1`). It never uses the network and returns zero cost. `available: false` is a hard `provider_unavailable` error with `configure_provider` recovery, not a placeholder success.
- Budget, provider, output and cancellation errors use stable local codes and actionable recovery actions (`configure_provider`, `reduce_request_or_increase_budget`, `reduce_output_limit`, `retry_run` or `resume_run`). Provider error bodies are not forwarded.

Focused tests in `src/modules/agent/aiRuntime.test.ts` prove:

- deterministic zero-spend success and the complete `draft → waiting → running → succeeded` transition sequence;
- unavailable provider failure with no `response` and no `succeeded` state;
- cost reservation rejection before a provider call;
- retry attempts counted against the physical call budget;
- oversized output rejection without returning the oversized text; and
- distinct explicit cancellation and whole-run timeout outcomes.

| Command | Result |
| --- | --- |
| `npm test -- --run src/modules/agent/aiRuntime.test.ts` | Exit 0; 1 file / 6 tests passed |
| `npm run typecheck` | Exit 0 |
| `npm run lint -- --no-warn-ignored` | Exit 0 |

S1 does not claim encrypted Company configuration, real provider calls, governed
receipt tool loops, Pack execution, contextual UI, locale/theme/mobile accessibility,
PostgreSQL or production readiness. Those remain S2-S5 scope.

## S2 — Implement secret-safe server configuration

`src/data/schema/agent.ts` adds the Company-owned `agent_provider_config` aggregate.
It stores provider/model, explicit data region and data policy, bounded runtime limits,
enabled/version metadata and an AES-256-GCM `credentialEnvelope`. The row is scoped by
the composite `masterFn` + `companyFn` foreign key, has one configuration per Company,
and requires the updater to be a Company member. Migration `drizzle/0110_parched_random.sql`
and the generated Demo schema include the table and constraints.

`src/modules/agent/providerConfiguration.ts` is the server-side boundary. It allowlists
the deterministic, OpenAI, Google and OpenAI-compatible provider IDs; allowlists the
implemented OpenAI/Google models; requires encrypted credentials for real providers;
supports explicit replacement or clearing; and returns only safe metadata with
`credentialConfigured`, never the envelope or tenant scope. Provider changes with an
existing credential require a new credential or an explicit clear decision. Compatible
endpoints are HTTPS-only, reject embedded credentials/query/hash/IP/localhost/local
hosts, and require an exact server egress allowlist. Runtime calls/retries, timeout,
input/output and micro-USD cost limits are bounded before persistence.

`GET /api/integration/agent-provider` derives the active Company from the authenticated
session and exposes the safe view only. The update action uses the shared command and
idempotency boundary, requires `integration.manage`, encrypts the raw secret at the
API edge, and sends only safe before/after metadata to the append-only audit log. Raw
secrets are not persisted in idempotency payloads, browser responses, public views or
audit records. Production RLS includes the new Company table and explicitly permits the
authenticated Agent issuer boundary to read its tenant row when the future execution
path needs it.

Focused tests prove:

- Company isolation and deterministic zero-spend configuration without a credential;
- AES-GCM storage, credential rotation, decryption of only the current envelope, and
  no old/new secret leakage in public views or audit rows;
- explicit provider-change credential decisions, malformed-envelope rejection and
  deterministic-provider credential rejection;
- allowlisted models, HTTPS/exact-host egress, bounded retries/calls and compatible
  endpoint configuration;
- API permission separation, session-derived tenant scope, tenant-override rejection,
  secret-free response/audit output, idempotent replay and unapproved endpoint failure.

| Command | Result |
| --- | --- |
| `npm test -- --run src/modules/agent/providerConfiguration.test.ts src/api/agentProvider.integration.test.ts` | Exit 0; 2 files / 7 tests passed |
| `npm run typecheck` | Exit 0 |
| `npm run lint -- --no-warn-ignored` | Exit 0 |
| `npm run check:demo-schema` | Exit 0; 111 ordered migrations / schema version 110 |
| `npm run check:drift` | Exit 0; 261 tables match generated schema |
| `npm run check:permissions` | Exit 0; 317 codes / 116 resources / 62 actions / 5 updates |
| `npm run check:production-rls` | Exit 0; 231 policy tables / 10 exemptions / 261 schema tables |

S2 does not claim a real provider account, live network model call, governed receipt
conversation, Pack execution, contextual UI, locale/theme/mobile accessibility,
PostgreSQL deployment or production readiness. Those remain S3-S5 scope.

## S3 — Implement the Receipt conversation loop

`src/api/receiptAssistant.ts` now provides the server-owned, bounded conversation
orchestrator. `src/api/routes/assistant.ts` exposes the human-session adapter at
`/api/assistant/receipts` plus separate approve, reject, cancel and execute actions.
The route is mounted after the tenant-module gate and retains browser CSRF protection.
The browser supplies only a bounded message or the exact reviewed confirmation
payload; Company, actor and assistant Agent identity are resolved server-side.

The loop advertises only the six versioned Receipt pilot actions from
`src/modules/agent/actionContracts.ts`. Every model tool call is parsed against the
same contract and dispatched through the existing human or authenticated-Agent
dispatcher, so a tool name cannot grant permission, widen Company scope or bypass
field/grant checks. Tool context sent back to the provider is bounded and Receipt or
document content remains explicitly untrusted data. Search/detail/prepare/get/export
return citations built from record/version/source or artifact hashes.

`receipt_pack.prepare` remains non-authorizing. When a server-selected assistant
Agent principal is configured, the loop binds the exact preview to a persisted G06
execution intent with a generated business Pack key and separately supplied
hash-only intent key. The approval endpoints call the existing human-only G06
decision functions. The execute endpoint calls the existing authenticated Agent
executor, then reads the persisted Pack and exports the PDF within the same verified
assistant result. It returns `authoritativeCompletion: true` only after the persisted
Pack identity/source hash and artifact hash are observed. A missing provider,
missing assistant identity, denied/invalid tool call, cancellation, stale facts,
unapproved intent or provider prose without a verified Pack fails or waits without a
Pack write; it cannot announce success from model text.

Focused loopback/PGlite evidence in `src/api/receiptAssistant.integration.test.ts`
proves search, bounded preparation/citations, exact intent approval, persisted Pack
and artifact verification, replay without a duplicate Pack, denied detail, tenant
authority rejection, cancellation without a Pack and unavailable-provider failure.
Existing G06 action/intent tests remain the shared regression proof for stale facts,
grant revocation, changed payload, concurrent execution and approval replay; this
adapter reaches those same functions rather than duplicating business logic.

| Command | Result |
| --- | --- |
| `npm test -- --run src/api/receiptAssistant.integration.test.ts` | Exit 0; 1 file / 6 tests passed |
| `npm test -- --run src/api/receiptAssistant.integration.test.ts src/api/agentActions.test.ts src/api/agentExecution.integration.test.ts src/api/agentProvider.integration.test.ts src/api/agentBoundary.integration.test.ts src/modules/agent/aiRuntime.test.ts src/modules/agent/actionContracts.test.ts` | Exit 0; 7 files / 34 tests passed |
| `npm test -- --run src/api/app.test.ts src/api/moduleEntitlement.test.ts src/api/http.test.ts src/api/companyReceipts.integration.test.ts src/modules/expenses/companyReceiptPack.test.ts` | Exit 0; 5 files / 42 tests passed |
| `npm run typecheck` | Exit 0 |
| `npm run lint -- --no-warn-ignored` | Exit 0 |
| `git diff --check` | Exit 0 |

S3 is local provider-fixture and PGlite/loopback evidence. It does not claim a real
provider account/network call, durable browser conversation state, contextual UI,
five-locale/theme/mobile accessibility acceptance, PostgreSQL deployment or
production readiness. Those remain S4-S5/release evidence.

## S4 — Build the contextual workspace

The current Company Receipts vanilla-JS screen now exposes a bounded assistant
workspace through the existing Demo/API adapter boundary. It shows the selected
Company, a preserved draft and date range, deterministic-fixture status, cited
source rows, exact currency-separated preview totals, confirmation/reason, visible
progress, cancellation and recovery/retry. The screen checks the active Company
scope before and after each assistant action; a changed scope fails closed and does
not reuse the draft or execute a Pack. Once execution starts, cancellation is not
offered because an ambiguous in-flight write is unsafe; the governed command's
idempotency/replay boundary remains authoritative.

The UI work is in `web/public/assets/screens-company-receipts.js` and
`web/public/assets/company-receipts.css`. `web/public/assets/erp-system-data-adapter.js`
implements a deterministic Demo fixture plus the shared Pack write/readback and
artifact hash verification; `web/public/assets/erp-system-api-adapter.js` forwards
AbortSignal cancellation and exposes the server assistant/decision/execute calls.
Forty `receiptAssistant.*` resources were added to each of `en`, `ms`, `zh`, `ja`
and `vi`; the generated English bootstrap is synchronized and all five key sets are
identical.

The focused browser fixture covers draft preservation, all assistant states, visible
sources and exact preview, approve/cancel/retry/recovery, scope-change denial, five
locales, light/dark themes, keyboard focus restoration, 1280px desktop and 375px
mobile bounds/touch targets. It records no unexpected page errors or hidden execution
requests. The separate Demo Company Receipts E2E invokes the actual adapter after the
review, approves the exact intent, persists one Pack through the shared command, reads
the Pack back and independently hashes the exported PDF bytes; the reported artifact
hash and persisted result match.

| Command | Result |
| --- | --- |
| `npm run test:e2e:receipt-assistant` | Exit 0; desktop and mobile cases passed, including five locales, both themes, cancellation/recovery, scope guard, focus and overflow checks |
| `npm run test:e2e:company-receipts-api` | Exit 0; authenticated PGlite API E2E passed confirmation, refresh/search/range, Preview/PDF/Print and responsive bounds |
| `npm run build:demo` | Exit 0; known Vite classic-script, missing runtime CSS, PGlite externalization/eval and large-chunk warnings only |
| `node tests/e2e/company-receipts.spec.mjs` after `npm run build:demo` | Exit 0; actual Demo clean-evidence confirmation, persisted Pack readback and independent PDF artifact/source hash assertions passed |
| `npm run check:i18n-bootstrap` / `npm run check:i18n-business` | Exit 0; the initial S5 run had 1,769 English bootstrap keys and 141 exact business values; the post-S5 TASK-232 locale follow-up regenerated the current 1,773-key bootstrap and retained 141 exact business values |

The first direct Demo E2E attempt immediately after the API E2E used the API build
output and timed out waiting for the Demo marker. Rebuilding Demo and rerunning the
same scenario passed; this exposed a build-output ordering constraint in the E2E
harness, not a product failure.

Playwright Chromium 149 was the local browser used for the matrix. The Codex
in-app browser could not be used because `cua.getState()` reported that the Mac was
locked and automatic unlock failed. Native WebMCP/provider execution is therefore
not claimed.

## S4 finding and post-S5 resolution

- **P2, linked TASK-232 (historical S4 observation):** `npm run audit:i18n --
  --browser` completed the browser matrix for 130 routes × 5 locales × 2 viewports;
  the Company Receipts route passed in all ten locale/viewport combinations. The
  static audit nevertheless reported 112 Agent Governance resource findings (28
  missing keys in each of `ms`, `zh`, `ja` and `vi`, plus related unmapped/hardcoded
  route labels). These were outside the Receipt Assistant resource set and were not
  silently treated as green at the time.
- **Resolved 2026-09-09:** The TASK-232 post-S5 follow-up repaired the four local
  Agent Governance packs, Admin route labels and purchase-wizard labels. The static
  audit now passes at 1,773 canonical keys / 74 local packs, and the full built-Demo
  matrix passes 130 routes × five languages × desktop/mobile with zero resource,
  hardcoded, render-error or overflow findings. See [TASK-232 locale remediation](TASK-232-2026-09-09.md#post-s5-agent-governance-locale-remediation).
  This closes the local i18n follow-up; production, remote-CI and physical-device
  evidence remain separate.

## S5 — Validate fixture and real-model journeys

S5 local validation is complete. The focused provider/assistant regression passed
7 files / 34 tests; the full local Vitest run passed **198 files with 3 skipped,
813 tests with 3 skipped** (816 total, exit 0, duration 1803.13 seconds). Root and
Web typechecks, lint, Demo/API builds, generated schema/pack/i18n checks, schema
drift, permission registry, production-RLS static coverage, focused browser E2E and
`git diff --check` all passed. The post-S5 TASK-232 follow-up also passed the full
static and built-Demo i18n audits at 1,773 canonical keys / 74 local packs and zero
blocking findings; this is local repository/Demo evidence, not a provider,
production or physical-device claim.

No provider account, approved spend or authorized network model endpoint is available
in this environment. Per the packet stop/defer rule, no credential or model call was
invented and no latency/call/cost result is presented as live evidence. The explicit
remaining gate is one approved redacted real-provider run against the same bounded
workspace, followed by any required production/release evidence. TASK-234 remains
`in_progress`; the local S1-S5 checkpoints and G07.1-G07.4 are recorded as verified
for repository/Demo scope, not as a real-provider or production claim.

## Current progress and handoff

- Recomputed registry totals after TASK-234/S5 local completion and the TASK-232
  follow-up: **225 Done / 7 In Progress / 5 Todo / 3 Blocked / 240 total; 15 pending**.
- AI workstreams /12; goal criteria /48; checkpoints /60: **5/12; 24/48; 36/60**.
  G07.1-G07.4 are accepted for local/repository evidence; the real-provider gate
  remains open.
- This-session increment: **0 workstreams / 0 goal criteria / 0 new checkpoints**;
  TASK-234 stays in progress because the required real-provider evidence is
  unavailable.
- No Todo task currently has all dependencies satisfied. TASK-238 remains in
  progress awaiting product/finance/tax-owner decisions; TASK-231, TASK-236,
  TASK-237 and TASK-239 remain gated by unfinished upstream evidence. Preserve the
  explicit provider, production and release gates when continuing.


## Goal refocus and HTTP cancellation bridge

The goal review restored Phase 2 (the real Receipt-to-Pack pilot) as the primary
milestone in `GOAL.md` and `docs/GOAL_EXECUTION_PLAN.md`. Source inspection found
that `src/server.ts` does not inject a Receipt assistant provider factory, and the
Company provider configuration is not bound to a real runtime adapter. These are
local source gaps as well as a later authorized-account gate. No existing local
checkbox or workstream was promoted to real-provider completion.

A focused runtime-entry fix now connects HTTP disconnects to cancellation:

- `src/api/routes/assistant.ts` owns one request-lifetime AbortController. An aborted
  request or prematurely closed response cancels provider setup and the runtime.
  Check cancellation before entering the provider, before tools/runtime, and before
  writing a result. Remove listeners in `finally`; normal response completion is
  not cancellation. Do not send a completion/error response to a disconnected client.
- `ReceiptAssistantProviderContext.signal` passes the same lifetime to asynchronous
  provider configuration/setup. Actual provider implementations must honor their
  supplied signal; this does not claim that an uncooperative remote provider can
  be forcibly stopped or that a committed transaction can be rolled back.
- Two real HTTP integration cases reproduce failure before the fix (both pending
  setup and pending model call ignored disconnect), then pass after it. No Pack
  row is created. Existing approval, replay, scoped identity and unavailable-provider
  cases remain covered.

Verification: `npx vitest run src/api/receiptAssistant.integration.test.ts
src/modules/agent/aiRuntime.test.ts src/modules/agent/providerConfiguration.test.ts`
passes **3 files / 19 tests**, exit 0 (37.98 seconds). Lint, root/Web typechecks,
PGlite Demo proof, Demo build, Markdown links and diff checks pass. The embedded
GOAL counter/dependency/mapping validator passes: 226 done / 14 pending; AI 6/12;
criteria 28/48; checkpoints 36/60; no dependency-ready Todo. Documentation and this
bounded repair do not close TASK-234 or change those counters.

No real-provider call, production deployment, PostgreSQL target check, remote CI
or full repository test suite was performed in this increment. Next implementation
is the selected real-provider adapter, Company configuration resolver and explicit
server bootstrap wiring, followed by its separately authorized live run.

The existing `node tests/e2e/receipt-assistant-workspace.spec.mjs` also passes both
desktop and mobile: state transitions, cancel/recovery, Company guard, five locales,
both themes and focus/overflow checks, with zero unexpected browser errors. This
is deterministic UI fixture evidence; the disconnect propagation proof is the
separate real-HTTP integration above.


## Runtime integration prerequisite — dispatched cost custody

Date: 2026-09-09. Scope: local zero-network provider fixtures, PGlite and UI
fixture regression. TASK-234 remains In Progress; no live-provider acceptance.
The preceding cancellation checkpoint was progress (new executable HTTP evidence),
not a waiting process. This continuation made a separate source-backed repair.

### Reproduction and implementation

Before the fix, all four initial new regressions failed: a budget allowing one
10-micro-USD reservation dispatched three failed calls; a failed call followed by
a valid 3-micro-USD response lost the original 10 reservation; oversized output
lost its reported cost; and the assistant dispatched a third call when prior
unknown plus known costs left only 7 of the required 10.

`src/modules/agent/aiRuntime.ts` now retains reservations after dispatch errors,
reconciles valid reported costs for the corresponding call before output-limit
rejection, and rejects reports above the promised per-call maximum. Retry counts
increase only when another physical attempt begins, after its budget check.
`src/api/receiptAssistant.ts` carries charged exposure and consumed retries across
tool turns and exposes `reservedCostMicros` separately from reported spend.
No database schema, Pack command, approval, tenant, credential or provider
activation behavior changed. Existing HTTP disconnect cases remain passing.

### Verified exit

- `npx vitest run src/modules/agent/aiRuntime.test.ts src/api/receiptAssistant.integration.test.ts src/modules/agent/providerConfiguration.test.ts`:
  **3 files / 25 tests passed**, exit 0, 39.21s. Six added cases cover unknown-cost
  retry exhaustion, valid-cost reconciliation, oversized-output cost retention,
  underestimated reservation rejection, cross-turn cost custody and cross-turn
  retry allowance. Assistant negative cases assert no persisted Pack.
- `npm run lint`: exit 0, no errors/warnings.
- `npm run typecheck` and `npm run typecheck:web`: exit 0.
- `env -u POSTGRES_URL npm run demo`: exit 0, isolated PGlite transaction,
  rollback and balanced-ledger proofs. PostgreSQL was deliberately not selected.
- `npm run build:demo`: exit 0; existing classic-script, PGlite eval and chunk-size
  warnings remain. This is not a warning-free build claim.
- `node tests/e2e/receipt-assistant-workspace.spec.mjs`: exit 0; desktop 1280x900
  and 375x812, five locales, light/dark, confirmation, cancel/recovery, Company
  guard, focus/touch/overflow and unexpected browser-error checks pass. This
  deterministic UI fixture is not a real provider journey.

Full Vitest, PostgreSQL, deployed-host/remote CI and real-provider account/cost
verification were not run for this bounded runtime repair. No schema or generated
artifact change required regeneration. Common docs/diff checks are recorded below.
The runtime still relies on a truthful adapter maximum; these synthetic amounts
are not live prices. Unknown cost is held conservatively, so a failed call may
exhaust a budget even if a later invoice establishes no charge.

Next: preserve this budget contract while implementing real protocol tool/message
mapping, authenticated Company configuration resolution and default-deny server
bootstrap. The current message transcript drops assistant tool-call metadata and
result call IDs; inspect and repair that mapping before claiming adapter coverage.
Only an authorized account/model/data-policy/spend run can close live acceptance.
Counts remain 226 Done / 6 In Progress / 5 Todo / 3 Blocked, 240 total;
AI workstreams 6/12, goal criteria 28/48, checkpoints 36/60.
Reusable lesson remains project-local: transport failure is not proof of no
billing; keep confirmed spend separate from conservative unknown exposure.

Final documentation checks: `npm run docs:check` passes 72 Markdown files / 735 local links; `git diff --check` passes. Project KB budget contract is synchronized.


## Concrete Responses adapter — local checkpoint

2026-09-09: `openAiProvider.ts` implements pinned GPT-4.1 mini Responses text and
custom-function HTTP serialization/parsing. `AiMessage` now retains tool calls and
call-result IDs; assistant history carries both. Native fetch uses fixed HTTPS,
redirect:error, bounded body reads, cancellation, store:false and no built-in
execution. Pricing is explicit deployment policy; reservation uses full model
context plus capped output. The adapter does not enable network access by itself.

`npx vitest run src/api/receiptAssistant.integration.test.ts
src/modules/agent/openAiProvider.test.ts src/modules/agent/aiRuntime.test.ts` passes
3 files / 39 tests, exit 0 (33.85s). This includes the existing governed pilot
repeated through injected Responses HTTP output: exact human confirmation,
persisted Pack read-back and PDF hash verification. Unit cases cover protocol
identity, malformed/unknown/oversized output, wrong model/usage, redirects,
stalled-body cancellation, timeout, sanitized errors and preflight rejection.
Lint/typecheck pass. Company resolver/bootstrap and common final gates follow.
No provider invoice, live request, production or real-model evidence is claimed.


## Company resolver and bootstrap — local integration verified

Date: 2026-09-09. Previous turn classification: progress (budget implementation
and passing regressions). Current turn: adapter, resolver and bootstrap source
implementation plus new local integration evidence. No provider spend occurred.

### Ownership and observable results

- `openAiProvider.ts` maps pinned OpenAI Responses text/functions, preserves tool
  call/result IDs, enforces fixed HTTPS/no redirects, bounded streamed body and
  usage/output, sanitizes failure bodies and rejects plain/escaped credential echo.
  `aiRuntime.ts` now counts transcript tool metadata in input limits.
- `receiptAssistantProvider.ts` reads only the session Company configuration inside
  tenant transactions, decrypts its credential, applies Company runtime limits and
  checks the selected Agent/owner/grants. Only granted tools are advertised. The
  complete configuration/grant snapshot is revalidated before every egress;
  changes require a fresh run. No DB transaction spans a provider request.
- `receiptAssistantBootstrap.ts`, `server.ts`, `.env.example` and Compose connect
  explicit activation, Agent identity and reviewed pricing/output limits. Default
  is disabled; a credential alone cannot activate calls. Unsupported provider,
  model, region, data policy, missing configuration or inadequate budget is denied.
- The HTTP/PGlite pilot passes in three modes: deterministic provider, injected
  Responses HTTP adapter, and persisted encrypted Company configuration + resolver
  + Responses adapter. Each requires the exact human confirmation and independently
  reads back persisted Pack/PDF hashes. These are fixtures, not real-model output.

### Verification

- Main focused command: `npx vitest run src/api/receiptAssistant.integration.test.ts
  src/modules/agent/openAiProvider.test.ts src/modules/agent/aiRuntime.test.ts
  src/receiptAssistantBootstrap.test.ts src/modules/agent/providerConfiguration.test.ts`:
  **5 files / 60 tests passed**, exit 0, 60.09s. The final escaped-credential hardening
  subsequently passes `npx vitest run src/modules/agent/openAiProvider.test.ts`:
  **19 tests**, exit 0. Do not add these overlapping counts.
- Company negative cases verify explicit activation, missing configuration, valid
  other-Company Agent/module without configuration, disabled config, tenant-only,
  tenant-local, unsupported model, zero budget, live credential rotation and grant
  revocation. No denied case creates a Pack; revoked/rotated runs send no second
  provider request. Company maxProviderCalls=1 overrides the route's 8-call fixture
  limit. This existing bounded-read path returns 200 with one factual tool result
  and authoritativeCompletion=false; an initial test expectation of 422 was corrected
  after inspecting the established terminal behavior, without changing product code.
- Additional Agent/G06/configuration regression initially passed 3 files / 8 tests
  but failed all 8 tests in `agentExecution.integration.test.ts`: its hardcoded noon
  preparation timestamp expired against the real HTTP execution clock. The fixture
  now captures time in each beforeEach. Production expiry logic is unchanged.
  `npx vitest run src/api/agentExecution.integration.test.ts
  src/modules/agent/aiRuntime.test.ts` then passes **2 files / 19 tests**, exit 0;
  all 8 replay/concurrent-edit/insert/cancel/grant-expiry cases pass.
- Lint (zero errors/warnings), root/Web typechecks, isolated
  `env -u POSTGRES_URL npm run demo`, `npm run build:demo`, and
  `node tests/e2e/receipt-assistant-workspace.spec.mjs` pass. Browser fixture covers
  desktop/375px, five locales, light/dark, focus, cancellation, Company guard and
  no unexpected browser errors. Build retains existing classic-script/PGlite eval/
  chunk-size warnings. `docker compose config --quiet` passes; no container started.
- `npm run docs:check` passes 72 files / 736 local links; `git diff --check` passes.
  Source/targeted changes were reviewed, including default activation and Compose
  environment parity. No schema/generated SQL change or migration was needed.

### Unmet gates and next action

No full Vitest rerun, PostgreSQL, deployed bootstrap, remote CI, production or
real-provider/account run was performed. Final adapter hardening reran its focused
unit suite, lint and root typecheck; existing browser/build evidence is unaffected
by that server-only validation addition. Real provider pricing/retention/account
approval and latency/call/cost evidence remain open; stored usage-based cost is a
conservative configured-rate estimate, not an invoice. The full-context reservation
is intentionally conservative and can exhaust a small budget after one failed call.

The former missing adapter/resolver/bootstrap source gap is now closed locally.
Next prepare the opt-in live pilot execution/evidence procedure using approved
synthetic SG/MY receipts, then obtain account/model/data-policy/spend authority
before any live request. Keep production and business-owner gates separate. TASK-234
remains In Progress; 226 Done / 6 In Progress / 5 Todo / 3 Blocked of 240; AI
workstreams 6/12, criteria 28/48, checkpoints 36/60 are unchanged.

Project KB current-focus item b2806d34-f343-48dc-af7e-d8a3629703f3 is synchronized with the local connection and remaining live gate. No global Skill change was needed; provider and fixture-time lessons remain source-backed project evidence.

## Reproducible pilot runner — local fixture evidence

Implemented `src/pilot/receiptPilot.ts`, its test and the thin
`scripts/receipt-assistant-pilot.ts` CLI. The runner seeds a fresh private disk-backed
PGlite database for SG or MY, creates two distinct synthetic receipt PDFs, and uses
the configured Company provider resolver through ordinary authenticated assistant,
approval/cancel and execute APIs. It closes and reopens the database before domain
rendering and checks persisted receipt IDs, source digest, PDF digest and the saved
file's digest. Atomic output files are private; temporary writes are cleaned up.
No schema, domain command, production data or existing database was changed.

Default/inherited-env-safe fixture mode injects Responses and explicitly labels
confirmation and usage as simulated. Live mode needs an explicit CLI flag, approved
account/data-policy/spend reference and reviewed rate/budget configuration, with a
maximum USD 1 per-run ceiling. It requires a TTY and the complete preview selection
digest before ordinary approval. This gate was tested without any live request.
The repeatable command and environment contract are in
[AI_PROVIDERS.md](../../AI_PROVIDERS.md#reproducible-receipt-pilot-runner).

### Observed verification

- `npx vitest run src/pilot/receiptPilot.test.ts`: **8 passed**, including both
  Companies, inherited-key isolation, missing authority/non-interactive rejection,
  excessive budget rejection, exact-confirmation cancellation with zero persisted
  Packs/no PDF, private evidence and successful database reopen/hash verification.
- Independent CLI fixture runs completed for both SG and MY: 2 receipt rows each,
  3 injected calls, 0 retries, simulated estimate 168 microUSD. These are synthetic
  measurements, not live latency/cost evidence; actual external model calls: zero.
- SG PDF SHA-256: `e9e5459849db68c6ceeac2459ba1cc20e87053cacf59d5637b83f1626f0373b0`.
  MY PDF SHA-256: `3532f43a95a03daa6f784af6f91b823679218c6209133835496b4b68af51a386`.
  Each saved PDF contains 3 pages and about 14.1 MB (existing renderer/font behavior).
  SG's three pages were rendered and visually inspected: both source receipts and
  SGD 36.5000 total are readable. Poppler reports embedded-font type mismatch warnings;
  the summary retains the existing long-purpose truncation. This is not a clean
  typography acceptance or physical-device PDF-viewer test. Opening SG in Codex
  returned `queued`; neither that nor rendering proves a human viewed the PDF.
- Initial identical-image fixture receipts were correctly rejected by the governed
  duplicate-evidence rule. The fixture now generates two distinct source PDFs.
  MY initially used an incorrect harness-only company-switch path; it now uses
  `/api/auth/session/actions/switch-company`. No domain policy was loosened.
- Lint: zero errors/warnings. Root and Web typechecks, isolated PGlite demo,
  build:demo and Receipt assistant desktop/375px browser regression all pass.
  Build retains existing classic-script/PGlite eval/chunk-size warnings.
- Documentation check passes 72 Markdown files / 740 local links; final diff check passes.
- No full Vitest, PostgreSQL, remote CI, deployed bootstrap, scanner/OCR worker,
  approved real-provider account, actual operator confirmation or production run
  was performed in this checkpoint. Earlier provider/route tests remain separate
  evidence; do not combine overlapping test counts.

### Continuation

The local runner is reviewable and exercised. Next obtain actual account/model,
data-policy and spend authorization, then run one interactive synthetic pilot and
record real provider usage/latency and operator artifact review. Keep business-owner
and production acceptance separate. TASK-234 stays In Progress; no task, workstream,
criterion or checkpoint counters change. Synthetic evidence cannot satisfy the
real-model acceptance gate. The project KB current-focus item is updated with this
checkpoint; no global Skill change is required.

## Inspection completeness — governed detail and source-file proof

The previous runner skipped directly from search to preparation. That was weaker
than the milestone's inspect-evidence requirement. The runner now calls `receipt.get`
for each fixture receipt and checks every preview row against successful governed
detail results: receipt version, document identity/version and SHA-256 must agree.
It then retrieves the exact document version through `/api/documents/:id/content`
using the existing authenticated permission/audit boundary, verifies its bytes,
and saves private `source-receipt-<id>.pdf` artifacts before invoking human review.
The interactive preview includes these paths. This changes pilot acceptance
orchestration, not the shared domain rules for Pack creation.

Verification on 2026-09-09:

- `npx vitest run src/pilot/receiptPilot.test.ts`: **13 passed**. SG/MY integration
  asserts both PDF source files exist, hash-match and have mode 0600 before the
  confirmation callback. Five negative detail cases cover absent/failed reads,
  receipt-version mismatch, document-version mismatch and document-hash mismatch.
  Existing cancellation still produces zero Packs/no result PDF. These negatives
  test the inspection guard; source-byte corruption/HTTP denial are not separately
  injected in this checkpoint.
- Independent SG CLI fixture completes with 5 injected calls, 0 retries and 280
  synthetic microUSD estimate. Saved Pack SHA-256:
  `03d247fe2e6d875426cfc21b5e4801d0c1b75739e9dfd1a82a33c78550fe4d98`.
  Persisted database reopen, both source hashes and final PDF hash pass.
- Lint (zero warnings/errors), root/Web typechecks, isolated PGlite demo and Demo
  build pass. Existing build warnings remain unchanged. Source review confirms
  provider resolver/bootstrap wiring and cancellation propagation are intact;
  those production files were not changed by this checkpoint.
- Desktop/375px Receipt assistant browser regression passes for five locales, both
  themes, cancellation/recovery and focus/overflow. Documentation check passes
  72 Markdown files / 740 local links; final diff check passes. Project KB current
  focus is synchronized with the inspection checkpoint and remaining live gate.

Source retrieval proves neither human viewing nor model image understanding.
`humanViewedSources` and `humanViewedPdf` remain false. No live-provider call,
external spend, full Vitest rerun, PostgreSQL or production validation occurred.
A concrete one-run SG authorization request has been presented; no answer is not
approval. The next real-model run still requires the approved account, pricing,
data policy and operator interaction. TASK-234 and the full goal remain active;
226 Done / 6 In Progress / 5 Todo / 3 Blocked of 240, AI 6/12, criteria 28/48 and
checkpoints 36/60 remain unchanged.

## Readable Pack artifact — font and layout repair

Current source: `companyReceiptPackPdf.ts` wraps complete register cell text by
measured font width and repeats headings across pages; it no longer slices away
merchant, receipt number, purpose or uploader text. `documents/pdfFont.ts` corrects
only renderer-owned OpenType font dictionaries after flush, before final save.
Imported source font streams are not rewritten. Both the register and unsupported
source identity pages use the same technical font handling.

Root-cause evidence: the installed fontkit exposes the `CFF ` table, while pdf-lib
checks `cff` and classifies the bundled OpenType/CFF font as TrueType (`FontFile2`,
`CIDFontType2`). Correct declarations are applied to the complete `OTTO` stream.
Localized alternate digits also lacked full-font Unicode mappings; disabling
`locl`/`liga` preserves ordinary extractable dates/amounts. Full-font embedding is
retained. A subset attempt produced invalid reader fonts and was rejected; an
initial stream-only repair missed the wrong descendant type. Actual reader output,
not passing library tests, determined the final repair. No dependency was upgraded.

Verified 2026-09-09:

- Final targeted suite: **3 files / 24 tests passed** (`companyReceiptPackPdf`,
  governed `companyReceiptPack`, and SG/MY `receiptPilot`). Covers full Unicode
  text/width preservation, multi-page long rows, deterministic bytes for frozen
  facts, register/placeholder font declarations, numeric mappings, source-page
  composition, governed replay/access, inspection and cancellation.
- Shared `taxEvidence.test.ts` regression: **2 tests passed**. Documentation check
  passes 72 Markdown files / 741 local links; final diff check passes.
- Independent SG pilot persisted/reopened its Pack and PDF. Final artifact SHA-256:
  `0750b3108e5eccddca605e704a4a4a81ddce7a86aeea1310e4fbf486399f5ffe`.
  Poppler rendered all 3 pages without warnings. Visual review and text extraction
  verify the full purpose, dates, SGD 36.5000 total and both 18.2500 rows.
- A separate 16-row, 4-page Chinese/Japanese/Vietnamese synthetic register was
  rendered and all pages visually inspected. Columns stay separated, headers
  repeat, all 16 END markers and multilingual labels/amounts extract correctly.
- Lint: zero errors/warnings. Root/Web typechecks, isolated PGlite demo, final Demo
  build, desktop/375px assistant browser and actual Demo Company Receipts browser
  regressions pass. Existing Vite classic-script/PGlite/chunk warnings remain.

The small Pack still embeds about 14 MB of font data; this change does not claim
PDF size optimization. Regenerating a Pack with the new renderer changes artifact
bytes/hashes, not the stored source selection or original evidence bytes. Existing
files are not rewritten. No full Vitest, PostgreSQL, production, physical-device
viewer or live-model run occurred. Human viewing/confirmation and the pending
single-run spend authorization remain unverified. TASK-234 and the broader goal
remain open; registry and goal counters are unchanged.

## Integrated regression and MCP fixture clock repair

The original `env -u POSTGRES_URL npm test -- --run` completed once, without restart,
on 2026-09-09. Exit **1** after **1283.31 seconds**: **200 passed / 2 failed / 3
skipped files (205 total)**; **880 passed / 2 failed / 3 skipped tests (885 total)**.
The three skipped files require PostgreSQL. The two failures were:

- `mcpAuthorizationBoundary.integration.test.ts`: the changed-approval case
  received `agent_execution_intent_expired` instead of `agent_execution_intent_not_approved`.
- `mcpInteroperability.integration.test.ts`: the dropped-response/replay case
  found no persisted Pack after the first execution attempt.

Both fixtures prepared/approved a short-lived intent at the fixed time
2026-09-09T12:00Z while the HTTP/MCP executor used actual time. Source inspection
and the first explicit expiry error identified the mismatch. Both fixtures now
capture preparation time in `beforeEach` and assert freshness before exercising
the intended HTTP behavior. Historical receipt dates and consistently injected
unit-test clocks remain unchanged. No production timeout, expiry, authorization,
approval, provider or PDF implementation was changed during this repair.

Post-repair verification:

- `npx vitest run src/api/mcpAuthorizationBoundary.integration.test.ts
  src/api/mcpInteroperability.integration.test.ts src/api/agentExecution.integration.test.ts
  src/modules/agent/agentExecutionIntent.test.ts`: **4 files / 16 tests passed**, exit 0.
  The installed TypeScript/Python client case actually ran and passed; it was not skipped.
  This closes both observed failures and rechecks cancellation, approval, replay and
  expiry behavior. Do not add overlapping targeted counts to the failed full run.
- Lint: zero errors/warnings; root/Web typechecks, isolated PGlite demo and
  build:demo pass. Existing Vite classic-script/PGlite/chunk-size warnings remain.
- No second full run was performed after these test-fixture-only edits. Browser
  checks from the preceding unchanged product source remain separate evidence;
  no browser, PostgreSQL, live-provider, production or physical-device rerun is claimed.

Repeated bug record:

- Bug pattern: historical fixture preparation clock mixed with real HTTP execution time.
- Where it appeared: earlier G06 execution fixture and these two MCP integration fixtures.
- Root cause: the fixture intent had expired before the test's intended boundary check.
- Failed previous approach: repairing the G06 fixture alone left equivalent MCP fixtures.
- Correct fix: per-test real preparation time; preserve production expiry rules.
- Regression check: freshness assertion plus actual authorization/cancel/replay clients.
- Verification evidence: failed full run and subsequent 16 passing targeted tests above.
- How to detect next time: inspect nearby HTTP intent/token/grant fixtures for fixed dates;
  distinguish business dates and deliberately injected expiry-test clocks.
- Skill decision: retain the ERP HTTP testing rule in `AI_NATIVE_EXECUTION.md`; existing
  autonomous-delivery/repeated-bug skills were checked, no new global skill is needed.
- Do not repeat: extending production TTL or weakening expiry to make stale test data pass.

The final milestone audit explicitly separates service-route CLI evidence, browser
fixtures and actual real-model/operator acceptance. Account/data/spend approval is
still unanswered; no live call or cost is authorized by this regression result.
TASK-234 remains In Progress. Counts remain 226 Done / 6 In Progress / 5 Todo / 3
Blocked of 240, AI 6/12, criteria 28/48 and checkpoints 36/60.

Documentation check passes 72 Markdown files / 744 local links; final diff check passes.
Project KB current focus is synchronized; the earlier budget entry now explicitly
marks its missing-provider-source statement as superseded by the later implementation.

## User-directed public Demo gateway default — 2026-09-10

User requested their GPT Demo endpoint as default and supplied its protocol guide.
Implemented `web/public/assets/receipt-demo-gateway.js`, loaded before the Demo
adapter: fixed public session/Responses URLs, `github-pages`, `demo-auto`, no
startup request, no private key, in-memory token, 60-second abort, 64-KiB response
bound, strict single search-field proposal and sanitized failures. The Demo
adapter uses the proposal before existing ERP Pack preparation. Explicit date and
search fields remain authoritative; confirmation/execution remain separate. The
five-locale notice now discloses outbound user-query processing. No receipt files
or Company records are included in the request. Monetary cost is unavailable.

The guide forbids native tools. This path is a model-assisted Demo search, not the
server six-tool pilot, production configuration or document Vision integration.
The public endpoint is the explicit exception to the Demo bundle URL rule; no
production ERP URL or credential is bundled. Existing Company API configuration
was not changed.

Verification:
- `npx vitest run src/receiptDemoGateway.test.ts`: 8 passed (session protocol,
  no startup request, malformed/extra/type-invalid/token-echo proposals, denied
  session, cancellation propagation and response size).
- `npm run lint`, root/Web typechecks, isolated `env -u POSTGRES_URL npm run demo`
  and `npm run build:demo` passed; existing build warnings remain.
- `node tests/e2e/receipt-assistant-workspace.spec.mjs`: desktop/375px,
  cancellation, scope guard, five locales, themes and focus/overflow passed.
- `node tests/e2e/company-receipts.spec.mjs`: injected gateway transport plus
  actual PGlite clean-evidence confirmation, assistant Pack persistence/PDF,
  filters, pagination and responsive facts passed. No live inference was made
  by these tests.
- Native Chrome opened the isolated local ERP page and reached Demo setup. Mac
  locked again during setup; actual gateway origin/model acceptance remains open.
  Chrome DevTools MCP was unavailable; native CUA and scripted browser tests are
  recorded distinctly, never called DevTools evidence.

TASK-234 remains in_progress. Recomputed counts: 226 done, 6 in_progress, 5 todo,
3 blocked (240). Full suite, live model, human source/PDF review, PostgreSQL and
production were not reverified in this checkpoint.

## Live Demo session preflight — 2026-09-10

The existing local preview at `http://127.0.0.1:4397/` returned HTTP 200.
Native Chrome UI interaction was unreliable; the in-app browser provided a
supported CDP capability. A real browser-origin request through the implemented
client failed before inference. A status-only follow-up confirmed
`POST https://gpt.yapweijun1996.com/demo/session` returns HTTP 403 with
`demo origin is not registered` for project `github-pages` and that local origin.
No session token was printed or saved, no model response was received, and no
receipt/Company data was submitted. The only intended model input was a synthetic
merchant query. No gateway configuration was changed.

This supersedes the prior unknown live-origin status: the tested origin is
rejected. Next required evidence is the ERP Demo project's correct ID and an
approved registered origin, followed by successful session/model execution.
Do not spoof Origin or treat generic CORS allowance as project authorization.
Chrome DevTools MCP remains unavailable; this evidence used CUA in-app CDP.
The existing preview service remains available for the next browser checkpoint.

## Production Receipt-to-Pack pilot — 2026-09-10

The fresh production PostgreSQL environment was exercised through the real local
HTTP API at revision `9ec8c0e5c1361dfe77c8a3e8cdca4730e0b56e05`, with no old data
imported. The new Master is `M-47F82ACB0A19`; the two Companies are
`C-SG-136B3C173074` and `C-MY-B2796BF9D340`. The first employee accounts were
created with generated credentials and are immediately `active`; the temporary
passwords were revealed only in memory for the authenticated pilot and were not
printed, persisted in this document, or sent to a provider.

The two uploaded managed-document versions were scanned by a one-shot ClamAV
worker (`clamav-task234-clamscan`) and both persisted as `clean`. The production
Compose stack currently has no long-running scanner/OCR worker and no local OCR
endpoint configured. The scan worker therefore created terminal `ocr_dead_letter`
records after the clean scan. This is fail-closed operational evidence, not a
claim of OCR success: manual metadata confirmation remains available while the
source is clean/current and no Receipt is already bound.

Each country was exercised with a real employee login and the governed API flow:
inspect the clean evidence and immutable source hash, confirm the metadata, create
the Company Receipt, prepare an exact date-bounded selection, compare the returned
row/currency/amount and selection digest, create the immutable Pack, read it back,
and export the PDF. The resulting persisted records are:

| Country | Evidence | Receipt | Pack | Selection/source digest | PDF SHA-256 | PDF bytes |
| --- | ---: | ---: | ---: | --- | --- | ---: |
| SG | document/version `1/1` | `1` (`ready`) | `1` (1 row, 1 document) | `bdb1832eaeef6519df9e126185c82b0488a1b242244c7a69d084358480544244` | `63a5daed1620bd6cabf94ab75151c608916fdb41b038f4c812e38ccd0bd4eee5` | 14,132,960 |
| MY | document/version `2/2` | `2` (`ready`) | `2` (1 row, 1 document) | `71cd72df53e9a805babb2dd63926e4789068f13433cbeaec6d4fd33e0d9f8fee` | `b41d5790d79bef776c17e5867baa58f08f1674b161d067c9401d3a69bd4061f6` | 14,132,963 |

The selected totals are `SGD 18.2500` and `MYR 21.8000`. Both PDFs returned
`application/pdf`, two pages, and the `x-receipt-pack-sha256` header matched the
independently computed body hash. Private copies were written to
`/Users/yapweijun/Documents/ERP-Production/evidence/task234-sg-receipt-pack.pdf`
and `/Users/yapweijun/Documents/ERP-Production/evidence/task234-my-receipt-pack.pdf`
mode 0600. An `open_in_codex` request for each SG and MY PDF was queued; this proves the
results were handed to the Codex panel but does not by itself prove a human visually
reviewed the rendered PDFs. Poppler rendered both two-page files without parse errors.
The register pages contain the expected Company Receipt Pack heading, date range,
country currency total, merchant/receipt number, purpose, uploader and amount. A
subsequent full-page audit found that each persisted source version is a 68-byte
`image/png` whose PNG header declares `1x1` pixels; the second PDF page therefore
contains only that source pixel at natural size. This is the synthetic pilot
fixture's source-material limitation, not a storage-integrity failure. The production
artifact is structurally valid and hash-bound, but its source page is not readable
receipt evidence and cannot receive human visual acceptance. Replace the synthetic
source with a readable receipt image/PDF before business sign-off; this agent-side
check is not a substitute for that review.

The final boundary check used separate authenticated SG and MY sessions. An SG
session received safe 404 responses for the MY Receipt and Pack, while replaying
the MY Pack key with the same reviewed selection digest returned HTTP 200 with
`replayed=true` and Pack `2`; the database still contains one Pack per Company.

This closes the local production Receipt-to-Pack persistence and artifact checks,
including the human-session confirmation path for MY and the previously completed
confirmation that produced the SG Receipt. It does not close the public gateway:
`https://gmb01.xyz/erp` still returns 502 because the system Cloudflare Tunnel
route has not been activated. It also does not claim a configured OCR worker, a
live provider-model call, native Chrome DevTools evidence, or human visual review
of the PDF. The guarded Tunnel activation script remains the next operator step;
no password or private key is required in chat.

## Registered Demo origin and live inference — 2026-09-10

Repository `docs/DEPLOYMENT.md` identifies the published Demo as
`https://yapweijun1996.github.io/ERP-System/`. A real in-app browser navigation
established that origin; no Origin header was fabricated and no gateway policy
was changed. From that page, `github-pages` session creation returned HTTP 201.
Thus the project ID is valid; only the previously tested localhost origin was
rejected.

One authorized real `/demo/v1/responses` request using `demo-auto` and the same
search-only prompt as the implemented client returned HTTP 200, status
`completed`, text `{"search":"Synthetic Cafe"}`. Reported usage was 72 input,
45 output and 117 total tokens, including 33 reasoning tokens. Monetary cost and
the underlying routed provider/model were not established; `demo-auto` is the
reported alias. Session tokens remained within the browser request closure and
were neither printed nor persisted. Only a synthetic merchant query was sent.

This proves the real gateway protocol and proposal response on the registered
origin. It does not prove the modified local client is deployed, the exact
Receipt-to-Pack browser journey, user source review, Pack creation, persisted PDF
opening or production readiness. No deployed files/settings or business records
were changed. Next checkpoint: exercise the current client and complete governed
Receipt-to-Pack journey in an authorized browser environment. Local origin remains
rejected until registered; do not repeat that failed request without a change.

## Actionable session rejection — 2026-09-10

The client now maps session HTTP 403 to `demo_gateway_session_denied` and a
bounded instruction to check the registered Demo project and website origin.
It never echoes the remote response body. The existing denied-session test
asserts this exact recovery contract and that only one request occurs, before
inference or ERP preparation. Eight gateway tests, lint, both typechecks,
isolated PGlite demo and Demo build passed (existing build warnings unchanged).

Actual in-app CDP verification first encountered the older generic error on
port 4397 with an active Service Worker. A fresh isolated origin on port 4398
loaded the current build and returned `demo_gateway_session_denied` with the
expected configuration instruction. Existing browser data/caches were not reset.
No model inference or Pack creation occurred in this negative check. Desktop/
375px layout regression was not rerun for this error-only change; earlier layout
proof remains separate. Full live Receipt-to-Pack acceptance remains open.

## Isolated GitHub Pages candidate — 2026-09-10

Built the current worktree using `GITHUB_PAGES=true`, repository
`yapweijun1996/ERP-System` and the existing `build:demo` command into private
`/var/folders/tx/dx_7t0r56svcjld4fx5j_pwr0000gn/T/erp-pages-candidate-3i4_xmq2`.
Added the workflow's `404.html`/`.nojekyll` outputs and generated the existing
release manifest with explicit revision `local-uncommitted-task234-20260910`.
This is not a Git commit or a published release.

All 136 manifest assets matched their byte counts and SHA-256 values. Manifest
SHA-256: `6be1355d67fc4e40a2d57e1d404c5c4f287c73d23dde7f22de4c5994c23aba6a`.
The `/ERP-System/` index loads the gateway before the Demo adapter and includes
the current session-denial recovery. An isolated in-app browser at local port
4399 reached the first-run language screen after database initialization with
zero captured console errors. No model request was made in this startup check.
The candidate preview was stopped after verification; the existing root preview
was preserved.

The working tree contains 148 modified/untracked paths spanning accumulated
work. No commit, push, workflow dispatch or deployment occurred. This artifact
is a concrete local review candidate, not CI/release approval or full live
Receipt-to-Pack evidence. Next release work must review the source scope and
preserve all unrelated work; actual operator confirmation/PDF viewing remains open.

## Demo reviewed-selection guard repair — 2026-09-10

Release source review found a real gap: `receiptAssistantExecute` accepted but
dropped `selectionDigest`, while `companyReceiptPack` duplicated the selection
query and amount calculation. The Demo adapter now requires a valid digest for
assistant execution, passes it through, reuses the shared selector inside the
creation transaction and compares the digest before insert. Idempotent replay
also checks the stored snapshot digest. Ordinary manual Pack callers retain their
optional-digest compatibility. No server/API/G06 command or schema was changed.

Extended `tests/e2e/company-receipts.spec.mjs` exercises the actual PGlite adapter:
missing digest and wrong digest reject; a real receipt metadata/version change
after preview rejects the old digest; each rejection leaves Pack count unchanged.
A new preview then creates a Pack, the same reviewed request replays the same ID,
and persisted PDF/source verification passes. Model transport is injected; this
is not human approval or live-model Pack evidence.

Verification: lint (zero warnings), root/Web typechecks, isolated PGlite demo,
Demo build and Company Receipts browser regression all passed. The browser suite
also covers desktop/mobile facts, permission/query filters, locale rendering and
PDF preview/download/print. Existing build warnings remain. Documentation check
passed 72 files/747 links; final diff check passed. Full Vitest, PostgreSQL,
production and the separate full assistant layout matrix were not rerun.

The earlier local Pages candidate predates this repair and must not be published
as the current version. TASK-234 remains in_progress for same-run live selection,
actual operator review/confirmation and persisted result/PDF opening.

## Persisted approval boundary audit — 2026-09-10

Inspected Demo decision/execution and server G06 source after the digest repair.
Demo decisions return `id:null`, synthetic version/status and do not write an
intent. Execution ignores executionIntentId/key and can create from a valid
selection digest without a persisted approval. Cancelling the UI therefore does
not establish backend revocation. Existing Demo E2E calls the decision function
but does not prove this property. Prior success claims remain limited to Pack
persistence, source integrity and UI flow.

The server module has explicit prepared/approved/cancelled/expired/version and
reviewed-facts verification. Its Node crypto, server audit-context and Pack helper
dependencies prevent directly importing it into the browser runtime. Recorded
shared-command extraction scope in TASK-234; no duplicate browser approval policy,
production change or broad refactor was introduced during this audit. No new
runtime tests were run; this is a source-backed incompleteness finding.

## Shared intent core extraction — 2026-09-10

Moved persistent G06 intent rules into `agentExecutionIntentCommands.ts`, a
platform-neutral factory with typed hashing, audit and Pack IO dependencies.
`agentExecutionIntent.ts` retains the server API/export facade and supplies Node
SHA-256 and existing helpers. All digest operations await sync/async bindings;
intent schema, TTL, authorization, optimistic versions and replay rules were
preserved. The audit-context helper already supported browser fallback, correcting
an overbroad statement in the previous dependency audit.

Verification:
- Original intent, HTTP execution and both MCP boundary/interoperability suites:
  4 files / 16 tests passed, including the real TypeScript/Python client paths.
- Parameterized intent suite runs both server hashing and asynchronous Web Crypto:
  1 file / 8 tests passed. This overlaps the first command's intent coverage;
  counts must not be added as disjoint tests.
- Browser-platform esbuild of the extracted core succeeded without Node bindings;
  its temporary output was removed. This proves bundle compatibility, not Demo IO.
- Lint, root/Web typechecks, isolated PGlite demo and Demo build passed; existing
  build warnings remain. No new browser UI or runtime binding was installed.

Demo approval persistence remains unimplemented until principal, Pack IO and
approval/execute bindings use this same core. No live model, deployment,
PostgreSQL or full-suite run occurred. Browser workflow/layout checks were not
rerun because the extracted core is not yet used by the browser. The earlier
candidate remains stale. TASK-234 remains in_progress.

## Shared Pack commands and browser bindings — 2026-09-10

Extracted Pack normalization/selection/persistence/replay/list/read into
`companyReceiptPackCommands.ts`, parameterized by sync/async SHA-256. The existing
server module retains its exports and owns document storage/PDF rendering.
Demo runtime now binds both Pack and G06 factories with Web Crypto and shared
audit. Demo Pack creation calls these shared read/replay/insert commands inside
its existing transaction, preserving its required assistant selection-digest guard.
No schema or PDF contract change was introduced.

Verification:
- Pack, intent, HTTP execution and MCP interoperability: 4 files / 21 tests passed.
- Intent parity with both intent and Pack factories using asynchronous Web Crypto:
  1 file / 8 tests passed; this overlaps earlier intent coverage.
- Lint, both typechecks, isolated PGlite demo and Demo build passed, with the
  existing build warnings.
- Actual Demo Company Receipts E2E passed, including stale/missing/wrong digest
  no-Pack assertions, corrected preview/create/replay/PDF checks and responsive
  receipt facts. Model transport remains injected.

Demo principal initialization and decision/execute entry points are not yet
connected to the persistent G06 core. This checkpoint makes shared IO available
and removes duplicate Pack writes; it does not establish governed approval,
actual human review, live Pack acceptance, PostgreSQL or production release.
Consolidated current status summaries to distinguish these remaining gaps from
completed portability work. TASK-234 remains in_progress; old candidate is stale.


## Demo persisted approval binding — 2026-09-10

The browser Demo now binds preparation, decisions and execution to shared G06 commands. The local synthetic principal has no login, credentials or grants. Bootstrap audit is attributed to the initiating human; execution/replay carries the agent and delegator. Actor/Company ownership, cancellation and persisted replay pass the new PGlite integration test. The gateway default tests pass all 8 cases. Root/web typechecks, lint, isolated Demo, Demo build and Company Receipts E2E pass. E2E also verifies unapproved, cancelled and stale requests create no Pack, then approved execution and replay read back the persisted PDF. Gateway responses in this regression are fixtures; no additional real inference or deployment occurred. Actual operator acceptance remains open.


## Open the verified assistant result — 2026-09-10

Source audit found that the assistant success view exposed the Pack ID and artifact
hash but no way to open that result. The existing screen now reads the exact Pack
ID through companyReceiptPackPdf with view purpose, verifies the returned bytes
against the completion hash, and renders an embedded PDF only while the same
modal/context/result remains active. Closing revokes its temporary URL. A failed
read or hash mismatch displays an error without repeating Pack creation. Existing
five-language Pack labels are reused.

The focused browser fixture verifies mismatch refusal, same-ID reading, exactly
one execution, desktop/375px preview layout and closing/reopening the success view.
It remains injected gateway/UI evidence, not a real-provider or human-viewing claim.
Common root/web typechecks, lint, isolated PGlite Demo and Demo build pass. Actual
Company Receipts E2E also passes, including persisted PDF hash consistency.

Gateway access investigation: the already signed-in Chrome Admin Settings page
at /admin/ui/#settings is read-only and directs server environment configuration.
No origin registration control is present there. No gateway settings, credentials,
permissions, provider calls or deployment were changed. A registered current-build
origin and actual human review remain necessary for the same-run live pilot.


## Reach every selected receipt before confirmation — 2026-09-10

The previous screen rendered only the first 20 rows and an unexpandable remainder
count. The assistant now renders batches of 20 with the existing localized Load
more action, focuses the first newly exposed row, and resets the batch for a new
request. Each row shows its exact amount/currency, receipt number, category,
purpose, notes, receipt/version, uploader and original-document identity/hash.
This changes presentation only; selection digests and approval/execution payloads
remain owned by the existing commands. All values are escaped before rendering.

A 21-row browser fixture checks that the last row is reachable and retains its
amount, purpose, document-version ID and hash. Desktop/375px expanded layouts,
confirmation/cancellation, PDF readback and five-language/two-theme checks pass.
Common lint, root/web typechecks, isolated Demo and Demo build pass. This does not
prove that original evidence files were opened, a human reviewed the rows, or a
real gateway model ran. No deployment or provider call occurred.

Next: bind original-evidence opening to the existing document content permission
and exact-version boundary; do not introduce an unrestricted source-file URL.
The pilot runner already uses authenticated /api/documents/:id/content with the
receipt's document version number. Browser adapters still need a supported bridge.


## Inspect exact original evidence — 2026-09-10

Each assistant preview row now exposes original-evidence inspection. It resolves
the governed receipt detail, checks receipt version/document identity/hash against
the preview, and reads the explicit document version through the existing document
content policy. It accepts PDF/PNG/JPEG/WebP up to 20 MB, verifies the bytes, and
shows a temporary embedded preview only while the same Company/actor/modal remains
active. Closing or starting a new request revokes the temporary evidence URL.
Inspection never approves an intent or creates a Pack.

API mode uses the existing authenticated documents content route with purpose and
access-key headers. Demo checks the same manage/self-read grants and binds a
read-only database storage provider with Web Crypto integrity. Shared scanAccess,
storageRead and accessCommands retain scan, ownership, exact-version and audit
policy; server wrappers retain default storage and transaction ownership. Browser
Demo uses the transaction-internal entry because its native PGlite transaction
client cannot start a nested ORM transaction. The initial real-browser failure
exposed this boundary and was repaired without duplicating access rules.

Four focused files / 19 tests pass, including storage/processing/API governance
and Demo denied, quarantined and cross-Company reads. Actual Company Receipts
browser E2E reads original bytes via the real Demo adapter and matches the preview
hash. Root/web types, lint, isolated Demo, Demo build and docs/diff checks pass.
Workspace desktop/375px regression verifies changed-byte refusal, exact-version
read and zero approval/execution during inspection. No full-suite, production,
real-provider or human-viewing completion is claimed.

The remaining milestone is the same-run real gateway and human acceptance from
a registered origin using the current build; the earlier Pages candidate is stale.
No gateway configuration, credential, access permission or deployment was changed.


## Current Pages candidate and rollback custody — 2026-09-10

Current source was built with the real /ERP-System/ Pages base into a private
independent directory. Candidate revision: `local-uncommitted-task234-cd4a5cba7814ac48`;
136 emitted files match their manifest byte counts and SHA-256 hashes. Manifest
SHA-256: `42c665bac21d361ddbbe5cf9e1d2ae46d2dafb3db03633bbe825a318fa6dd235`. Script-tag order and local entry
references pass; current selection/evidence/PDF controls exist in emitted assets.
The high-confidence private-key scan found no matches (not a comprehensive secret
audit). In-app browser startup reached the setup wizard with no console errors.
This is startup evidence only; no model was called from the candidate.

GitHub reports workflow-based Pages. The live manifest and last successful run
34241457749 both identify `a4b7982bcf10ac71d709740af80dc2e79991e1c5`. Its original
Actions artifact expired on 2026-09-09. A private rollback copy was recovered from
the live manifest and assets: all 133 files match; .nojekyll is not publicly served
and was reconstructed as the empty file proved by its manifest hash. Rollback
manifest SHA-256: `c1ea28faaf3e967ed8b591c923b99c4a488030809ea1d7ca3630d23a857928d1`. No redeployment was performed.

Candidate and rollback are local custody artifacts, not published revisions or
a tested rollback deployment. No commit/push/workflow dispatch occurred. A new
`env -u POSTGRES_URL npm test -- --run` was started for release regression; its
result remains pending and must not be reported as passing. Existing full-run
evidence remains historical until the new process reaches a terminal result.


## Release preflight while full regression runs — 2026-09-10

The existing full-suite process was re-polled and remains live; its worker process
is executing. No restart or passing conclusion was inferred from the quiet log.
All 857 recorded candidate build inputs still match their saved hashes. Generated
checks pass: 111 ordered migrations, 261 tables with no drift, 1,788 English
bootstrap keys and 141 business translation allowlist values.

Targeted release diff review confirms explicit server assistant activation remains
false by default; Company provider configuration, encrypted credential handling and
cancellation checks remain separate. The accumulated candidate also contains Agent
identity/confirmation/MCP foundations, document knowledge, account recovery and
translation changes. It must be reviewed as that combined source scope rather than
published as a receipt-screen-only patch. No source changes, commit, push or
deployment occurred during this preflight. Full-suite result remains pending.


## Final full-suite release regression — 2026-09-10

The subsequent full `npm test -- --reporter=dot` process completed with exit code
0: **204 files passed / 3 skipped; 900 tests passed / 3 skipped**. Duration
1206.83 seconds. The process was observed to completion without restart. This
supersedes the earlier 895-test integrated run and the MCP fixture-clock failures.

The three skipped suites are `postgresSecurity.integration.test.ts`,
`agentLifecycle.postgres.integration.test.ts`, and
`platformProvisioning.postgres.integration.test.ts`. Each explicitly selects
`describe.skip` when `POSTGRES_URL` is absent. This run does not establish
PostgreSQL acceptance, a deployment, or the same-run real gateway and human
Receipt-to-Pack acceptance. TASK-234 remains in progress.


## Exact source freeze and clean Pages build — 2026-09-10

Created local release ref `release/task234-demo-pilot-20260910`, commit
`db33a6420f7ad7b7d744062ad624b5392a94d5a2`, tree
`1ceab0248c5f20a124d93af9d611d9f91c3d0dc8`, parent
`a4b7982bcf10ac71d709740af80dc2e79991e1c5`. A temporary index captured the
180 inventoried changed/new repository paths. The original index hash and all
inventoried worktree file hashes were unchanged by snapshot creation and push.
The original local main checkout remains at its previous base with its work
preserved; the isolated detached checkout owns release verification.

Staged diff checking caught trailing blank lines in four newly added document
modules that an unstaged tracked-file check did not cover. Only those final blank
lines were removed. Comparing the 857 prior candidate inputs proved these were
the only input differences, each exactly one removed final newline.

In the clean checkout, both lockfile installs, lint, root/web typechecks, docs
check, isolated PGlite transaction proof and Pages build passed. Its 136 emitted
files are byte-for-byte identical to the previously tested candidate, excluding
the release manifest that now identifies the actual commit. The isolated checkout
is clean. This supports reuse of the existing affected-browser and full-regression
evidence; it is not a fresh full-suite or PostgreSQL claim.

Installation audit is not clean: root reports 14 advisories (6 low, 4 moderate,
4 high); web reports one high. High findings include brace-expansion/minimatch,
nanoid and server-side nodemailer. No dependency upgrade or claim of production
security acceptance is included in this static Pages checkpoint. Exact audit
outputs are temporary local evidence; the existing production gates remain open.

The user's standing authorization was used for a non-force push of this exact
commit to remote main after verifying the remote parent had not moved. Existing
Pages environment protection remains main-only. Pages run 34422487666 and CI run
34422487691 were observed in progress. Publication and remote CI are not yet
claimed successful by this entry. Browser handoff encountered a locked Mac;
Computer Use requires manual unlock before live page interaction can continue.


## Registered Pages release verified — 2026-09-10

[Pages run 34422487666](https://github.com/yapweijun1996/ERP-System/actions/runs/34422487666)
completed successfully for `db33a6420f7ad7b7d744062ad624b5392a94d5a2`.
At 2026-09-10T00:46:02Z, the live release manifest at
[the registered origin](https://yapweijun1996.github.io/ERP-System/) identified that
exact revision and workflow. All 135 served assets matched both the manifest byte
count/SHA-256 and the clean local committed build byte-for-byte. The 136th entry
is the known empty `.nojekyll` marker, which Pages does not serve (404); this is
recorded separately and is not a claimed successful HTTP asset read.

The public browser default GPT Demo gateway is now deployed. No provider inference
was made during release verification, and this does not establish the real
Receipt-to-Pack journey. Computer Use returned a locked-Mac error before browser
interaction. Manual unlock is required; no unlock bypass or session/Origin
transplant was attempted. Remote CI run 34422487691 was still executing all four
Vitest shards at the last observation. TASK-234 and the wider goal remain open.


## Real-browser setup obstruction and local repair — 2026-09-10

Computer Use became available again and opened the deployed registered-origin
Demo. Its existing IndexedDB schema upgraded from v103 to v110 without reported
console errors. The normal first-run wizard reached Finish but the shared command
rejected the default selection: Expenses & Tax was disabled for the existing
Master. The static picker recommended a module the current entitlement forbade.
No entitlement bypass or database reset was used in the real browser.

`readSetupModuleCatalogWithin` now reads only the scoped Master's configuration,
uses existing bootstrap defaults for absent rows, and projects unavailable
modules/dependencies without writing. Demo payload/adapter transports that view;
the wizard disables unavailable choices with five-language Platform-activation
copy. Production's new-Master defaults and transactional submission guard remain
unchanged. The read projection is advisory; it cannot authorize a write.

Verification: module provisioning and Demo setup tests pass, 2 files / 8 tests.
The real PGlite browser fixture explicitly updates two entitlement rows and checks
the affected row count before exercising disabled modules, dependent modules,
visible explanation, no horizontal overflow and successful lawful setup at desktop
and 375px. All four existing wizard viewports pass. The first fixture attempt used
the wrong scope property and updated no rows; it was corrected to the observed
`DB.erpSystem.scope.masterFn` contract and now asserts the fixture actually applied.
Lint, root/web typechecks, PGlite transaction proof and Demo build pass. This repair
has not yet been deployed by this entry and does not prove human Pack acceptance.


## Setup repair release verified — 2026-09-10

Commit `eaf7f6ebe9527fb895080201b71bfec9494751ce` (parent `db33a642`, tree
`c0629d8080a7b74fa5d09b47e580e4b30aad62ee`) contains only the six implementation/test
paths and six supporting documentation paths inventoried for this repair. Its
857 build inputs were checked against the current source. A temporary Git index
again preserved the original index. The private build passed and all 136 manifest
entries were hash-verified before a non-force push to the unchanged remote parent.

[Pages run 34423634266](https://github.com/yapweijun1996/ERP-System/actions/runs/34423634266)
succeeded. At 2026-09-10T01:02:21Z the live revision and workflow matched, and all
135 served assets matched both manifest bytes/hash and the local Pages build.
The empty `.nojekyll` remains separately recorded as not served. Remote CI run
34422487691 had all four Vitest shards pass before the new commit superseded its
remaining validation job; its terminal conclusion is cancelled, not success.
New CI run 34423634351 was observed running all four shards.

The Mac became locked again when attempting native Chrome control. The browser
connector cannot create Chrome tabs in this session; native Chrome remains a
possible route after manual unlock. The existing registered-origin browser Master
has an explicit disabled entitlement and Demo does not expose the API-only
Platform allocation workspace. Resume with an isolated fresh Demo browser session
or a properly authorized existing workspace, preserving the old database and its
explicit restrictions. No real gateway run, human approval, Pack creation or PDF
opening is claimed by this checkpoint. The earlier synthetic source PDFs remain
available locally for a future clearly labelled pilot upload.


## Real Demo login identity repair — 2026-09-10

A fresh native Chrome Incognito session on the registered origin completed the
normal wizard for Receipt Pilot Singapore with the public Demo administrator.
After submitting the login form for that administrator, the UI displayed Avery
Tan in the seeded Acme Singapore Company instead. Source evidence showed boot
scope fixed at C-SG and applyData falling back to that Company's owner when the
selected account was absent. No real gateway request or pilot Pack write followed
this incorrect identity.

An added setup browser assertion reproduced the failure before repair: the new
administrator/Company identity never appeared after login (15-second assertion
timeout). The Demo adapter now resolves current eligible human role memberships
before scoped payload loading, restores only an authorized Company preference,
and never substitutes another owner for a selected identity. Invalid membership
retires local sign-in; Company switching no longer exempts Company Owners from
membership checks. Login and persona projection exclude non-login/non-human or
offboarded accounts. Reset clears the additional Company preference.

After repair, all four wizard viewports pass. Desktop and 375px cover actual login
form submission, identity/Company preservation through reload, rejection of a
foreign saved Company, Company Owner cross-Company denial and membership revocation
returning to an unprivileged login view. The normal Company Receipts and Receipt
assistant browser regressions also pass, including real Demo evidence access,
Pack/PDF operations, cancellation/scope guards, five locales and both themes.
Lint, root/web typechecks, PGlite transaction proof and Demo build pass.

Remote CI34423634351 for the earlier eaf7f6e revision passed all four Vitest shards
and its PostgreSQL16 non-superuser security lifecycle step. Its cross-engine
transaction proof was still executing at observation; the full job is not yet
claimed successful. This new repair changes Demo JavaScript and its browser test,
not the server/PostgreSQL source. Production and real human pilot gates stay open.


## Login repair release verification — 2026-09-10

Commit `40d3cf0f4d05646a32f6ff12a5b52294cce0907f` was deployed successfully
by Pages run 34425059328. At 2026-09-10T01:25:31Z all 135 HTTP-served assets
matched both the release manifest hashes and the locally built committed inputs.
The remaining manifest entry is the empty, unserved `.nojekyll` marker.
CI run 34425059341 remains in progress. Chrome reported the Mac locked;
the existing isolated Company has not been reset, and real gateway-to-Pack
human acceptance remains open.


## Closed shell menu accessibility repair — 2026-09-10

Native Chrome reload showed the correct new Demo administrator. Continued UI
navigation repeatedly exposed closed Account/Fiscal menus. The shared .pop CSS
used opacity and pointer-events without visibility, so visually closed controls
remained in accessibility navigation. A new setup/login browser assertion failed
before the repair with "dismissed shell menu remains accessible".

The shared stylesheet now uses visibility:hidden for closed menus and
visibility:visible for .show. No business commands, permissions, schema or
Company configuration changed. The existing four-viewpoint setup regression
passes, including desktop/375px role navigation, open and Escape dismissal.
Independent 1280/375px shell checks passed closed/open, scroll positions,
Escape and trigger toggle with zero browser errors and bounded menu rectangles.
Lint, root/web typechecks, PGlite transaction demo and Demo build pass.
Native Chrome post-release acceptance and the real Receipt-to-Pack human outcome
remain open. The local screenshot harness waits for the existing opacity
transition to settle; an intermediate animation frame is not a visibility proof.


The menu repair is now deployed as commit
`6f06a883bdddbdb86d92ca5f1fa233534246c5d4`, Pages run 34426075225 (success).
At 2026-09-10T01:37:26Z all 135 served assets matched the manifest and local
committed build byte-for-byte. The empty .nojekyll marker remains unserved.
All 857 inventoried build inputs matched the snapshot commit, and the original
Git index stayed unchanged. CI 34426075276 is still running; the prior login
revision passed all four unit shards before the menu repair superseded it.
Mac lock prevented the next native Chrome check. No live receipt upload,
human confirmation or Pack creation is claimed. New clearly labeled synthetic
PDF sources for Receipt Pilot Singapore were rendered and inspected locally
(SGD 18.25 and SGD 23.50; no real payment or tax-invoice claim).


## Native Chrome resumed and full CI passed — 2026-09-10

CI run 34426075276 completed successfully for 6f06a883: all four unit shards and
the aggregate typecheck/transaction/browser validation job passed. Native Chrome
reloaded the original Incognito workspace and showed Receipt Pilot Singapore /
Demo Administrator. Closed-menu navigation no longer obstructed the page.
Company Receipts opened normally with zero records; Confirm receipt correctly
reported no eligible evidence. My Receipts correctly refused an account without
an active employee link.

The normal Add Staff flow was inspected. Existing identity reuse only joins an
identity to a different Company; activateStaffOnboardingWithin deliberately
rejects an existing userCompany membership. No membership guard was changed.
A separate Synthetic Receipt Operator / receipt.pilot@example.test draft is now
prepared in the browser, on Login identity, with an empty initial-password field.
Department Synthetic Testing, title Receipt Pilot Operator and synthetic salary
1000 are test-only values. No employee/account activation has occurred. The
operator must personally enter and submit the initial credential via the UI;
passwords must not be sent to the agent or recorded in evidence. After activation,
continue governed source upload and the same-run gateway/human/Pack journey.


## Immediate account access — 2026-09-10

User decision: remove mandatory activation for all Demo and real Company accounts.
Shared onboarding and account creation now write active accounts with no first-login
password-change requirement or initial login expiry. Resets replace the password and
revoke old sessions without reintroducing activation. The first-login form and API
permission gate are removed; the retired endpoint returns 410 without modifying
credentials. Historical schema names remain compatible. Migration 0111 repairs
legacy pending flags, preserves disabled/offboarded access and appends one audit
record per changed user. Re-running it does not duplicate evidence.

Actual browser creation exposed an older six-grant Employee-role validator that
rejected the nine-grant role established by migration 0097. The validator now accepts
the exact current or legacy contract, without rewriting existing roles or permitting
extra grants/broader scopes. Own-receipt mutation commands still enforce ownership.
Demo logout and normal login clear the prior administrator impersonation marker.

Verification: four focused test files / 19 tests passed; session tests / 11 passed;
auth lifecycle and authorization freshness / 10 passed.
The real Demo adapter E2E passes at 1280px and 375px: create through the form, inspect
persisted active state, sign out, sign in with the assigned password, verify ordinary
employee identity (no administrator impersonation), and open My Receipts. There is
no activation form, horizontal overflow or browser console/page error. Both final
screenshots were inspected. These are synthetic local browser fixtures, not human
production acceptance. Lint, root/web typechecks, PGlite transaction proof, Demo build,
112 ordered migration artifacts, i18n and documentation link checks pass. Production
PostgreSQL migration was not executed by these local checks.

Relevant sources: `src/modules/hr/employeeAccount.ts`, `staffOnboarding.ts`,
`src/api/http.ts`, `src/api/routes/auth.ts`, migration 0111 and
`tests/e2e/staff-account-immediate.spec.mjs`. Account/API regression logs and screenshots
are retained under `/tmp/erp-account-immediate-*`. TASK-234 remains in progress;
real gateway-to-Pack and human evidence/selection review remain open.

Release verification — 2026-09-10T10:09:19Z: account-access revision
`55a57b39302fc6bdf44a68bcd4ccfdbf9c45545f` is deployed by Pages run 34464383892.
All 135 served files match the committed local build by size and SHA-256; the empty
`.nojekyll` marker is separately unserved. CI run 34464383875 is still running;
no full-CI or production PostgreSQL migration acceptance is claimed. Native Chrome
follow-up was unavailable because the Mac was locked. The 40 local focused tests
and desktop/375px synthetic browser results remain the verified account evidence.


### Live pilot access audit — 2026-09-10T10:15Z

Native Chrome access again returned Mac locked. The same access condition was
observed during the account release turn and two consecutive goal continuations.
An unlock request remains pending. The account change is deployed and locally
verified, but the live Receipt-to-Pack evidence/selection review and persisted
result acceptance cannot proceed in the existing workspace until access returns.
No human confirmation, live upload or Pack outcome is claimed. The goal is blocked
on this human-access dependency; TASK-234 remains in progress.

CI run 34464383875 was authoritatively observed in progress at 10:15Z with all four
Vitest shards running. The existing watcher session 31149 remains live; no test was
restarted or cancelled. Resume that run, not a replacement, when checking results.
After Mac unlock, resume the existing Receipt Pilot Singapore workspace without
resetting data, complete normal account creation, inspect receipt evidence, obtain
exact human confirmation, and verify/open the persisted Pack and PDF.

### Native Chrome account-policy confirmation

Native Chrome resumed — 2026-09-10: user selected A and the Mac unlocked.
The existing Receipt Pilot Singapore workspace was preserved and refreshed to the
new account-access release. The actual Add Staff screen now shows Password and
account-ready-after-creation wording; the old activation/first-use expiry/forced
change copy is absent. The synthetic staff profile was re-entered and the browser
is at Login identity with the account password awaiting operator entry. No account
has been created or receipt uploaded yet. Two route-label fallback warnings were
visible in DevTools; no failure was attributed to them. CI 34464383875 shards 2/4
and 3/4 passed; shards 1/4 and 4/4 remain running. Full CI is not yet accepted.


### Automatic password and manual email handoff

Automatic credential handoff — 2026-09-10: user-directed Add Staff now generates
secure passwords automatically. The API uses Node crypto and Demo uses Web Crypto,
with 24 random bytes. Shared onboarding stores the login hash and seven-day encrypted
handoff atomically; existing-identity links preserve credentials. No manual password
entry or activation is required. Creation stays on the employee record. HR-write
operators can copy the password and generate/copy a complete English email template
for manual delivery. Every copy rechecks the reveal boundary; responses are no-store,
clipboard failure is explicit, and plaintext is not persisted or automatically sent.
This supersedes the earlier operator-password-entry handoff. Local API and desktop/
375px tests pass; final integrated regression and release verification follow.

Source ownership: src/auth/employeeAccountLifecycle.ts generates server credentials;
src/modules/hr/staffOnboarding.ts stores hash/envelope in the shared transaction;
src/api/routes/hr.ts gates generation/reveal and sets no-store; Demo adapter rechecks
live hr.write before account mutations/reveal. screens-hr-new.js removes password
entry and stays in HR; screens-hr.js provides the ephemeral handoff dialog. The
organization login code comes from the scoped Master via readEmployeeAccount.

Evidence covers generated-password login, idempotent replay retaining one envelope,
no plaintext in ordinary response/database/audit/localStorage, HR allow/employee deny,
existing-user preservation, reset/revocation/expiry regressions, copy/preview equality,
clipboard denial, modal closure cleanup and bounded desktop/375px layout. Screenshots
mask the credential-containing preview. No email has been sent. Production PostgreSQL
rollout and the real receipt pilot remain separate and unaccepted.

Final local result: 4 files / 20 tests passed in 34.12 seconds. Desktop/375px
creation, password/email clipboard, failure reporting, HR identity retention, direct
login and employee reveal denial pass with zero page/console errors. Both settled
masked handoff screenshots were inspected and fit the viewport. Lint, root/web
types, PGlite transaction proof, Demo build, i18n artifacts and docs/diff checks pass.
Logs: /tmp/erp-auto-password-final-tests.log, browser-final.log and final-static2.log
under the same erp-auto-password prefix. No plaintext credential appears in evidence.

Automatic credential release verified — 2026-09-10T11:17:30Z: revision
`0e204ff60ba308a75ca2c7f31b9b3e73d2975864`, Pages run 34469731345 succeeded.
All 135 served files match the exact committed local build by size and SHA-256;
the empty .nojekyll marker is separately unserved. Original worktree/index retained.
Latest CI 34469731319 remains running; previous CI 34464383875 passed all four unit
shards before its aggregate job was superseded/cancelled. No full-CI acceptance is
claimed. Native Chrome follow-up was unavailable because the Mac locked again.
The old manual-password request is obsolete. Refresh and resume ordinary Add Staff:
password generation is automatic, followed by employee-record Account handoff.
No email was sent; production deployment and real Receipt-to-Pack acceptance remain open.


### Automatic credential release CI checkpoint

CI checkpoint — 2026-09-10: run 34469731319 for 0e204ff passed all four Vitest
shards, lint, both typechecks, generated-schema/drift/permission checks, PostgreSQL
16 non-superuser security lifecycle and the PGlite/PostgreSQL transaction/concurrency
proof. The build passed; the five-language desktop/mobile browser matrix is running.
Remaining smoke/screen/layout/subpath checks are pending, so full CI is not accepted.
Native Chrome again reports Mac locked; unlock request pending. No real receipt
upload, human selection confirmation or persisted live Pack outcome is claimed.


## Goods Receipts translation release — 2026-09-10

Release verification — 2026-09-10T11:57:17Z: translation repair
539f4f008308a07b4023fb49e31028905bdce1e3 is deployed by Pages 34473757892.
All 135 served assets match the committed build; the empty .nojekyll marker is
separately unserved. All 859 build inputs match the snapshot and the original
index is unchanged. Desktop/mobile smoke, 50-route transaction layout and the
operational workspace layout audits pass with zero console/page errors.
CI 34473757884 remains running; full-screen local audit is running. Production
root and health still return HTTP 502; no production migration/deployment claimed.

Additional local release checks — 2026-09-10: production public-subpath browser
contract passed against an isolated API-mode build at /erp (no production network
or writes). This is source/runtime fixture evidence, not a repair of the remote
502. The original Demo dist remained available to the running 130-route audit.

Native browser follow-up — 2026-09-10: the existing Receipt Pilot Singapore
Incognito session showed the new Add Staff profile form. During a batched synthetic
field entry the user switched the foreground Chrome window, and part of the synthetic
text landed in a different unsent chat draft. No send action was taken. Cleanup was
interrupted by another window switch and is not verified. Foreground input is paused
until the operator provides a stable window or removes the draft text. Do not reuse
old accessibility indices or continue multi-field native input during concurrent
user activity. No employee creation, receipt upload or Pack acceptance is claimed.

## Tunnel authorization attempt — 2026-09-10

The guarded system Tunnel activation script was invoked through the macOS
administrator-authentication dialog and safely cancelled after the automation
session could not observe the dialog. The system config SHA-256 remains
`86071035baffea2ce90873353db612068921c567794f39fcb51919ae7e749076`; no backup or
configuration mutation was created, and the public probe remains HTTP 502. This is
an external authentication boundary, not an application rollback or a failed
Receipt-to-Pack transaction.
The prepared candidate was independently checked with `cloudflared tunnel --config
~/Documents/ERP-Production/cloudflared-config.proposed.yml ingress validate` and
returned `OK`; the active system configuration remains unchanged.

## Current regression and Demo gateway recheck — 2026-09-10

The current workspace full Vitest run completed with 204 test files passed and 3
skipped; 900 tests passed and 3 skipped (about 20 minutes). This covers the dirty
workspace containing the HR tenant-context/idempotency repair and is separate from
the successful b932 GitHub Actions run and the coherent 9ec8c0e production image.

The registered Pages origin was rechecked against the configured browser Demo
gateway: `POST /demo/session` returned HTTP 201 and the bounded `demo-auto`
response returned HTTP 200 with the sanitized proposal `{"search":"Cafe"}`. The
unregistered local preview origin returned HTTP 403. No session token or business
data was printed, saved or sent. The production server assistant remains default
deny, and no production OCR endpoint is configured.

The same checkpoint passed schema-drift (261 tables), permission-registry (317
codes), Demo schema/pack, i18n bootstrap/business and production-RLS coverage
checks (231 policy tables over 261 generated schema tables). The local release
verifier also matched the running 9ec8c0e revision and all 126 production asset
hashes.

The local browser smoke also passed at 1280×800 and 375×812 with the expected Demo
title/dashboard content and zero console or page errors. This smoke result is
local browser evidence and does not establish public Tunnel, OCR-worker or human
visual-review acceptance.

Overnight production recheck — 2026-09-11: the loopback API/Web/DB/calendar-worker
remain healthy on revision `03487b13ce838407d97cd00697bd2b54b4a7c918`; public
`/erp/health` remains HTTP 502 because the active system Tunnel has no `/erp` rule.
The guarded candidate still returns `OK` from cloudflared ingress validation, and
the active system configuration remains unchanged.

Both persisted SG/MY PDF artifacts were queued again to the current Codex panel on
2026-09-11; `open_in_codex` returned `queued` for each. This makes the result
available for operator review and does not prove that a business user viewed or
accepted either PDF.

The guarded Tunnel activation script passes `bash -n`, ShellCheck and its non-root
guard; the proposed configuration's pinned SHA-256 and 0600 mode match the review,
and the executable script is 0700. No privileged installation or rollback was
executed in this checkpoint.

## Production Pack source-image audit — 2026-09-11

The full-page audit was repeated against the private SG and MY artifacts and the
source rows in the production PostgreSQL database. Both source rows are `image/png`,
68 bytes, `page_count=1`, with PNG dimensions `1x1`; the document blob length matches
the version metadata. `pdfinfo` reports two A4 pages and `pdffonts` reports the
embedded Noto OpenType font. `pdfimages` reports a single `1x1` image on page 2 and
`pdftotext` returns no text for that page. The register page and pack/body hashes
remain valid. This confirms a synthetic placeholder source rather than a renderer
or storage-integrity defect. Human visual/business acceptance is therefore still
open, and the pilot source must be replaced before treating the PDF as usable
receipt evidence.

The active cloudflared log provides the direct 502 cause: `gmb01.xyz/erp/health`
matches `ingressRule=5` and attempts `localhost:6859`, which refuses the connection.
The reviewed candidate routes only `/erp` to the healthy production proxy at
`127.0.0.1:18791`; privileged activation remains pending.

Focused current recheck — 2026-09-10: 16 TASK-234 runtime/API/provider/assistant
test files passed with 115 tests. `npm run test:e2e:receipt-assistant` passed the
Demo workspace at desktop/mobile sizes with cancel/recovery, scope, locale, theme
and focus checks. `npm run test:e2e:company-receipts-api` passed the isolated
authenticated API Receipt/Pack journey, including confirmation, search/range,
Preview/PDF/Print and responsive bounds. No real provider call or public Tunnel is
claimed by these local checks.

The current native WebMCP rerun also passed on 2026-09-11: Chrome 152.0.7977.83
with the official `WebMCPTesting` flag registered the six governed Receipt/Pack
tools and completed native reads, visible cancellation/confirmation, one-Pack/PDF
verification, permission/scope/navigation retirement and 375px bounds. This is
isolated Demo evidence and does not claim production or real-provider execution.

## Tiny-image evidence-page repair — 2026-09-11

The production source audit identified the actual presentation boundary: both fresh
Receipt Pack source versions are governed 68-byte `image/png` blobs whose PNG headers
declare `1x1` pixels. The bytes and database metadata agree, but rendering that pixel
at natural size produces a page that is technically present and visually unreadable.
The source remains unchanged; no migration, seed, document mutation or database reset
was performed.

The renderer repair was implemented in `src/modules/documents/evidencePdf.ts` and
covered by the one-pixel identity-page test in
`src/modules/expenses/companyReceiptPackPdf.test.ts`. PNG/JPEG sources below 2×2
pixels now receive a readable identity page containing the reason, original file name,
MIME type and SHA-256. The original source bytes remain the governed storage object.
The release sequence was `4162dad` (tiny-image guard), `8201a79` (force-recreate every
application container), `ab2e354` (wrapped identity-page text) and
`03487b1` (single-line notice). The final release script completed successfully with
the database preserved; API, Web, calendar-worker and PostgreSQL were healthy and API
health matched Web `release.json` at
`03487b13ce838407d97cd00697bd2b54b4a7c918`.

Final private artifacts were regenerated through the authenticated, tenant-scoped
render path and are mode 0600:

| Country | Pack | Source selection digest | PDF SHA-256 | PDF bytes | Structural result |
| --- | ---: | --- | --- | ---: | --- |
| SG | `1` | `bdb1832eaeef6519df9e126185c82b0488a1b242244c7a69d084358480544244` | `3bcba83bc29d60f407c6a6ec9194702b3a9d9c8734270e0904c2235ebfc18b69` | 28,263,373 | 2 A4 pages, PDF 1.7, embedded Noto OpenType, no page-2 image XObject |
| MY | `2` | `71cd72df53e9a805babb2dd63926e4789068f13433cbeaec6d4fd33e0d9f8fee` | `4a377d1ef83945709109c3b07e7e9f0d435ddca89c09a81713651c04810d210a` | 28,263,378 | 2 A4 pages, PDF 1.7, embedded Noto OpenType, no page-2 image XObject |

`pdftotext` returns the single-line notice `Source image is too small; identity is
preserved below.` followed by the original file name, MIME type and source SHA-256 on
page 2 for both artifacts. Poppler rendered both pages without parse errors; direct
visual inspection shows the register page is readable and the identity page is fully
visible. This is renderer and artifact evidence, not human business acceptance: the
source itself is still a synthetic one-pixel placeholder and must be replaced with a
readable receipt image/PDF before production sign-off. The public `/erp/health` 502 and
the disabled production OCR/provider gates are unchanged. Final repository checks after
the renderer edit passed the focused 12-test Vitest run, lint, root/web typechecks,
PGlite demo, Demo build, desktop/mobile smoke, authenticated Company Receipts API E2E,
documentation links/diff checks and the loopback release verifier. The prior full
workspace regression remains separately recorded as 204 files / 900 tests passed with
3 PostgreSQL suites skipped because `POSTGRES_URL` was unset.

## Immediate account access copy cleanup — 2026-09-11

The Demo login shell no longer presents the retired first-login activation rule.
The normal account branch now states that access is ready immediately; the
showcase branch retains its separate one-click wording. The Demo data adapter
comment now describes lifecycle fields as compatibility and credential-handoff
metadata while confirming that new accounts are active immediately. Localized
handoff labels now describe the seven-day credential-copy window rather than a
first-use deadline. Historical
`account_state` and encrypted handoff names remain for migration, audit and
recovery compatibility.

`node tests/e2e/staff-account-immediate.spec.mjs` passed at 1280px and 375px,
covering generated-password creation, active-account persistence, direct employee
login, credential/email copy, no activation form and the revised login-shell copy.
An actual local Chrome Demo session also rendered `Account access is ready
immediately; sign in with your account credentials.` after sign-out. This is a
local Demo/browser check; it does not claim that the stale GitHub Pages deployment
or the public Cloudflare Tunnel has been updated.

The HR employee profile's retained `preactivated` compatibility state now renders
as `Legacy account` (with equivalent localized wording) and uses the ready account
tone. This keeps old database rows readable without presenting activation as a
current user action. The source-level change passed the Demo build, lint, web
typecheck and desktop/mobile smoke checks.

## Mac screen-saver preference recheck — 2026-09-11

The current user preference was read back after the requested change: macOS
`com.apple.screensaver` reports `idleTime=0`, `askForPassword=0` and
`askForPasswordDelay=0`. `pmset -g custom` reports AC display sleep and system
sleep both set to `0`; battery system sleep remains `1` by design. This confirms
the screen saver and lock prompt are disabled for the current user and does not
claim that a manually locked session can be bypassed.

## Full workspace regression recheck — 2026-09-11

The current dirty workspace was rechecked with `npm test -- --run`. Vitest exited
successfully after 1186.52 seconds: 204 test files passed, 3 were skipped, 901
tests passed and 3 were skipped. This is the latest repository-level regression
evidence and includes the account-access copy, provider/bootstrap, Receipt Pack
renderer and related permission changes. Three PostgreSQL guards were skipped
because `POSTGRES_URL` was unset; this run does not establish production,
public-Tunnel, OCR, real-provider or human visual acceptance.

## Fresh disk-backed pilot runner recheck — 2026-09-11

The reproducible `scripts/receipt-assistant-pilot.ts --fixture` runner completed
again for both Company scopes against new private PGlite databases. SG (`C-SG`)
and MY (`C-MY`) each created two synthetic receipts, performed a successful
`receipt.get` detail and source-byte hash check for every selected row, required
the exact selection digest before creation, persisted one Pack, closed and
reopened the database, then re-rendered and hash-verified the saved PDF. Both
runs used the pinned OpenAI-shaped local fixture with five provider calls, zero
retries and simulated confirmation; no network provider or spend was used.

The SG run produced Pack `1`, selection/source SHA-256
`05457082a622250b28a307702d8d1e6cfcdeebe22edc34cf61bac25ed7100dc7` and PDF
SHA-256 `a60e5eb9eefd756eb114736baefb27543ed1828fb0886b5587e3971b3d663141`
(14,134,021 bytes). The MY run produced Pack `1`, selection/source SHA-256
`7ecf130c9a46e012ce472c80890421c22caba23f6d22d1dcc9a7256a8abae4c7` and PDF
SHA-256 `42b30b7428c60197ccaa638ddbaa03b175a83d49ed425602c28d661ffd5d54e6`
(14,134,021 bytes). The runner reports `persistedAfterReopen=true`,
`savedPdfVerified=true` and `sourceFilesHashVerified=true` for both. It also
reports `humanViewedPdf=false` and `humanViewedSources=false`; these results are
technical synthetic evidence, not real-provider, production or business-user
acceptance.

## Refreshed GitHub Pages candidate after account-copy cleanup — 2026-09-11

The current worktree was rebuilt with the existing Pages settings (`GITHUB_PAGES=true`,
`VITE_DATA_MODE=demo`) into the private candidate directory
`/tmp/erp-pages-candidate-F5T0sA`. The manifest is explicitly marked
`local-uncommitted-task234-20260911`, contains 136 hashed assets, and has SHA-256
`41c6292fc224381436875259388e76f0dda9f0ca621782ffeb16f501c248ca47`. The normal
Pages fallback files are present; the HTML load-order check confirms the Demo
gateway precedes the Demo adapter, and every manifest byte count/hash matches.

This candidate includes the current immediate-account and legacy-account wording.
It is a private local artifact only: no commit, push, workflow dispatch or public
deployment occurred, and it does not close the public Tunnel, real-provider, OCR
or human-acceptance gates.

## Synthetic source PDF readability recheck — 2026-09-11

The two private source PDFs saved by the fresh runner were independently checked
with `pdfinfo` and `pdftotext`. Each is a one-page A4 PDF (PDF 1.7) with readable
text beginning `SYNTHETIC RECEIPT - NOT A REAL TRANSACTION` and bounded receipt
metadata. This confirms that the runner's source-file handoff is inspectable; it
does not change the production source rows, which remain synthetic 1×1 PNGs, and
does not establish human business acceptance or a real OCR/provider result.

## Isolated Pages freeze recheck — 2026-09-11

The current source was overlaid onto the unchanged remote `main` parent
`b9326f87732d904dc640d78d8b783418f5e9eb90` in a detached worktree, with the
original working tree and index left untouched. The reviewed 26-path source
snapshot is commit `35739bc0e14a0b021020234c33cd79666f78ef03` (`feat: update
TASK-234 receipt pilot release`). Clean root and web `npm ci` installs completed;
lint, root/web typechecks, documentation links, diff checks and the isolated
PGlite Demo proof all passed. The Pages build completed with 136 manifest files,
the gateway script before the Demo adapter, present `404.html`/`.nojekyll`, and
matching byte counts/SHA-256 for every manifest entry. Excluding the release
manifest, its 135 emitted assets are byte-for-byte identical to the private
`/tmp/erp-pages-candidate-F5T0sA` candidate. Dependency audit warnings are
unchanged and remain a separate security-gate concern.

This is a local freeze candidate only: it has not been pushed, workflow-dispatched
or deployed, and the public Pages site remains the older revision. The source
snapshot is ready for a non-force push only after the remote parent is confirmed
unchanged.

## Final focused browser/API recheck — 2026-09-11

After the account-copy cleanup, `npm run test:e2e:receipt-assistant` passed for
desktop/mobile workspace state, cancellation and recovery, scope guards, five
locales, both themes and focus/overflow checks. The rebuilt API-mode application
also passed `Company Receipts API E2E (PGlite)`: authenticated confirmation,
refresh/search/date-range reads, Preview/PDF/Print and responsive bounds. The
working tree was then restored with a successful `npm run build:demo`, and the
final `npm run smoke` passed at 1280×800 and 375×812 with zero console/page
errors. These checks are local evidence; they do not close the public Tunnel,
production OCR/provider or human visual/business-acceptance gates.

## Public Pages revision recheck — 2026-09-11

`https://yapweijun1996.github.io/ERP-System/` returned HTTP 200, but its served
HR bundle still contains the retired `Awaiting activation` wording and does not
contain `Legacy account` or `Account access is ready immediately`. The public
site therefore remains an older Demo revision; the current candidate remains
private under `/tmp/erp-pages-candidate-F5T0sA` and has not been pushed or
deployed. A Code-MCP Chromium inspection attempt also returned an internal tool
error, so this recheck uses the direct served HTML/bundle response as its
source evidence.

## Public Pages deployment and hosted Demo Receipt-to-Pack E2E — 2026-09-11

The isolated release commit `ae7a3cfabde0e03a704d943dc1d99cc3bb672e2a` was
pushed with a normal fast-forward update and deployed by GitHub Pages workflow
[34516594532](https://github.com/yapweijun1996/ERP-System/actions/runs/34516594532).
The public `release.json` now reports that revision, `dataMode=demo`,
`fileCount=136` and the workflow run ID. The public index and HR bundle return
HTTP 200; the served bundle contains `Legacy account` and no longer contains
`Awaiting activation`. All 135 served assets (the empty `.nojekyll` marker is
not served) match the release manifest byte counts and SHA-256 values.

An isolated, fresh browser context on the actual GitHub Pages origin completed
the Setup Wizard with `hr` and `expenses_tax` selected, signed in the newly
created administrator immediately, and rendered the Company Receipts register.
The context then used one explicitly labelled synthetic PNG fixture
(`task234-public-synthetic-receipt.png`, 67,453 bytes, SHA-256
`4cb3d1be0e46bcd2ceffe1c5d36657fafdd6d507a975580dc5026ccd418ba96e`) as a
governed clean-evidence fixture. The visible evidence picker and receipt
confirmation form created one Company Receipt. The assistant then made the
real browser-origin Demo gateway calls `POST /demo/session` (201) and
`POST /demo/v1/responses` (200), displayed a one-row exact preview with
`SGD 32.8000`, required the visible human confirmation action, and reached the
succeeded state. The result was reopened through the hosted UI's PDF iframe
and read back from the isolated browser database: one persisted Pack, one
selected receipt/document, 14,183,204 PDF bytes and artifact SHA-256
`e8e6dab66480dbeba35543426e9d9a5b496bc32b4af8fba217eb47dd18046770`.
The run recorded zero console or page errors and no gateway token was printed
or persisted in the browser output.

The synthetic clean-scan row was inserted only inside this disposable browser
context because the static Demo upload path deliberately has no malware scanner;
it is not production OCR/scanner evidence. The hosted run therefore proves the
deployed UI, real Demo gateway protocol, confirmation boundary, shared Demo
commands and persisted Pack/PDF readback, but it does not prove a real provider
account, production OCR, public `/erp` Tunnel health or human business
acceptance of a real receipt. The first long natural-language prompt returned
an invalid gateway proposal; retrying with the concise merchant term `Coffee`
completed the same governed flow, leaving provider-output variance as a live
assistant usability risk.

## Public Pages docs-only manifest follow-up — 2026-09-11

The documentation-only follow-up commit
`e2e24f4f61259f58cd412eebb85e2fac75c5db6a` was pushed normally after the hosted
run and deployed by GitHub Pages workflow
[34519668605](https://github.com/yapweijun1996/ERP-System/actions/runs/34519668605).
The current public `release.json` reports that revision, `dataMode=demo`,
`fileCount=136` and workflowRunId `34519668605`. A fresh read-only origin
verification returned HTTP 200 for `index.html` and `screens-hr.js`, confirmed
the HR bundle contains `Legacy account` and no `Awaiting activation`, confirmed
the gateway script tag precedes the Demo adapter, and matched all 135 served
assets against the manifest byte counts and SHA-256 values. This follow-up
changes documentation and manifest revision only; it does not change the
application assets used by the hosted E2E above.
