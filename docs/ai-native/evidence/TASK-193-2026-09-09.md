# TASK-193 — Local tenant account recovery — 2026-09-09

## Scope and result

Company Owner and Master Admin reuse the tenant human-account recovery command.
The login screen now links to a five-language recovery page at
`<public mount>/reset-password`. The page requests email instructions, confirms a
new password and returns the user to sign-in. It does not authenticate the user
from the reset link. The local mail sink captures messages in memory only.

TASK-193 remains **Blocked**. No SMTP configuration, external email, production
migration, deployment or live recovery was performed. Platform Superadmin has a
separate `platform_principal` credential/session boundary and no email recovery
implementation in the current release scope. The tenant endpoint must not reset it.
The task's three-realm acceptance is therefore still incomplete.

## Source-backed gaps and changes

- `src/auth/lifecycle.ts`: confirmation already locked tokens and enforced expiry,
  single use and the Employee-account exclusion, but did not recheck active human
  login eligibility. It now rejects inactive, login-disabled or non-human users
  without consuming the token or changing the password. Existing session revocation
  remains in the same transaction.
- `src/api/routes/auth.ts`: normalize email before constructing the throttle key,
  preventing leading whitespace from splitting one account across keys. Count the
  attempt before token/outbox issuance so issuance failures also count. Public
  responses remain identical for unknown, ineligible and throttled accounts.
- `src/worker/outbox.ts`: raw mail exceptions could include a password or bearer
  link. Persist only application-owned payload/token/delivery/delivery-record phase
  codes; preserve leased retry, backoff and terminal dead-letter behavior.
- `web/public/assets/auth-recovery.js`, `platform-workspace.js`, `app.js` and
  `web/index.html`: add tenant recovery without introducing a Platform reset path.
  The token is read from a URL fragment, immediately removed with `replaceState`,
  and held only in memory. Handle fragment-only email navigation as well as a new
  document; clear password inputs after attempted confirmation. Bound fetches to
  15 seconds and show localized failures without displaying server/provider text.
- The five canonical locale files and generated English bootstrap provide the new
  labels. Shared auth layout and theme behavior are reused.

## Verification

- `npx vitest run src/auth/lifecycle.test.ts src/api/authLifecycle.test.ts src/worker/outbox.test.ts src/auth/rateLimit.test.ts src/auth/adminLifecycle.test.ts src/modules/integration/eventLog.test.ts`
  passes **6 files / 30 tests**, exit 0 (42.11 seconds).
- The API mail-sink journey exercises Company Owner and Master Admin, exact target
  audit attribution, encrypted/redacted outbox payloads, old-session revocation,
  login with the new password, link replay and Platform-principal non-mutation.
- Domain fixtures cover superseded links, the exact expiry boundary and live
  eligibility downgrade. Request tests cover seven email-case/whitespace variants
  yielding only five issued tokens. Worker tests inject secret-bearing errors and
  verify that persisted evidence contains neither the secret nor bearer token.
- `npm run build` and `npx tsx tests/e2e/auth-recovery.spec.ts` pass against a fresh
  local PGlite API with an in-memory mail sink. Browser checks cover the login
  entry and realm visibility, desktop/375px, full-document and fragment-only links,
  mismatch, actual password persistence, replay denial, token absence from URL and
  browser storage, cleared inputs, status focus and no horizontal overflow.
- The same browser fixture also passes beneath `/erp` after
  `ERP_PUBLIC_URL=https://erp.example.test/erp npm run build`, followed by
  `TASK193_TEST_MOUNT=/erp npx tsx tests/e2e/auth-recovery.spec.ts`.
  Both root and subpath runs use only a disposable local server/database.
- Browser request/retry checks pass in en/ms/zh/ja/vi and light/dark themes. Temporary
  desktop/mobile screenshots were visually inspected; no permanent screenshots or
  bearer URLs are retained. No page exceptions occurred in the main journeys;
  negative-path HTTP failures and aborted requests are intentional test stimuli.
- Root and Web typechecks, lint, PGlite `demo`, API build, Demo build, generated
  i18n checks and diff checks pass. Existing Vite classic-script/chunk warnings
  remain. `POSTGRES_URL` was removed for the Demo proof so it used only PGlite.

## Remaining evidence and limits

- The full repository Vitest suite, production PostgreSQL/FORCE-RLS recovery path,
  current-revision remote CI, actual SMTP delivery and physical-device checks were
  not run in this task. The six-file suite is targeted regression evidence only.
- The shared request limiter remains an identifier-plus-IP policy; this work does
  not establish distributed abuse protection or a production capacity budget.
- Browser refresh intentionally discards the in-memory token; reopening the email
  link is required. Ambiguous cross-Master emails and Employee-linked accounts
  remain ineligible for this public recovery command.
- Platform Superadmin recovery needs its own approved control-plane design and
  implementation, token/session/audit tests and operator-recovery acceptance.
  Tenant token storage or tenant-role authority must not be repurposed for it.
- Production SMTP, failure alerts and an authorized delivery drill remain required
  before closing TASK-193. Repository counts are unchanged.
