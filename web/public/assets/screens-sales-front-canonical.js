/* ============================================================
   ARIA ERP — canonical sales enquiry / quotation front chain
   Formal ErpSystemData resources only; no data-*.js dependency.
   ============================================================ */
(function canonicalSalesFrontScreens(){
  function copy(){

    const packs={
      en:{
        enquiries:'Enquiries',quotations:'Quotations',newEnquiry:'New enquiry',
        newQuotation:'New quotation',autoGenerated:'Auto-generated',assignedOnCreate:'Number assigned on create',recent:'Recently used',selectCustomer:'Search or select a customer',selectCustomerHelp:'Select a customer to continue.',subjectPlaceholder:'e.g. Office equipment annual quotation',estimatedHelp:'Optional, enter the expected transaction amount',ownerHelp:'You can change the owner',subject:'Subject',channel:'Channel',customer:'Customer',newCustomer:'Create new customer',customerName:'Customer name',customerCode:'Customer code',industry:'Industry',customerNamePlaceholder:'e.g. Acme Pte Ltd',customerCodePlaceholder:'Optional — generated automatically',customerCodeHelp:'Optional. Leave blank to generate automatically.',noCustomers:'No customers yet',noCustomersHelp:'Create a customer to continue with this enquiry.',customerCreated:'Customer created',saveEnquiry:'Save enquiry',saveOpenDetails:'Save and open details',
        owner:'Owner',date:'Date',estimated:'Estimated value',status:'Status',
        quoteDate:'Quote date',validUntil:'Valid until',probability:'Win probability',
        total:'Total',lines:'Quoted items',product:'Product',quantity:'Quantity',
        unitPrice:'Unit price',tax:'Tax',net:'Net',nonStock:'Non-stock',create:'Create',cancel:'Cancel',
        addRow:'Add row',addStockLine:'Add stock item',addNonStockLine:'Add non-stock line',lineDescription:'Description',uom:'UOM',removeLine:'Remove line',noLines:'No quotation lines yet.',noLinesHelp:'Add a stock item or free-text service.',selectProduct:'Select a product',pricingNote:'Tax and totals are recalculated by the server.',lineTypeStock:'Stock',lineTypeNonStock:'Non-stock',lineAmount:'Line amount',taxCode:'Tax code',customerRequired:'Select a customer.',lineRequired:'Complete every line before creating the quotation.',createQuotation:'Create quotation',
        convert:'Convert to quotation',issue:'Issue quotation',accept:'Accept quotation',
        toOrder:'Convert to sales order',created:'Record created',converted:'Enquiry converted',
        issued:'Quotation issued',accepted:'Quotation accepted',orderCreated:'Draft sales order created',
        emptyEnquiry:'No canonical enquiries yet.',emptyQuotation:'No canonical quotations yet.',
        emptyHelp:'Create a record to start the customer sales chain.',
        all:'All',newStatus:'New',quoted:'Quoted',lost:'Lost',draft:'Draft',sent:'Sent',
        acceptedStatus:'Accepted',convertedStatus:'Converted',rejected:'Rejected',expired:'Expired',
        details:'Quotation details',currency:'Currency',dataLimit:'Showing the first 100 canonical records.',
        enquiryHelp:'Capture a customer request before pricing and formal quotation.',
        quotationHelp:'Formal offers with tax snapshots, validity and controlled conversion to an order.',
        orderNo:'Sales order number',quotationNo:'Quotation number',enquiryNo:'Enquiry number',
        direct:'Direct',email:'Email',phone:'Phone',web:'Web',back:'Back',totalEnquiries:'Total enquiries',newEnquiries:'New',quotedEnquiries:'Quoted',pipelineValue:'Pipeline value',filter:'Filter',export:'Export',createFailed:'Create failed',conversionItemsRequired:'Add and save enquiry items before conversion.',quotationItemsRequired:'Complete every quotation item.',conversionFailed:'Conversion failed',actionFailed:'Action failed',
      },
      ms:{
  "enquiries": "Pertanyaan",
  "quotations": "Sebut harga",
  "newEnquiry": "Pertanyaan baharu",
  "newQuotation": "Sebut harga baharu",
  "autoGenerated": "Dijana automatik",
  "recent": "Baru-baru ini digunakan",
  "selectCustomer": "Cari atau pilih pelanggan",
  "selectCustomerHelp": "Pilih pelanggan untuk meneruskan.",
  "subjectPlaceholder": "cth. Sebut harga peralatan pejabat tahunan",
  "estimatedHelp": "Pilihan, masukkan amaun transaksi dijangka",
  "ownerHelp": "Pemilik boleh ditukar",
  "subject": "Subjek",
  "channel": "Saluran",
  "customer": "Pelanggan",
  "newCustomer": "Cipta pelanggan baharu",
  "customerName": "Nama pelanggan",
  "customerCode": "Kod pelanggan",
  "industry": "Industri",
  "customerNamePlaceholder": "cth. Acme Sdn Bhd",
  "customerCodePlaceholder": "Pilihan — dijana automatik",
  "customerCodeHelp": "Pilihan. Biarkan kosong untuk menjana kod secara automatik.",
  "noCustomers": "Belum ada pelanggan",
  "noCustomersHelp": "Cipta pelanggan untuk meneruskan pertanyaan ini.",
  "customerCreated": "Pelanggan dicipta",
  "assignedOnCreate": "Nombor diberikan semasa disimpan",
  "saveEnquiry": "Simpan pertanyaan",
  "saveOpenDetails": "Simpan dan buka butiran",
  "owner": "Pemilik",
  "date": "Tarikh",
  "estimated": "Nilai anggaran",
  "status": "Status",
  "quoteDate": "Tarikh sebut harga",
  "validUntil": "Sah sehingga",
  "probability": "Kebarangkalian menang",
  "total": "Jumlah",
  "lines": "Item sebut harga",
  "product": "Produk",
  "quantity": "Kuantiti",
  "unitPrice": "Harga unit",
  "tax": "Cukai",
  "net": "Bersih",
  "nonStock": "Bukan stok",
  "addRow": "Tambah baris",
  "addStockLine": "Tambah item stok",
  "addNonStockLine": "Tambah baris bukan stok",
  "lineDescription": "Penerangan",
  "uom": "UOM",
  "removeLine": "Buang baris",
  "noLines": "Belum ada baris sebut harga.",
  "noLinesHelp": "Tambah item stok atau perkhidmatan teks bebas.",
  "selectProduct": "Pilih produk",
  "pricingNote": "Cukai dan jumlah akan dikira semula oleh pelayan.",
  "lineTypeStock": "Stok",
  "lineTypeNonStock": "Bukan stok",
  "lineAmount": "Amaun baris",
  "taxCode": "Kod cukai",
  "customerRequired": "Pilih pelanggan.",
  "lineRequired": "Lengkapkan setiap baris sebelum mencipta sebut harga.",
  "createQuotation": "Cipta sebut harga",
  "create": "Cipta",
  "cancel": "Batal",
  "convert": "Tukar kepada sebut harga",
  "issue": "Keluarkan sebut harga",
  "accept": "Terima sebut harga",
  "toOrder": "Tukar kepada pesanan jualan",
  "created": "Rekod dicipta",
  "converted": "Pertanyaan ditukar",
  "issued": "Sebut harga dikeluarkan",
  "accepted": "Sebut harga diterima",
  "orderCreated": "Draf pesanan jualan dicipta",
  "emptyEnquiry": "Belum ada pertanyaan kanonik.",
  "emptyQuotation": "Belum ada sebut harga kanonik.",
  "emptyHelp": "Cipta rekod untuk memulakan rantaian jualan pelanggan.",
  "all": "Semua",
  "newStatus": "Baharu",
  "quoted": "Telah disebut",
  "lost": "Hilang",
  "draft": "Draf",
  "sent": "Dihantar",
  "acceptedStatus": "Diterima",
  "convertedStatus": "Ditukar",
  "rejected": "Ditolak",
  "expired": "Tamat tempoh",
  "details": "Butiran sebut harga",
  "currency": "Mata wang",
  "dataLimit": "Memaparkan 100 rekod kanonik pertama.",
  "enquiryHelp": "Rekod permintaan pelanggan sebelum harga dan sebut harga rasmi.",
  "quotationHelp": "Tawaran rasmi dengan petikan cukai, tempoh sah dan penukaran pesanan terkawal.",
  "orderNo": "Nombor pesanan jualan",
  "quotationNo": "Nombor sebut harga",
  "enquiryNo": "Nombor pertanyaan",
  "direct": "Terus",
  "email": "E-mel",
  "phone": "Telefon",
  "web": "Web",
  "back": "Kembali",
  "totalEnquiries": "Jumlah pertanyaan",
  "newEnquiries": "Baharu",
  "quotedEnquiries": "Telah disebut",
  "pipelineValue": "Nilai saluran",
  "filter": "Tapis",
  "export": "Eksport",
  "createFailed": "Gagal mencipta",
  "conversionItemsRequired": "Tambah dan simpan item pertanyaan sebelum penukaran.",
  "quotationItemsRequired": "Lengkapkan setiap item sebut harga.",
  "conversionFailed": "Gagal menukar",
  "actionFailed": "Tindakan gagal"
},
      zh:{
        enquiries:'询价',quotations:'报价单',newEnquiry:'新建询价',
        newQuotation:'新建报价单',autoGenerated:'自动生成',recent:'最近使用',selectCustomer:'搜索或选择客户',selectCustomerHelp:'请选择客户以继续。',subjectPlaceholder:'例如：办公室设备年度报价',estimatedHelp:'可选，填写预计交易金额',ownerHelp:'可更改负责人',subject:'主题',channel:'渠道',customer:'客户',
        owner:'负责人',date:'日期',estimated:'预计金额',status:'状态',
        quoteDate:'报价日期',validUntil:'有效期至',probability:'赢单概率',
        total:'总额',lines:'报价项目',product:'产品',quantity:'数量',
        unitPrice:'单价',tax:'税额',net:'未税金额',nonStock:'非库存',create:'创建',cancel:'取消',
        addRow:'添加行',addStockLine:'添加库存商品',addNonStockLine:'添加非库存行',lineDescription:'描述',uom:'单位',removeLine:'删除行',noLines:'暂无报价明细。',noLinesHelp:'添加库存商品或自由文本服务。',selectProduct:'选择产品',pricingNote:'税额和合计将由服务器重新计算。',lineTypeStock:'库存',lineTypeNonStock:'非库存',lineAmount:'行金额',taxCode:'税码',customerRequired:'请选择客户。',lineRequired:'创建报价单前请完整填写每一行。',createQuotation:'创建报价单',
        convert:'转为报价单',issue:'发出报价单',accept:'确认客户接受',
        toOrder:'转为销售订单',created:'记录已创建',converted:'询价已转为报价单',
        issued:'报价单已发出',accepted:'报价单已接受',orderCreated:'销售订单草稿已创建',
        emptyEnquiry:'目前没有标准询价。',emptyQuotation:'目前没有标准报价单。',
        emptyHelp:'创建记录以开始客户销售流程。',
        all:'全部',newStatus:'新建',quoted:'已报价',lost:'已丢失',draft:'草稿',sent:'已发出',
        acceptedStatus:'已接受',convertedStatus:'已转订单',rejected:'已拒绝',expired:'已过期',
        details:'报价详情',currency:'币种',dataLimit:'显示前 100 条标准记录。',
        enquiryHelp:'在定价和正式报价前记录客户需求。',
        quotationHelp:'包含税率快照、有效期及受控订单转换的正式报价。',
        orderNo:'销售订单编号',quotationNo:'报价单编号',enquiryNo:'询价编号',
        direct:'直接',email:'电邮',phone:'电话',web:'网站',back:'返回',totalEnquiries:'询价总数',newEnquiries:'新建',quotedEnquiries:'已报价',pipelineValue:'预计总额',filter:'筛选',export:'导出',newCustomer:'新建客户',customerName:'客户名称',customerCode:'客户编码',industry:'行业',customerNamePlaceholder:'例如：Acme 公司',customerCodePlaceholder:'可选，留空自动生成',customerCodeHelp:'可选。留空将自动生成客户编码。',noCustomers:'暂无客户',noCustomersHelp:'请先创建客户以继续新建询价。',customerCreated:'客户已创建',assignedOnCreate:'保存成功后分配单号',saveEnquiry:'保存询价',saveOpenDetails:'保存并打开详情',createFailed:'创建失败',conversionItemsRequired:'请先添加并保存询价明细。',quotationItemsRequired:'请完整填写每个报价项目。',conversionFailed:'转换失败',actionFailed:'操作失败',
      },
      ja:{
  "enquiries": "引合",
  "quotations": "見積書",
  "newEnquiry": "引合を作成",
  "newQuotation": "見積書を作成",
  "autoGenerated": "自動生成",
  "recent": "最近使用",
  "selectCustomer": "顧客を検索または選択",
  "selectCustomerHelp": "顧客を選択して続行してください。",
  "subjectPlaceholder": "例：オフィス設備の年間見積もり",
  "estimatedHelp": "任意。予想取引金額を入力",
  "ownerHelp": "担当者を変更できます",
  "subject": "件名",
  "channel": "チャネル",
  "customer": "顧客",
  "owner": "担当者",
  "date": "日付",
  "estimated": "見込金額",
  "status": "ステータス",
  "quoteDate": "見積日",
  "validUntil": "有効期限",
  "probability": "受注確度",
  "total": "合計",
  "lines": "見積明細",
  "product": "製品",
  "quantity": "数量",
  "unitPrice": "単価",
  "tax": "税額",
  "net": "税抜額",
  "nonStock": "非在庫",
  "addRow": "行を追加",
  "addStockLine": "在庫品目を追加",
  "addNonStockLine": "非在庫行を追加",
  "lineDescription": "説明",
  "uom": "単位",
  "removeLine": "行を削除",
  "noLines": "見積明細はまだありません。",
  "noLinesHelp": "在庫品目または自由入力サービスを追加してください。",
  "selectProduct": "商品を選択",
  "pricingNote": "税額と合計はサーバーで再計算されます。",
  "lineTypeStock": "在庫",
  "lineTypeNonStock": "非在庫",
  "lineAmount": "行金額",
  "taxCode": "税コード",
  "customerRequired": "顧客を選択してください。",
  "lineRequired": "見積書を作成する前に全ての行を入力してください。",
  "createQuotation": "見積書を作成",
  "create": "作成",
  "cancel": "キャンセル",
  "convert": "見積書へ変換",
  "issue": "見積書を発行",
  "accept": "受注承認",
  "toOrder": "受注へ変換",
  "created": "レコードを作成しました",
  "converted": "引合を見積書へ変換しました",
  "issued": "見積書を発行しました",
  "accepted": "見積書を承認しました",
  "orderCreated": "受注ドラフトを作成しました",
  "emptyEnquiry": "標準引合はありません。",
  "emptyQuotation": "標準見積書はありません。",
  "emptyHelp": "レコードを作成して販売フローを開始します。",
  "all": "すべて",
  "newStatus": "新規",
  "quoted": "見積済",
  "lost": "失注",
  "draft": "ドラフト",
  "sent": "送付済",
  "acceptedStatus": "承認済",
  "convertedStatus": "受注変換済",
  "rejected": "却下",
  "expired": "期限切れ",
  "details": "見積詳細",
  "currency": "通貨",
  "dataLimit": "最初の100件の標準レコードを表示しています。",
  "enquiryHelp": "価格設定と正式見積の前に顧客要求を記録します。",
  "quotationHelp": "税率スナップショット、有効期限、受注変換管理を持つ正式提案です。",
  "orderNo": "受注番号",
  "quotationNo": "見積番号",
  "enquiryNo": "引合番号",
  "direct": "直接",
  "email": "メール",
  "phone": "電話",
  "web": "ウェブ",
  "back": "戻る",
  "totalEnquiries": "引合総数",
  "newEnquiries": "新規",
  "quotedEnquiries": "見積済",
  "pipelineValue": "見込総額",
  "filter": "フィルター",
  "export": "エクスポート",
  "newCustomer": "新しい顧客を作成",
  "customerName": "顧客名",
  "customerCode": "顧客コード",
  "industry": "業種",
  "customerNamePlaceholder": "例：Acme株式会社",
  "customerCodePlaceholder": "任意 — 自動生成",
  "customerCodeHelp": "任意。空欄の場合は自動生成します。",
  "noCustomers": "顧客がまだありません",
  "noCustomersHelp": "この引合を続けるには顧客を作成してください。",
  "customerCreated": "顧客を作成しました",
  "assignedOnCreate": "保存時に番号を割り当て",
  "saveEnquiry": "引合を保存",
  "saveOpenDetails": "保存して詳細を開く",
  "createFailed": "作成に失敗しました",
  "conversionItemsRequired": "変換前に引合明細を追加して保存してください。",
  "quotationItemsRequired": "すべての見積明細を入力してください。",
  "conversionFailed": "変換に失敗しました",
  "actionFailed": "操作に失敗しました"
},
      vi:{
  "enquiries": "Yêu cầu báo giá",
  "quotations": "Báo giá",
  "newEnquiry": "Tạo yêu cầu",
  "newQuotation": "Tạo báo giá",
  "autoGenerated": "Tự động tạo",
  "recent": "Dùng gần đây",
  "selectCustomer": "Tìm kiếm hoặc chọn khách hàng",
  "selectCustomerHelp": "Vui lòng chọn khách hàng để tiếp tục.",
  "subjectPlaceholder": "vd. báo giá thiết bị văn phòng hằng năm",
  "estimatedHelp": "Tùy chọn, nhập số tiền giao dịch dự kiến",
  "ownerHelp": "Bạn có thể thay đổi người phụ trách",
  "subject": "Chủ đề",
  "channel": "Kênh",
  "customer": "Khách hàng",
  "owner": "Phụ trách",
  "date": "Ngày",
  "estimated": "Giá trị ước tính",
  "status": "Trạng thái",
  "quoteDate": "Ngày báo giá",
  "validUntil": "Hiệu lực đến",
  "probability": "Xác suất thắng",
  "total": "Tổng cộng",
  "lines": "Dòng báo giá",
  "product": "Sản phẩm",
  "quantity": "Số lượng",
  "unitPrice": "Đơn giá",
  "tax": "Thuế",
  "net": "Trước thuế",
  "nonStock": "Không tồn kho",
  "addRow": "Thêm dòng",
  "addStockLine": "Thêm mặt hàng tồn kho",
  "addNonStockLine": "Thêm dòng không tồn kho",
  "lineDescription": "Mô tả",
  "uom": "ĐVT",
  "removeLine": "Xóa dòng",
  "noLines": "Chưa có dòng báo giá.",
  "noLinesHelp": "Thêm mặt hàng tồn kho hoặc dịch vụ nhập tự do.",
  "selectProduct": "Chọn sản phẩm",
  "pricingNote": "Thuế và tổng tiền sẽ được máy chủ tính lại.",
  "lineTypeStock": "Tồn kho",
  "lineTypeNonStock": "Không tồn kho",
  "lineAmount": "Thành tiền dòng",
  "taxCode": "Mã thuế",
  "customerRequired": "Hãy chọn khách hàng.",
  "lineRequired": "Hoàn tất mọi dòng trước khi tạo báo giá.",
  "createQuotation": "Tạo báo giá",
  "create": "Tạo",
  "cancel": "Hủy",
  "convert": "Chuyển thành báo giá",
  "issue": "Phát hành báo giá",
  "accept": "Chấp nhận báo giá",
  "toOrder": "Chuyển thành đơn bán hàng",
  "created": "Đã tạo bản ghi",
  "converted": "Đã chuyển yêu cầu thành báo giá",
  "issued": "Đã phát hành báo giá",
  "accepted": "Đã chấp nhận báo giá",
  "orderCreated": "Đã tạo đơn bán hàng nháp",
  "emptyEnquiry": "Chưa có yêu cầu chuẩn.",
  "emptyQuotation": "Chưa có báo giá chuẩn.",
  "emptyHelp": "Tạo bản ghi để bắt đầu quy trình bán hàng.",
  "all": "Tất cả",
  "newStatus": "Mới",
  "quoted": "Đã báo giá",
  "lost": "Thất bại",
  "draft": "Nháp",
  "sent": "Đã gửi",
  "acceptedStatus": "Đã chấp nhận",
  "convertedStatus": "Đã chuyển đổi",
  "rejected": "Từ chối",
  "expired": "Hết hạn",
  "details": "Chi tiết báo giá",
  "currency": "Tiền tệ",
  "dataLimit": "Hiển thị 100 bản ghi chuẩn đầu tiên.",
  "enquiryHelp": "Ghi nhận yêu cầu khách hàng trước khi định giá và báo giá chính thức.",
  "quotationHelp": "Đề nghị chính thức với thuế chụp tại thời điểm tạo, hiệu lực và chuyển đổi có kiểm soát.",
  "orderNo": "Số đơn bán hàng",
  "quotationNo": "Số báo giá",
  "enquiryNo": "Số yêu cầu",
  "direct": "Trực tiếp",
  "email": "E-mail",
  "phone": "Điện thoại",
  "web": "Web",
  "back": "Quay lại",
  "totalEnquiries": "Tổng yêu cầu",
  "newEnquiries": "Mới",
  "quotedEnquiries": "Đã báo giá",
  "pipelineValue": "Giá trị tiềm năng",
  "filter": "Lọc",
  "export": "Xuất",
  "newCustomer": "Tạo khách hàng mới",
  "customerName": "Tên khách hàng",
  "customerCode": "Mã khách hàng",
  "industry": "Ngành nghề",
  "customerNamePlaceholder": "vd. Công ty Acme",
  "customerCodePlaceholder": "Tùy chọn — tự động tạo",
  "customerCodeHelp": "Tùy chọn. Để trống để tự động tạo mã.",
  "noCustomers": "Chưa có khách hàng",
  "noCustomersHelp": "Tạo khách hàng để tiếp tục yêu cầu này.",
  "customerCreated": "Đã tạo khách hàng",
  "assignedOnCreate": "Cấp số khi lưu thành công",
  "saveEnquiry": "Lưu yêu cầu",
  "saveOpenDetails": "Lưu và mở chi tiết",
  "createFailed": "Tạo thất bại",
  "conversionItemsRequired": "Hãy thêm và lưu các mặt hàng yêu cầu trước khi chuyển đổi.",
  "quotationItemsRequired": "Hãy hoàn tất mọi dòng báo giá.",
  "conversionFailed": "Chuyển đổi thất bại",
  "actionFailed": "Thao tác thất bại"
},
    };
    const pack=i18nLegacy(packs);
    return key=>pack[key]||packs.en[key]||key;
  }

  function adapter(){
    if(!window.ErpSystemData) throw new Error('ERP data adapter is unavailable.');
    return window.ErpSystemData;
  }
  function byId(rows){ return new Map((rows||[]).map(row=>[Number(row.id),row])); }
  function today(){ return new Date().toISOString().slice(0,10); }
  function plusDays(days){
    return new Date(Date.now()+days*86400000).toISOString().slice(0,10);
  }
  function sequence(prefix){
    return `${prefix}-${Date.now().toString().slice(-7)}`;
  }
  function requestKey(){
    return globalThis.crypto&&typeof globalThis.crypto.randomUUID==='function'
      ?globalThis.crypto.randomUUID():`${Date.now()}-${Math.random().toString(36).slice(2)}`;
  }
  function currency(){
    return (DB.company&&DB.company.currency)||'SGD';
  }
  function amountLabel(value,code){
    const n=Number(value||0);
    try{
      return new Intl.NumberFormat(undefined,{style:'currency',currency:code||currency()}).format(n);
    }catch{ return `${code||currency()} ${n.toFixed(2)}`; }
  }
  function statusLabel(s,status){
    return ({
      new:s('newStatus'),quoted:s('quoted'),lost:s('lost'),draft:s('draft'),sent:s('sent'),
      accepted:s('acceptedStatus'),converted:s('convertedStatus'),rejected:s('rejected'),
      expired:s('expired'),
    })[status]||status;
  }
  function statusTone(status){
    return ({
      new:'info',quoted:'accent',lost:'danger',draft:'neutral',sent:'info',
      accepted:'ok',converted:'accent',rejected:'danger',expired:'warn',
    })[status]||'neutral';
  }
  function openQuote(id){
    window.ACTIVE_SALES_QUOTATION_ID=Number(id);
    navigate('quotation');
  }
  function openEnquiry(id){
    window.ACTIVE_SALES_ENQUIRY_ID=Number(id);
    return navigate('txn-view');
  }
  window.openSalesEnquiry=openEnquiry;

  async function loadFrontData(){
    const a=adapter();
    const pages=await Promise.all([
      a.list('sales/enquiries',{limit:100}),
      a.list('sales/quotations',{limit:100}),
      a.list('sales/quotation-lines',{limit:100}),
      a.list('sales/customers',{limit:100}),
      a.list('inventory/products',{limit:100}),
    ]);
    return {
      enquiries:pages[0].data||[],
      quotations:pages[1].data||[],
      lines:pages[2].data||[],
      customers:pages[3].data||[],
      products:pages[4].data||[],
    };
  }

  function openNewEnquiry(data,onDone,initial={}){
    const s=copy();
    let customers=Array.isArray(data.customers)?data.customers.slice():[];
    const channelOptions=`<option value="direct">${esc(s('direct'))}</option>
      <option value="email">${esc(s('email'))}</option><option value="phone">${esc(s('phone'))}</option>
      <option value="web">${esc(s('web'))}</option>`;
    openModal(`
      <div class="sales-enquiry-modal-head">
        <button type="button" class="sales-enquiry-close" data-enquiry-close aria-label="${esc(s('cancel'))}">${ic('x')}</button>
        <div class="sales-enquiry-titleblock">
          <h2>${esc(s('newEnquiry'))}</h2>
          <div class="sales-enquiry-docmeta"><span>${ic('file')}${esc(s('enquiryNo'))}</span><b aria-hidden="true">·</b><span>${esc(s('assignedOnCreate'))}</span></div>
        </div>
      </div>
      <div class="sales-enquiry-modal-scroll">
        <div class="sales-enquiry-card">
          <section class="sales-enquiry-section">
            <div class="sales-enquiry-label-row">
              <label for="salesEnquiryCustomer">${esc(s('customer'))} <i aria-hidden="true">*</i></label>
              <button type="button" class="sales-enquiry-new-customer" data-enquiry-add-customer aria-label="${esc(s('newCustomer'))}" title="${esc(s('newCustomer'))}">${ic('plus')}<span>${esc(s('newCustomer'))}</span></button>
            </div>
            <div data-enquiry-customer-picker></div>
          </section>

          <section class="sales-enquiry-section">
            <div class="sales-enquiry-label-row"><label for="salesEnquirySubject">${esc(s('subject'))} <i aria-hidden="true">*</i></label><span class="sales-enquiry-counter" data-enquiry-subject-count>0 / 100</span></div>
            <div class="sales-enquiry-control"><input id="salesEnquirySubject" maxlength="100" value="${esc(initial.subject||'')}" placeholder="${esc(s('subjectPlaceholder'))}" required></div>
          </section>

          <section class="sales-enquiry-section">
            <div class="sales-enquiry-label-row"><label for="salesEnquiryDate">${esc(s('date'))} <i aria-hidden="true">*</i></label></div>
            <div class="sales-enquiry-control sales-enquiry-control-select sales-enquiry-date-control">
              <span class="sales-enquiry-control-icon">${ic('calendar')}</span>
              <input id="salesEnquiryDate" type="date" value="${esc(initial.enquiryDate||today())}" required>
              <span class="sales-enquiry-control-chevron">${ic('chevR')}</span>
            </div>
          </section>

          <section class="sales-enquiry-section">
            <div class="sales-enquiry-label-row"><label for="salesEnquiryChannel">${esc(s('channel'))}</label></div>
            <div class="sales-enquiry-control sales-enquiry-control-select">
              <span class="sales-enquiry-control-icon">${ic('inbox')}</span>
              <select id="salesEnquiryChannel">${channelOptions.replace(`value="${esc(initial.channel||'direct')}"`,`value="${esc(initial.channel||'direct')}" selected`)}</select>
              <span class="sales-enquiry-control-chevron">${ic('chevR')}</span>
            </div>
          </section>

          <section class="sales-enquiry-section">
            <div class="sales-enquiry-label-row"><label for="salesEnquiryValue">${esc(s('estimated'))}</label></div>
            <div class="sales-enquiry-amount-control">
              <span class="sales-enquiry-currency">${esc(currency())}${ic('chevD')}</span>
              <input id="salesEnquiryValue" type="number" min="0" step="0.01" inputmode="decimal" value="${esc(initial.estimatedValue==null?'0':initial.estimatedValue)}" aria-label="${esc(s('estimated'))}">
            </div>
            <p class="sales-enquiry-help">${esc(s('estimatedHelp'))}</p>
          </section>

          <section class="sales-enquiry-section">
            <div class="sales-enquiry-label-row"><label for="salesEnquiryOwner">${esc(s('owner'))}</label></div>
            <div class="sales-enquiry-control sales-enquiry-control-select">
              <span class="sales-enquiry-control-icon">${ic('user')}</span>
              <input id="salesEnquiryOwner" value="${esc(initial.ownerName||'Demo Sales')}" aria-describedby="salesEnquiryOwnerHelp">
              <span class="sales-enquiry-control-chevron">${ic('chevR')}</span>
            </div>
            <p class="sales-enquiry-help" id="salesEnquiryOwnerHelp">${esc(s('ownerHelp'))}</p>
          </section>
        </div>
      </div>
      <div class="sales-enquiry-modal-foot">
        <button type="button" class="btn soft sales-enquiry-cancel" data-enquiry-cancel>${esc(s('cancel'))}</button>
        <button type="button" class="btn soft sales-enquiry-save" data-enquiry-save>${esc(s('saveEnquiry'))}</button>
        <button type="button" class="btn primary sales-enquiry-create" data-enquiry-save-open>${ic('plus')}<span>${esc(s('saveOpenDetails'))}</span></button>
      </div>
    `);
    const modal=document.querySelector('#modalEl');
    modal?.classList.add('sales-enquiry-modal');
    const query=selector=>modal?.querySelector(selector);
    const customerPicker=query('[data-enquiry-customer-picker]');
    let customer=null;
    let customerHelp=null;
    const setInvalid=(input,invalid)=>{
      if(!input) return;
      input.classList.toggle('is-invalid',invalid);
      input.closest('.sales-enquiry-control,.sales-enquiry-amount-control')?.classList.toggle('is-invalid',invalid);
    };
    const setCustomerInvalid=invalid=>{
      setInvalid(customer,invalid);
      customerPicker?.classList.toggle('is-invalid',invalid);
    };
    const readDraft=()=>({
      customerId:Number(customer?.dataset.value||0),
      subject:query('#salesEnquirySubject')?.value||'',
      enquiryDate:query('#salesEnquiryDate')?.value||today(),
      channel:query('#salesEnquiryChannel')?.value||'direct',
      estimatedValue:query('#salesEnquiryValue')?.value||'0',
      ownerName:query('#salesEnquiryOwner')?.value||'Demo Sales',
    });
    const openNewCustomer=()=>{
      const draft=readDraft();
      const resume=()=>{closeModal();openNewEnquiry(data,onDone,draft);};
      appModal({
        icon:'user',title:s('newCustomer'),width:520,
        body:`<div class="fld"><span>${esc(s('customerName'))} *</span><input id="quickCustomerName" maxlength="200" placeholder="${esc(s('customerNamePlaceholder'))}" autofocus></div>
          <div class="fld"><span>${esc(s('customerCode'))}</span><input id="quickCustomerCode" maxlength="40" placeholder="${esc(s('customerCodePlaceholder'))}"><small class="hint">${esc(s('customerCodeHelp'))}</small></div>
          <div class="fld"><span>${esc(s('industry'))}</span><input id="quickCustomerIndustry" maxlength="120"></div>`,
        actions:`${btn(s('cancel'),{cls:'soft',attrs:'data-quick-customer-cancel'})}${btn(s('create'),{icon:'plus',cls:'primary',attrs:'data-quick-customer-create'})}`,
      });
      const quickModal=document.querySelector('#modalEl');
      const nameInput=quickModal?.querySelector('#quickCustomerName');
      const createButton=quickModal?.querySelector('[data-quick-customer-create]');
      quickModal?.querySelector('[data-quick-customer-cancel]')?.addEventListener('click',resume);
      quickModal?.querySelector('.modal-head .iconbtn.x')?.addEventListener('click',resume);
      document.querySelector('#modalScrim')?.addEventListener('click',resume);
      createButton?.addEventListener('click',async()=>{
        const name=nameInput?.value.trim()||'';
        if(!name){ nameInput?.classList.add('is-invalid'); nameInput?.focus(); return; }
        createButton.disabled=true;
        try{
          const result=await adapter().create('sales/customers',{
            name,
            code:quickModal?.querySelector('#quickCustomerCode')?.value.trim()||undefined,
            industry:quickModal?.querySelector('#quickCustomerIndustry')?.value.trim()||undefined,
          },requestKey());
          const created=result?.data;
          if(!created?.id) throw new Error('Customer was created without an id.');
          data.customers=[...(data.customers||[]),created];
          closeModal();
          toast(s('customerCreated'),'ok');
          openNewEnquiry(data,onDone,{...draft,customerId:Number(created.id)});
        }catch(error){
          createButton.disabled=false;
          toast(error&&error.message||s('createFailed'),'danger');
        }
      });
    };
    const renderCustomerPicker=selectedId=>{
      const selected=Number(selectedId||0);
      const options=customers.map(row=>({
        value:String(row.id),label:String(row.name||row.code||`#${row.id}`),sub:String(row.code||''),
      }));
      if(customerPicker) customerPicker.innerHTML=customers.length
        ?`<div class="sales-enquiry-customer-combo">${combobox({
            id:'salesEnquiryCustomer',value:selected?String(selected):'',options,placeholder:s('selectCustomer'),
          })}</div><p class="sales-enquiry-help" id="salesEnquiryCustomerHelp">${esc(s('selectCustomerHelp'))}</p>`
        :`<div class="sales-enquiry-empty-customer">
            <div class="sales-enquiry-empty-customer-copy"><span class="sales-enquiry-empty-customer-icon">${ic('user')}</span><span><b>${esc(s('noCustomers'))}</b><small>${esc(s('noCustomersHelp'))}</small></span></div>
          </div><p class="sales-enquiry-help" id="salesEnquiryCustomerHelp">${esc(s('selectCustomerHelp'))}</p>`;
      customer=query('#salesEnquiryCustomer');
      customerHelp=query('#salesEnquiryCustomerHelp');
      if(customerHelp) customerHelp.hidden=Boolean(customer?.dataset.value);
      if(customer) wireCombobox('salesEnquiryCustomer',{options,onChange:()=>{
        if(customerHelp) customerHelp.hidden=Boolean(customer.dataset.value);
        setCustomerInvalid(false);
      }});
    };
    query('[data-enquiry-close]')?.addEventListener('click',closeModal);
    query('[data-enquiry-cancel]')?.addEventListener('click',closeModal);
    query('[data-enquiry-add-customer]')?.addEventListener('click',openNewCustomer);
    renderCustomerPicker(initial.customerId);
    const subjectInput=query('#salesEnquirySubject');
    const subjectCount=query('[data-enquiry-subject-count]');
    if(subjectCount&&subjectInput) subjectCount.textContent=`${subjectInput.value.length} / 100`;
    subjectInput?.addEventListener('input',()=>{
      if(subjectCount) subjectCount.textContent=`${subjectInput.value.length} / 100`;
      setInvalid(subjectInput,false);
    });
    query('#salesEnquiryDate')?.addEventListener('change',event=>setInvalid(event.currentTarget,false));
    let lastSubmission='';
    let idempotencyKey='';
    const submit=async openDetails=>{
      const subject=subjectInput?.value.trim()||'';
      const customerId=Number(customer?.dataset.value||0);
      const dateInput=query('#salesEnquiryDate');
      if(!customerId){
        setCustomerInvalid(true);
        if(customerHelp) customerHelp.hidden=false;
        customer?.focus();
        return;
      }
      if(!subject){ setInvalid(subjectInput,true); subjectInput?.focus(); return; }
      if(!dateInput?.value){ setInvalid(dateInput,true); dateInput?.focus(); return; }
      const payload={
        customerId,
        subject,
        channel:query('#salesEnquiryChannel')?.value||'direct',
        estimatedValue:query('#salesEnquiryValue')?.value||'0',
        currency:currency(),
        ownerName:query('#salesEnquiryOwner')?.value.trim()||'Demo Sales',
        enquiryDate:dateInput.value,
      };
      const signature=JSON.stringify(payload);
      if(signature!==lastSubmission){ lastSubmission=signature; idempotencyKey=requestKey(); }
      const submitButtons=[query('[data-enquiry-save]'),query('[data-enquiry-save-open]')].filter(Boolean);
      submitButtons.forEach(item=>{item.disabled=true;});
      try{
        const result=await adapter().create('sales/enquiries',payload,idempotencyKey);
        const created=result?.data;
        if(!created?.id) throw new Error('Enquiry was created without an id.');
        closeModal(); toast(s('created'),'ok');
        if(openDetails) openEnquiry(created.id); else onDone&&onDone(created);
      }catch(error){
        submitButtons.forEach(item=>{item.disabled=false;});
        toast(error&&error.message||s('createFailed'),'danger');
      }
    };
    query('[data-enquiry-save]')?.addEventListener('click',()=>submit(false));
    query('[data-enquiry-save-open]')?.addEventListener('click',()=>submit(true));
  }

  function openEnquiryConversion(enquiry,data){
    const s=copy();
    if(!(data.lines||[]).length){toast(s('conversionItemsRequired'),'warn');return;}
    const byProduct=new Map(data.products.map(row=>[Number(row.id),row]));
    const sourceLines=(data.lines||[]).map(row=>({
      lineType:row.lineType||'stock',productId:row.productId==null?null:Number(row.productId),description:String(row.description||''),uom:String(row.uom||'unit'),qty:String(row.qty),unitPrice:String(row.estimatedUnitPrice),
    }));
    const defaultTax=DB.company&&DB.company.country==='MY'?'SV':'SR';
    const lineRows=sourceLines.map((line,index)=>{const product=byProduct.get(line.productId)||{},nonStock=line.lineType==='non_stock';return `<tr data-convert-line="${index}"><td class="lineno">${index+1}</td><td class="l" data-label="${esc(s('product'))}"><b>${esc(line.description||product.name||'#'+line.productId)}</b><small>${esc(nonStock?s('nonStock'):(product.sku||''))} · ${esc(line.uom)}</small></td><td data-label="${esc(s('quantity'))}"><input class="lineinput" data-convert-qty type="number" min="0.0001" step="0.0001" value="${esc(line.qty)}"></td><td data-label="${esc(s('unitPrice'))}"><input class="lineinput" data-convert-price type="number" min="0" step="0.0001" value="${esc(line.unitPrice)}"></td><td class="l" data-label="${esc(s('tax'))}"><input class="lineinput l" data-convert-tax maxlength="12" value="${esc(defaultTax)}"></td></tr>`;}).join('');
    appModal({
      icon:'receipt',title:`${s('convert')} · ${enquiry.docNo}`,width:780,
      body:`<div class="fldrow c2">
        <div class="fld"><span>${esc(s('quotationNo'))}</span><input id="salesConvertNo" value="${esc(sequence('Q'))}"></div>
        <div class="fld"><span>${esc(s('validUntil'))}</span><input id="salesConvertValid" type="date" value="${plusDays(30)}"></div>
      </div><div class="panel enquiry-conversion-lines" style="margin-top:14px"><div class="panel-h">${ic('box')}<h3>${esc(s('lines'))}</h3><span class="cap neutral" style="margin-left:auto">${sourceLines.length}</span></div><div class="tablewrap"><table class="lines"><thead><tr><th class="lineno">#</th><th class="l">${esc(s('product'))}</th><th>${esc(s('quantity'))}</th><th>${esc(s('unitPrice'))}</th><th class="l">${esc(s('tax'))}</th></tr></thead><tbody>${lineRows}</tbody></table></div></div>`,
      actions:btn(s('cancel'),{cls:'soft',attrs:'data-convert-cancel'})
        +btn(s('convert'),{icon:'receipt',cls:'primary',attrs:'data-convert-save'}),
    });
    document.querySelector('[data-convert-cancel]')?.addEventListener('click',closeModal);
    document.querySelector('[data-convert-save]')?.addEventListener('click',async event=>{
      const button=event.currentTarget; button.disabled=true;
      try{
        const lines=[...document.querySelectorAll('[data-convert-line]')].map(row=>({
          lineType:sourceLines[Number(row.dataset.convertLine)].lineType,
          productId:sourceLines[Number(row.dataset.convertLine)].productId,
          description:sourceLines[Number(row.dataset.convertLine)].description,
          uom:sourceLines[Number(row.dataset.convertLine)].uom,
          qty:row.querySelector('[data-convert-qty]').value,
          unitPrice:row.querySelector('[data-convert-price]').value,
          taxCode:row.querySelector('[data-convert-tax]').value.trim().toUpperCase(),
        }));
        if(lines.some(line=>Number(line.qty)<=0||Number(line.unitPrice)<0||!line.taxCode)){throw new Error(s('quotationItemsRequired'));}
        const result=await adapter().action('sales/enquiries',enquiry.id,'convert-to-quotation',{
          docNo:document.querySelector('#salesConvertNo').value.trim(),
          quoteDate:today(),validUntil:document.querySelector('#salesConvertValid').value,
          currency:enquiry.currency||currency(),probability:'50',
          lines,
        },`convert-sales-enquiry-${enquiry.id}`);
        closeModal(); toast(s('converted'),'ok'); openQuote(result.data.quotationId);
      }catch(error){ button.disabled=false; toast(error&&error.message||s('conversionFailed'),'danger'); }
    });
  }
  /* txn-view owns the record-specific read workspace, while this module owns
     the already-audited conversion command and its product-entry form. Share
     that one workflow rather than grow a second browser-side implementation. */
  window.openCanonicalEnquiryConversion=openEnquiryConversion;

  SCREENS['enquiries']=async function(root){
    const s=copy(),data=await loadFrontData();
    const customers=byId(data.customers);
    const totalValue=data.enquiries.reduce((sum,row)=>sum+Number(row.estimatedValue||0),0);
    transactionListPage(root,{
      module:'sales',route:'enquiries',title:s('enquiries'),description:s('enquiryHelp'),
      rows:data.enquiries,rowId:row=>row.id,
      filters:[['all',s('all')],['new',s('newStatus')],['quoted',s('quoted')],['lost',s('lost')]],
      filterFn:(row,status)=>row.status===status,
      kpis:[
        {label:s('totalEnquiries'),value:data.enquiries.length,filter:'all'},
        {label:s('newEnquiries'),value:data.enquiries.filter(row=>row.status==='new').length,filter:'new'},
        {label:s('quotedEnquiries'),value:data.enquiries.filter(row=>row.status==='quoted').length,filter:'quoted'},
        {label:s('pipelineValue'),value:amountLabel(totalValue,currency())},
      ],
      primaryAction:{label:s('newEnquiry'),icon:'plus',onClick:()=>openNewEnquiry(data,()=>navigate('enquiries'))},
      note:s('dataLimit'),
      columns:[
        {label:s('enquiries'),render:row=>`<div class="cellsub"><b class="docnum">${esc(row.docNo)}</b><small>${esc(dateValue(row.enquiryDate))}</small></div>`},
        {label:s('customer'),render:row=>esc((customers.get(Number(row.customerId))||{}).name||'#'+row.customerId)},
        {label:s('subject'),render:row=>esc(row.subject)},
        {label:s('channel'),render:row=>esc(row.channel)},
        {label:s('owner'),render:row=>esc(row.ownerName)},
        {label:s('estimated'),align:'r',render:row=>`<b class="tnum">${esc(amountLabel(row.estimatedValue,row.currency))}</b>`},
        {label:s('status'),render:row=>cap(statusLabel(s,row.status),statusTone(row.status))},
      ],
      rowAction:{
        label:row=>`${t('common.open')} ${row.docNo}`,
        run:row=>openEnquiry(row.id),
      },
      empty:{icon:'chat',title:s('emptyEnquiry'),description:s('emptyHelp')},
    });
  };

  SCREENS['quotations']=async function(root){
    const s=copy(),data=await loadFrontData(),customers=byId(data.customers);
    transactionListPage(root,{
      module:'sales',route:'quotations',title:s('quotations'),description:s('quotationHelp'),
      rows:data.quotations,rowId:row=>row.id,
      filters:[['all',s('all')],['draft',s('draft')],['sent',s('sent')],['accepted',s('acceptedStatus')],['converted',s('convertedStatus')]],
      filterFn:(row,status)=>row.status===status,
      primaryAction:{label:s('newQuotation'),icon:'plus',onClick:()=>navigate('new-quotation')},
      note:s('dataLimit'),
      columns:[
        {label:s('quotations'),render:row=>`<div class="cellsub"><b class="docnum">${esc(row.docNo)}</b><small>${esc(dateValue(row.quoteDate))}</small></div>`},
        {label:s('customer'),render:row=>esc((customers.get(Number(row.customerId))||{}).name||'#'+row.customerId)},
        {label:s('validUntil'),render:row=>esc(dateValue(row.validUntil))},
        {label:s('probability'),align:'r',render:row=>`<span class="tnum">${num(Number(row.probability))}%</span>`},
        {label:s('total'),align:'r',render:row=>`<b class="tnum">${esc(amountLabel(row.totalAmount,row.currency))}</b>`},
        {label:s('status'),render:row=>cap(statusLabel(s,row.status),statusTone(row.status))},
      ],
      rowAction:{
        label:row=>`${t('common.open')} ${row.docNo}`,
        run:row=>openQuote(row.id),
      },
      empty:{icon:'receipt',title:s('emptyQuotation'),description:s('emptyHelp')},
    });
  };

  SCREENS['quotation']=async function(root){
    const s=copy(),data=await loadFrontData();
    const id=Number(window.ACTIVE_SALES_QUOTATION_ID)||Number(data.quotations[0]?.id);
    const quotation=data.quotations.find(row=>Number(row.id)===id)||data.quotations[0];
    if(!quotation){
      root.innerHTML=`<div class="content full"><section class="master"><div class="statepanel empty">
        ${ic('receipt')}<h3>${esc(s('emptyQuotation'))}</h3><p>${esc(s('emptyHelp'))}</p></div></section></div>`;
      return;
    }
    window.ACTIVE_SALES_QUOTATION_ID=Number(quotation.id);
    const customer=byId(data.customers).get(Number(quotation.customerId))||{};
    const products=byId(data.products);
    const lines=data.lines.filter(row=>Number(row.quotationId)===Number(quotation.id))
      .sort((a,b)=>Number(a.lineNo)-Number(b.lineNo));
    const rows=lines.map((line,index)=>{
      const item=products.get(Number(line.productId))||{},nonStock=line.lineType==='non_stock';
      return `<tr><td class="lineno">${index+1}</td><td class="l li-name"><b>${esc(line.description||item.name||'#'+line.productId)}</b>
        <small>${esc(nonStock?s('nonStock'):(item.sku||''))} · ${esc(line.uom||item.uom||'')}</small></td><td class="tnum">${num(Number(line.qty))}</td>
        <td class="tnum">${esc(amountLabel(line.unitPrice,quotation.currency))}</td>
        <td class="tnum">${esc(amountLabel(line.netAmount,quotation.currency))}</td>
        <td class="tnum">${esc(amountLabel(line.taxAmount,quotation.currency))}</td></tr>`;
    }).join('');
    const actions=[
      quotation.status==='draft'?btn(s('issue'),{icon:'send',cls:'primary',attrs:'data-quote-action="issue"'}):'',
      quotation.status==='sent'?btn(s('accept'),{icon:'check',cls:'primary',attrs:'data-quote-action="accept"'}):'',
      quotation.status==='accepted'?btn(s('toOrder'),{icon:'bag',cls:'primary',attrs:'data-quote-action="convert-to-order"'}):'',
      quotation.status==='converted'&&quotation.orderId
        ?btn(s('toOrder'),{icon:'bag',cls:'primary',attrs:'data-view-order'}):'',
    ].join('');
    root.innerHTML=`<div class="content full"><section class="master"><div class="pagehead">
      ${crumbs([DB.company.name,{label:t('nav.sales'),route:'sales-home'},{label:s('quotations'),route:'quotations'},{cur:quotation.docNo}])}
      ${typeof salesNav==='function'?salesNav('quotations'):''}</div><div class="docwrap"><div class="docpage">
      <div class="dochead"><div class="dh-row1"><div><h1 class="dt">${ic('receipt')}${esc(s('details'))}
        <span class="dnum">${esc(quotation.docNo)}</span></h1>
        <div class="h1sub">${esc(customer.name||'#'+quotation.customerId)}</div></div>
        <div class="dactions">${cap(statusLabel(s,quotation.status),statusTone(quotation.status))}</div></div>
        <div class="docmeta">
          <div class="dm"><small>${esc(s('customer'))}</small><b>${esc(customer.name||'#'+quotation.customerId)}</b></div>
          <div class="dm"><small>${esc(s('quoteDate'))}</small><b>${esc(dateValue(quotation.quoteDate))}</b></div>
          <div class="dm"><small>${esc(s('validUntil'))}</small><b>${esc(dateValue(quotation.validUntil))}</b></div>
          <div class="dm"><small>${esc(s('probability'))}</small><b>${num(Number(quotation.probability))}%</b></div>
          <div class="dm"><small>${esc(s('currency'))}</small><b>${esc(quotation.currency)}</b></div>
        </div></div>
      <div class="doclayout"><div class="docmain"><div class="panel"><div class="panel-h"><h3>${esc(s('lines'))}</h3></div>
        <table class="lines"><thead><tr><th class="lineno">#</th><th class="l">${esc(s('product'))}</th>
          <th>${esc(s('quantity'))}</th><th>${esc(s('unitPrice'))}</th><th>${esc(s('net'))}</th><th>${esc(s('tax'))}</th>
        </tr></thead><tbody>${rows}</tbody></table></div></div>
        <aside class="summary"><div class="sumcard"><div class="sumrow"><span class="sk2">${esc(s('net'))}</span>
          <span class="sv tnum">${esc(amountLabel(quotation.netAmount,quotation.currency))}</span></div>
          <div class="sumrow"><span class="sk2">${esc(s('tax'))}</span><span class="sv tnum">${esc(amountLabel(quotation.taxAmount,quotation.currency))}</span></div>
          <div class="sumrow total"><span class="sk2">${esc(s('total'))}</span><span class="sv tnum">${esc(amountLabel(quotation.totalAmount,quotation.currency))}</span></div>
        </div></aside></div>
      </div></div>
      <div class="set-savebar">${btn(s('back'),{icon:'chevL',cls:'soft',attrs:'data-back-quotes'})}<div class="grow"></div>${actions}</div>
    </section></div>`;
    root.querySelector('[data-back-quotes]')?.addEventListener('click',()=>navigate('quotations'));
    root.querySelector('[data-view-order]')?.addEventListener('click',()=>navigate('sales-order'));
    root.querySelectorAll('[data-quote-action]').forEach(button=>button.addEventListener('click',async()=>{
      const name=button.dataset.quoteAction; button.disabled=true;
      try{
        const payload=name==='convert-to-order'?{docNo:sequence('SO'),orderDate:today()}:{};
        const result=await adapter().action(
          'sales/quotations',quotation.id,name,payload,`sales-quotation-${quotation.id}-${name}`);
        if(name==='issue') toast(s('issued'),'ok');
        if(name==='accept') toast(s('accepted'),'ok');
        if(name==='convert-to-order'){
          toast(s('orderCreated'),'ok');
          navigate('sales-order',{no:result.data.orderDocNo});
          return;
        }
        navigate('quotation');
      }catch(error){ button.disabled=false; toast(error&&error.message||s('actionFailed'),'danger'); }
    }));
  };

  /* The canonical quotation editor follows the same Header–Detail contract in
     Demo and Production; the older Demo-only CRUD path is not used here. */
  SCREENS['new-quotation']=async function(root){
    const s=copy(),data=await loadFrontData();
    const customers=Array.isArray(data.customers)?data.customers:[];
    const products=Array.isArray(data.products)?data.products:[];
    const company=DB.company||{};
    const defaultTaxCode=company.country==='MY'||company.taxRegime==='SST'?'SV':'SR';
    const fallbackTaxRate=defaultTaxCode==='SV'?8:9;
    const state={
      docNo:sequence('Q'),customerId:Number(customers[0]?.id||0),quoteDate:today(),validUntil:plusDays(30),probability:'50',lines:[],
    };
    const configuredTaxRules=DB.taxRules||DB.erpSystem?.taxRules||DB.dashboardMetrics?.taxRules||
      (DB.erpSystem?.dataMode==='api'?[]:(DB.taxCodes||[]));
    const rawTaxRules=configuredTaxRules.map(row=>({
      code:String(row.taxCode||row.tax_code||row.code||''),
      rate:Number(row.rate??row.taxRate??row.tax_rate??0),
      companyFn:row.companyFn||row.company_fn||null,
      validFrom:String(row.validFrom||row.valid_from||''),
      validTo:String(row.validTo||row.valid_to||''),
    })).filter(row=>row.code&&(!row.companyFn||row.companyFn===DB.erpSystem?.scope?.companyFn));
    const taxRulesFor=onDate=>{
      const active=rawTaxRules.filter(row=>(!row.validFrom||row.validFrom<=onDate)&&(!row.validTo||row.validTo>onDate));
      const rules=active.reduce((resolved,row)=>{
        const existing=resolved.find(item=>item.code===row.code);
        if(!existing||row.validFrom>existing.validFrom){
          if(existing) resolved[resolved.indexOf(existing)]=row; else resolved.push(row);
        }
        return resolved;
      },[]);
      if(!rules.some(row=>row.code===defaultTaxCode)) rules.unshift({code:defaultTaxCode,rate:fallbackTaxRate});
      return rules;
    };
    const taxRateFor=code=>taxRulesFor(state.quoteDate).find(row=>row.code===code)?.rate??0;
    const newLine=lineType=>({lineType,productId:null,description:'',uom:'unit',qty:'1',unitPrice:'0',taxCode:defaultTaxCode});
    state.lines.push(newLine('stock'));
    const number=value=>{const parsed=Number(value);return Number.isFinite(parsed)?parsed:0;};
    const lineNet=line=>Math.max(0,number(line.qty))*Math.max(0,number(line.unitPrice));
    const totals=()=>state.lines.reduce((result,line)=>{const net=lineNet(line);result.net+=net;result.tax+=net*taxRateFor(line.taxCode)/100;return result;},{net:0,tax:0});
    const taxOptions=line=>{
      const options=taxRulesFor(state.quoteDate);
      if(line.taxCode&&!options.some(row=>row.code===line.taxCode)) options.unshift({code:line.taxCode,rate:taxRateFor(line.taxCode)});
      return options.map(row=>`<option value="${esc(row.code)}" ${row.code===line.taxCode?'selected':''}>${esc(row.code)} · ${row.rate}%</option>`).join('');
    };
    const productOptions=line=>`<option value="">${esc(s('selectProduct'))}</option>${products.map(row=>`<option value="${row.id}" ${Number(row.id)===Number(line.productId)?'selected':''}>${esc(row.sku||('#'+row.id))} · ${esc(row.name||'')}</option>`).join('')}`;
    const summary=()=>{
      const t=totals();
      return `<div class="sales-quotation-summary-title">${esc(s('lines'))}<span>${state.lines.length}</span></div><div class="sumrow"><span class="sk2">${esc(s('net'))}</span><span class="sv tnum">${esc(amountLabel(t.net,currency()))}</span></div><div class="sumrow"><span class="sk2">${esc(s('tax'))}</span><span class="sv tnum">${esc(amountLabel(t.tax,currency()))}</span></div><div class="sumrow total"><span class="sk2">${esc(s('total'))}</span><span class="sv tnum">${esc(amountLabel(t.net+t.tax,currency()))}</span></div>`;
    };
    const lineMarkup=(line,index)=>{
      const stock=line.lineType==='stock';
      return `<article class="sales-quotation-line" data-quote-line="${index}">
        <div class="sales-quotation-line-head"><div class="sales-quotation-line-title"><span class="sales-quotation-line-number">${index+1}</span><div><strong>${esc(stock?s('lineTypeStock'):s('lineTypeNonStock'))}</strong><small>${esc(line.description||s(stock?'selectProduct':'lineDescription'))}</small></div></div><button type="button" class="iconbtn sales-quotation-line-remove" data-remove-line aria-label="${esc(s('removeLine'))}" title="${esc(s('removeLine'))}">${ic('trash')}</button></div>
        <div class="sales-quotation-line-mode" role="group" aria-label="${esc(s('addRow'))}"><button type="button" class="sales-quotation-mode-btn ${stock?'is-active':''}" data-line-type="stock">${esc(s('lineTypeStock'))}</button><button type="button" class="sales-quotation-mode-btn ${stock?'':'is-active'}" data-line-type="non_stock">${esc(s('lineTypeNonStock'))}</button></div>
        <div class="sales-quotation-line-grid">
          ${stock?`<div class="sales-quotation-field sales-quotation-field-wide"><label>${esc(s('product'))} <i>*</i></label><select data-line-product>${productOptions(line)}</select></div>`:''}
          <div class="sales-quotation-field sales-quotation-field-wide"><label>${esc(s('lineDescription'))} <i>*</i></label><input data-line-description value="${esc(line.description)}" maxlength="500" placeholder="${esc(s('lineDescription'))}"></div>
          <div class="sales-quotation-field"><label>${esc(s('quantity'))} <i>*</i></label><input data-line-qty type="number" min="0.0001" step="0.0001" inputmode="decimal" value="${esc(line.qty)}"></div>
          <div class="sales-quotation-field"><label>${esc(s('uom'))}</label><input data-line-uom value="${esc(line.uom)}" maxlength="40" ${stock?'readonly':''}></div>
          <div class="sales-quotation-field"><label>${esc(s('unitPrice'))} <i>*</i></label><input data-line-price type="number" min="0" step="0.0001" inputmode="decimal" value="${esc(line.unitPrice)}"></div>
          <div class="sales-quotation-field"><label>${esc(s('taxCode'))}</label><select data-line-tax data-business-text>${taxOptions(line)}</select></div>
          <div class="sales-quotation-field sales-quotation-line-amount"><label>${esc(s('lineAmount'))}</label><output data-line-net>${esc(amountLabel(lineNet(line),currency()))}</output></div>
        </div>
      </article>`;
    };
    const emptyLines=()=>`<div class="sales-quotation-empty-lines"><span class="sales-quotation-empty-icon">${ic('box')}</span><strong>${esc(s('noLines'))}</strong><p>${esc(s('noLinesHelp'))}</p></div>`;
    const refreshLine=(lineElement,line)=>{
      const amount=lineElement.querySelector('[data-line-net]');
      if(amount) amount.textContent=amountLabel(lineNet(line),currency());
      const summaryTarget=root.querySelector('[data-quote-summary]');
      if(summaryTarget) summaryTarget.innerHTML=summary();
    };
    const renderLines=()=>{
      const target=root.querySelector('[data-quote-lines]');
      if(target) target.innerHTML=state.lines.length?state.lines.map(lineMarkup).join(''):emptyLines();
      const count=root.querySelector('[data-quote-line-count]');
      if(count) count.textContent=`${state.lines.length} · ${esc(s('lines'))}`;
      const summaryTarget=root.querySelector('[data-quote-summary]');
      if(summaryTarget) summaryTarget.innerHTML=summary();
      bindLines();
    };
    const bindLines=()=>{
      root.querySelectorAll('[data-quote-line]').forEach(lineElement=>{
        const index=Number(lineElement.dataset.quoteLine),line=state.lines[index];
        if(!line) return;
        lineElement.querySelectorAll('[data-line-type]').forEach(button=>button.addEventListener('click',()=>{
          const nextType=button.dataset.lineType;
          if(nextType===line.lineType) return;
          if(nextType==='stock'){
            if(!products.length){toast(s('lineRequired'),'warn');return;}
            line.lineType='stock';line.productId=null;line.description='';line.uom='unit';
          }else{line.lineType='non_stock';line.productId=null;line.uom=line.uom||'unit';}
          renderLines();
        }));
        lineElement.querySelector('[data-line-product]')?.addEventListener('change',event=>{
          line.productId=Number(event.currentTarget.value)||null;
          const item=products.find(row=>Number(row.id)===line.productId);
          if(item){line.description=item.name||'';line.uom=item.uom||'unit';lineElement.querySelector('[data-line-description]').value=line.description;lineElement.querySelector('[data-line-uom]').value=line.uom;}
          refreshLine(lineElement,line);
        });
        lineElement.querySelector('[data-line-description]')?.addEventListener('input',event=>{line.description=event.currentTarget.value;});
        lineElement.querySelector('[data-line-uom]')?.addEventListener('input',event=>{line.uom=event.currentTarget.value;});
        lineElement.querySelector('[data-line-qty]')?.addEventListener('input',event=>{line.qty=event.currentTarget.value;refreshLine(lineElement,line);});
        lineElement.querySelector('[data-line-price]')?.addEventListener('input',event=>{line.unitPrice=event.currentTarget.value;refreshLine(lineElement,line);});
        lineElement.querySelector('[data-line-tax]')?.addEventListener('change',event=>{line.taxCode=event.currentTarget.value;refreshLine(lineElement,line);});
        lineElement.querySelector('[data-remove-line]')?.addEventListener('click',()=>{state.lines.splice(index,1);renderLines();});
      });
    };
    const addLine=lineType=>{
      if(lineType==='stock'&&!products.length){toast(s('lineRequired'),'warn');return;}
      state.lines.push(newLine(lineType));renderLines();
      root.querySelector(`[data-quote-line="${state.lines.length-1}"] [data-line-${lineType==='stock'?'product':'description'}]`)?.focus();
    };
    root.innerHTML=`<div class="content full"><section class="master">
      <div class="pagehead">${crumbs([DB.company.name,{label:t('nav.sales'),route:'sales-home'},{label:s('quotations'),route:'quotations'},{cur:s('newQuotation')}])}${typeof salesNav==='function'?salesNav('quotations'):''}</div>
      <div class="docwrap"><div class="docpage sales-quotation-authoring-page"><div class="dochead"><div class="dh-row1"><div><div class="dt">${ic('receipt')}${esc(s('newQuotation'))}</div><div class="h1sub">${esc(s('quotationHelp'))}</div></div>${cap(s('draft'),'neutral')}</div></div>
        <div class="panel sales-quotation-header-panel"><div class="panel-h"><h3>${esc(s('details'))}</h3></div><div class="panel-body"><div class="fldrow c2"><div class="fld"><span>${esc(s('quotationNo'))}</span><input id="newQuoteNo" value="${esc(state.docNo)}"></div><div class="fld"><span>${esc(s('customer'))} <i class="req">*</i></span><select id="newQuoteCustomer"><option value="">${esc(s('selectCustomer'))}</option>${customers.map(row=>`<option value="${row.id}" ${Number(row.id)===state.customerId?'selected':''}>${esc(row.code||('#'+row.id))} · ${esc(row.name||'')}</option>`).join('')}</select></div></div><div class="fldrow c3"><div class="fld"><span>${esc(s('quoteDate'))}</span><input id="newQuoteDate" type="date" value="${esc(state.quoteDate)}"></div><div class="fld"><span>${esc(s('validUntil'))}</span><input id="newQuoteValid" type="date" value="${esc(state.validUntil)}"></div><div class="fld"><span>${esc(s('probability'))}</span><input id="newQuoteProbability" type="number" min="0" max="100" step="1" value="${esc(state.probability)}"></div></div></div></div>
        <div class="doclayout sales-quotation-authoring-layout"><div class="docmain"><div class="panel sales-quotation-lines-panel"><div class="panel-h">${ic('box')}<h3>${esc(s('lines'))}</h3><span class="sales-quotation-line-count" data-quote-line-count></span></div><div class="panel-body sales-quotation-lines-body"><div data-quote-lines></div><div class="sales-quotation-add-row-bar"><span>${esc(s('addRow'))}</span><div>${btn(s('addStockLine'),{icon:'plus',cls:'soft',attrs:'data-add-line="stock"'})}${btn(s('addNonStockLine'),{icon:'plus',cls:'soft',attrs:'data-add-line="non_stock"'})}</div></div><p class="sales-quotation-pricing-note">${ic('info')}${esc(s('pricingNote'))}</p></div></div></div><aside class="summary sales-quotation-summary" data-quote-summary>${summary()}</aside></div>
      </div></div><div class="set-savebar sales-quotation-savebar">${btn(s('cancel'),{icon:'chevL',cls:'soft',attrs:'data-cancel-quote'})}<div class="grow"></div>${btn(s('createQuotation'),{icon:'plus',cls:'primary',attrs:'data-create-quote'})}</div>
    </section></div>`;
    root.querySelector('#newQuoteNo')?.addEventListener('input',event=>{state.docNo=event.currentTarget.value;});
    root.querySelector('#newQuoteCustomer')?.addEventListener('change',event=>{state.customerId=Number(event.currentTarget.value)||0;});
    root.querySelector('#newQuoteDate')?.addEventListener('change',event=>{state.quoteDate=event.currentTarget.value;renderLines();});
    root.querySelector('#newQuoteValid')?.addEventListener('change',event=>{state.validUntil=event.currentTarget.value;});
    root.querySelector('#newQuoteProbability')?.addEventListener('input',event=>{state.probability=event.currentTarget.value;});
    root.querySelectorAll('[data-add-line]').forEach(button=>button.addEventListener('click',()=>addLine(button.dataset.addLine)));
    root.querySelector('[data-cancel-quote]')?.addEventListener('click',()=>navigate('quotations'));
    root.querySelector('[data-create-quote]')?.addEventListener('click',async event=>{
      const button=event.currentTarget;
      const invalidIndex=state.lines.findIndex(line=>{
        const validType=line.lineType==='stock'||line.lineType==='non_stock';
        const validIdentity=line.lineType==='stock'?Number.isSafeInteger(Number(line.productId))&&Number(line.productId)>0:!line.productId&&Boolean(line.description.trim());
        return !validType||!validIdentity||!line.uom.trim()||!Number.isFinite(Number(line.qty))||Number(line.qty)<=0||!Number.isFinite(Number(line.unitPrice))||Number(line.unitPrice)<0||!line.taxCode.trim();
      });
      if(!state.customerId){toast(s('customerRequired'),'warn');root.querySelector('#newQuoteCustomer')?.focus();return;}
      if(!state.lines.length||invalidIndex>=0){toast(s('lineRequired'),'warn');root.querySelector(`[data-quote-line="${Math.max(0,invalidIndex)}"] [data-line-${invalidIndex>=0&&state.lines[invalidIndex]?.lineType==='stock'?'product':'description'}]`)?.focus();return;}
      button.disabled=true;
      try{
        const result=await adapter().create('sales/quotations',{
          docNo:state.docNo.trim(),customerId:state.customerId,quoteDate:state.quoteDate,validUntil:state.validUntil,currency:currency(),probability:state.probability,
          lines:state.lines.map(line=>({lineType:line.lineType,productId:line.lineType==='stock'?Number(line.productId):null,description:line.description.trim(),uom:line.uom.trim(),qty:line.qty,unitPrice:line.unitPrice,taxCode:line.taxCode.trim().toUpperCase()})),
        },requestKey());
        toast(s('created'),'ok');openQuote(result.data.id);
      }catch(error){button.disabled=false;toast(error&&error.message||s('createFailed'),'danger');}
    });
    renderLines();
  };
})();
